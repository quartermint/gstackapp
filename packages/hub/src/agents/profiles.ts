/**
 * Agent profiles re-export from shared package
 *
 * This module provides convenient access to agent profile definitions
 * within the hub package.
 */

export {
  AGENT_PROFILE_DEFINITIONS,
  type AgentProfile,
  type AgentProfileId,
  type AllowedTool,
  isAgentProfileId,
} from '@mission-control/shared';
