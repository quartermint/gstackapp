---
phase: 16-prerequisites-stack-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - packages/api/scripts/db-init.ts
autonomous: true
requirements: [PRE-03]
must_haves:
  truths:
    - "CLAUDE.md accurately describes Postgres (Neon) as the primary database, not SQLite"
    - "PROJECT.md stack section reflects the Neon Postgres migration"
    - "Harness SQLite references are preserved (legitimate usage)"
    - "Obsolete db-init.ts script is removed"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate technology stack documentation"
      contains: "Neon"
    - path: ".planning/PROJECT.md"
      provides: "Accurate project constraints and stack"
      contains: "Postgres"
  key_links:
    - from: "CLAUDE.md"
      to: "packages/api/src/db/client.ts"
      via: "Stack documentation matches actual DB driver"
      pattern: "Neon.*Postgres"
---

<objective>
Update CLAUDE.md and PROJECT.md to accurately reflect the SQLite-to-Neon Postgres migration. Remove obsolete db-init.ts script.

Purpose: PRE-03 requires documentation accuracy. CLAUDE.md still describes SQLite as the primary database with 13+ stale references. New developers (or Claude in future sessions) would get incorrect context about the stack.

Output: Accurate documentation, no stale SQLite references in active docs (harness SQLite preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-prerequisites-stack-cleanup/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md technology stack to reflect Neon Postgres</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    packages/api/src/db/client.ts
    packages/api/package.json
  </read_first>
  <action>
  Update the following sections in CLAUDE.md. **DO NOT touch any harness-related SQLite references** -- the harness legitimately uses better-sqlite3.

  **Constraints section (line ~40):**
  - Change `Hono + SQLite + Drizzle + React` to `Hono + Postgres (Neon) + Drizzle + React`

  **Database and ORM table (lines ~61-64):**
  - Replace the `better-sqlite3` row with the actual Postgres driver. Check `packages/api/package.json` for the exact driver package and version (likely `@neondatabase/serverless` or `postgres`).
  - Replace the `sqlite-vec` row: Note that vector search is deferred (cross-repo search tests are skipped). If pgvector is not currently in use, replace with a note: "Vector search: deferred to Phase 20 (DASH-05). Previously sqlite-vec, pgvector TBD."
  - Update the Drizzle row description: change `First-class better-sqlite3 driver` to `First-class Postgres driver via Neon serverless`

  **Alternatives Considered table (lines ~127-130):**
  - Update Database row: Flip the recommendation. Postgres (Neon) is now primary. SQLite was the Phase 1 choice, migrated in commit c1fc394.
  - Update Vector Search row: Note pgvector as the target, sqlite-vec was Phase 1.
  - Update ORM Prisma comparison: Remove `worse SQLite support` -- replace with current reasoning (Drizzle is lighter, type-safer, SQL-first).

  **Sources section (lines ~150-163):**
  - Replace `better-sqlite3 npm` link with Neon serverless driver link
  - Replace `sqlite-vec GitHub` link with note about pgvector (or remove if not in use)
  - Update `Drizzle ORM docs` description: change `SQLite driver` to `Postgres driver`

  **Preserve these references (they are about the harness, not the main API):**
  - Any mention of harness using better-sqlite3 for token tracking
  - The harness package.json dependency on better-sqlite3
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && grep -n "sqlite\|SQLite\|better-sqlite" CLAUDE.md | grep -v -i "harness\|token.track" | head -20</automated>
  </verify>
  <acceptance_criteria>
  - `grep -ci "sqlite" CLAUDE.md` returns a low count (only harness-related mentions remain)
  - `grep -c "Neon\|Postgres\|postgres" CLAUDE.md` returns at least 5 (confirming Postgres is documented)
  - The Constraints line reads `Hono + Postgres (Neon) + Drizzle + React`
  - No `better-sqlite3` references remain outside of harness context
  </acceptance_criteria>
  <done>CLAUDE.md accurately describes Neon Postgres as the primary database. Harness SQLite references preserved. All 13+ stale SQLite references updated or removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md and remove obsolete db-init.ts</name>
  <files>
    .planning/PROJECT.md
    packages/api/scripts/db-init.ts
  </files>
  <read_first>
    .planning/PROJECT.md
    packages/api/scripts/db-init.ts
    packages/api/package.json
  </read_first>
  <action>
  **PROJECT.md updates (4 stale references):**

  1. **Line ~32** (`Cross-repo findings embedded via sqlite-vec`): Change to `Cross-repo findings embedded via pgvector (deferred to Phase 20)` or note current state honestly.

  2. **Line ~89** (`Backend: Hono + SQLite + Drizzle ORM`): Change to `Backend: Hono + Postgres (Neon) + Drizzle ORM (api package) + SQLite (harness token tracking)`

  3. **Line ~92** (`Embeddings: sqlite-vec`): Change to `Embeddings: pgvector (deferred, cross-repo search pending Phase 20)`

  4. **Line ~117** (`sqlite-vec is lightweight` decision): Add note: `Migrated to Neon Postgres in c1fc394. Vector search migration to pgvector deferred to Phase 20 (DASH-05).`

  **Remove obsolete script:**

  Delete `packages/api/scripts/db-init.ts`. This is a SQLite initialization script using better-sqlite3 and sqlite-vec. The main API now uses Drizzle with Neon Postgres -- Drizzle migrations handle schema management. Verify no npm scripts reference it:
  ```bash
  grep -r "db-init" packages/api/package.json
  ```
  If a script references it, remove that script entry too.

  **Run full test suite after deletion** to confirm db-init.ts is truly dead code:
  ```bash
  npm run test --workspace=@gstackapp/api -- --run
  ```
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && grep -n "sqlite\|SQLite" .planning/PROJECT.md | grep -v -i "harness\|token\|migrat\|Phase 20\|deferred\|c1fc394" | head -10</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "sqlite-vec" .planning/PROJECT.md` returns 0 (no active sqlite-vec references)
  - `.planning/PROJECT.md` line ~89 contains "Postgres" not "SQLite" for the backend stack
  - `packages/api/scripts/db-init.ts` does not exist (`test ! -f packages/api/scripts/db-init.ts`)
  - `npm run test --workspace=@gstackapp/api -- --run` passes
  </acceptance_criteria>
  <done>PROJECT.md accurately reflects Neon Postgres migration. Obsolete db-init.ts removed. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Documentation-only changes plus dead code removal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | db-init.ts removal | accept | Script is dead code -- Drizzle migrations are the active schema management path. Removal verified by test suite passing. |
</threat_model>

<verification>
1. `grep -ri "sqlite\|better-sqlite" CLAUDE.md .planning/PROJECT.md | grep -v -i "harness\|token\|migrat\|deferred\|Phase 20\|c1fc394"` returns empty (no stale references)
2. `test ! -f packages/api/scripts/db-init.ts` succeeds
3. Full test suite passes
</verification>

<success_criteria>
- CLAUDE.md technology stack section accurately describes Neon Postgres
- PROJECT.md stack and constraints reflect the migration
- Harness SQLite references are untouched
- Obsolete db-init.ts is removed
- Full test suite passes after changes
</success_criteria>

<output>
After completion, create `.planning/phases/16-prerequisites-stack-cleanup/16-03-SUMMARY.md`
</output>
