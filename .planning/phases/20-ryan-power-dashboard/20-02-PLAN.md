---
phase: 20-ryan-power-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - packages/api/src/routes/pipelines.ts
  - packages/web/src/components/power/TopologyView.tsx
  - packages/web/src/components/power/TopologyFilterBar.tsx
  - packages/web/src/components/power/IdeationWorkspace.tsx
  - packages/web/src/components/power/FlowStepNode.tsx
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Ryan sees all active pipeline runs grouped by repo in the Topology view"
    - "Pipeline stages update in real time via SSE"
    - "Filter controls let Ryan filter by repo and status (All/Running/Complete/Flagged)"
    - "Ideation view shows a horizontal flow diagram header (Office Hours -> CEO Review -> Eng Review -> Execution)"
  artifacts:
    - path: "packages/web/src/components/power/TopologyView.tsx"
      provides: "Cross-repo pipeline topology page with grouped pipeline runs"
    - path: "packages/web/src/components/power/TopologyFilterBar.tsx"
      provides: "Filter bar with repo multi-select and status filter pills"
    - path: "packages/web/src/components/power/IdeationWorkspace.tsx"
      provides: "Enhanced ideation view with flow diagram header"
    - path: "packages/web/src/components/power/FlowStepNode.tsx"
      provides: "Individual flow step circle node with active/complete/pending states"
  key_links:
    - from: "packages/web/src/components/power/TopologyView.tsx"
      to: "/api/pipelines"
      via: "usePipelineList() hook"
      pattern: "usePipelineList"
    - from: "packages/web/src/components/power/TopologyView.tsx"
      to: "packages/web/src/components/pipeline/PipelineTopology.tsx"
      via: "Reuses existing PipelineTopology component per pipeline run"
      pattern: "PipelineTopology"
    - from: "packages/web/src/components/power/IdeationWorkspace.tsx"
      to: "packages/web/src/components/ideation/IdeationView.tsx"
      via: "Wraps existing IdeationView below flow header"
      pattern: "IdeationView"
---

<objective>
Build the pipeline topology cross-repo view (DASH-02) and enhanced ideation workspace (DASH-03).

Purpose: Topology gives Ryan a bird's-eye view of all pipelines running across repos. Ideation workspace adds a visual flow diagram showing where each idea sits in the office-hours -> CEO -> eng -> execution pipeline.
Output: TopologyView with filters, IdeationWorkspace with flow diagram header, wired into App.tsx routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ryan-power-dashboard/20-CONTEXT.md
@.planning/phases/20-ryan-power-dashboard/20-RESEARCH.md
@.planning/phases/20-ryan-power-dashboard/20-UI-SPEC.md
@.planning/phases/20-ryan-power-dashboard/20-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- Executor needs these existing component interfaces -->

From packages/web/src/hooks/usePipelineFeed.ts:
```typescript
export interface PipelineListItem {
  id: string;
  status: string;
  headSha: string;
  startedAt: string | null;
  completedAt: string | null;
  createdAt: string;
  reviewUnit: {
    type: 'pr' | 'push';
    title: string;
    authorLogin: string;
    prNumber: number | null;
    ref: string | null;
  };
  repo: { fullName: string };
  stages: Array<{ stage: string; verdict: string }>;
}
export function usePipelineList(): UseQueryResult<PipelineListItem[]>
```

From packages/web/src/components/pipeline/PipelineTopology.tsx:
```typescript
// Renders 5-stage pipeline visualization with StageNode + StageConnector
// Props include stages array with verdict status
// Import: import { PipelineTopology } from '../pipeline/PipelineTopology'
```

From packages/web/src/components/ideation/IdeationView.tsx:
```typescript
interface IdeationViewProps {
  onLaunchExecution: (sessionId: string) => void;
}
export function IdeationView({ onLaunchExecution }: IdeationViewProps)
```

From packages/web/src/components/layout/Sidebar.tsx (after Plan 01):
```typescript
export type AppView =
  | 'dashboard' | 'trends' | 'repos' | 'ideation'
  | 'autonomous' | 'operator'
  | 'topology' | 'knowledge' | 'intelligence'
```

Stage identity colors (from DESIGN.md):
```
CEO Review: #FF8B3E
Eng Review: #36C9FF
Design Review: #B084FF
QA: #2EDB87
Security: #FF5A67
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TopologyView and TopologyFilterBar</name>
  <files>
    packages/web/src/components/power/TopologyView.tsx,
    packages/web/src/components/power/TopologyFilterBar.tsx,
    packages/web/src/App.tsx
  </files>
  <read_first>
    packages/web/src/hooks/usePipelineFeed.ts,
    packages/web/src/components/pipeline/PipelineTopology.tsx,
    packages/web/src/components/pipeline/StageNode.tsx,
    packages/web/src/components/pipeline/StageConnector.tsx,
    packages/web/src/components/shared/EmptyState.tsx,
    packages/web/src/components/shared/Skeleton.tsx,
    packages/web/src/App.tsx,
    .planning/phases/20-ryan-power-dashboard/20-UI-SPEC.md,
    DESIGN.md
  </read_first>
  <action>
1. **Create `TopologyFilterBar.tsx`** in `packages/web/src/components/power/`:
- Props: `repos: string[]` (unique repo names), `selectedRepos: string[]`, `onRepoChange: (repos: string[]) => void`, `statusFilter: 'all' | 'running' | 'complete' | 'flagged'`, `onStatusChange: (status) => void`
- Layout: horizontal bar, `bg-surface border-b border-border px-6 py-2 flex items-center gap-4`
- Left side: Repo multi-select implemented as a dropdown button that shows a checkable list. Button text: "{N} repos selected" or "All repos". Dropdown: `absolute top-full left-0 mt-1 bg-surface border border-border rounded-md shadow-lg z-10 p-2 min-w-[200px]`. Each repo as a checkbox row.
- Center: Status filter pills as a button group. Each pill: `px-3 py-1 rounded-full text-[13px] font-body`. Active: `bg-accent-muted text-accent`. Inactive: `text-text-muted hover:text-text-primary`. Labels: "All", "Running", "Complete", "Flagged".
- Right side: accent CTA placeholder (not wired yet, just the button styled): `bg-accent text-background rounded-md px-4 py-1.5 font-display font-semibold text-[13px]`, text "Run Pipeline".

2. **Create `TopologyView.tsx`** in `packages/web/src/components/power/`:
- Uses `usePipelineList()` hook to get all pipelines
- State: `selectedRepos: string[]` (default all), `statusFilter: 'all' | 'running' | 'complete' | 'flagged'` (default 'all')
- Derive unique repo list from pipeline data: `Array.from(new Set(pipelines.map(p => p.repo.fullName)))`
- Filter pipelines by selectedRepos and statusFilter:
  - 'running': `status === 'running'`
  - 'complete': `status === 'completed'`
  - 'flagged': any stage has verdict 'FLAG' or 'BLOCK'
- Group filtered pipelines by `repo.fullName`
- Layout: `<div className="flex flex-col h-full">`
  - Header: `<div className="px-8 pt-8 pb-4"><h1 className="font-display text-[24px] font-semibold text-text-primary">Pipeline Topology</h1></div>`
  - `<TopologyFilterBar ... />`
  - Scrollable content: `<div className="flex-1 overflow-y-auto px-8 py-6 space-y-6">`
  - Per repo group: mono label with repo fullName (11px JetBrains Mono uppercase), then each pipeline as a row containing the existing `PipelineTopology` component (or the stage data rendered inline if PipelineTopology expects different props — check the actual component interface)
  - If PipelineTopology is not directly reusable (check its props), render a simplified inline view: horizontal row of `StageNode` components connected by `StageConnector` with the pipeline's stage verdicts
- Empty state: "No active pipelines" heading + "Trigger a pipeline run to see the review topology." body (use `EmptyState` component if available)
- Loading state: `Skeleton` components
- SSE updates happen automatically via `useSSEQuerySync` at App level — query cache invalidation is already wired

3. **Wire into App.tsx**: Replace the placeholder `case 'topology'` from Plan 01 with `return <TopologyView />`. Import TopologyView.
  </action>
  <acceptance_criteria>
    - test -f packages/web/src/components/power/TopologyView.tsx returns 0
    - test -f packages/web/src/components/power/TopologyFilterBar.tsx returns 0
    - grep -c "usePipelineList" packages/web/src/components/power/TopologyView.tsx returns at least 1
    - grep -c "TopologyFilterBar" packages/web/src/components/power/TopologyView.tsx returns at least 1
    - grep -c "repo.fullName" packages/web/src/components/power/TopologyView.tsx returns at least 1
    - grep -c "statusFilter" packages/web/src/components/power/TopologyFilterBar.tsx returns at least 1
    - grep "case 'topology'" packages/web/src/App.tsx returns a match
    - grep -c "TopologyView" packages/web/src/App.tsx returns at least 1
    - grep -c "Pipeline Topology" packages/web/src/components/power/TopologyView.tsx returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit -p packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>TopologyView shows all pipeline runs grouped by repo with real-time SSE updates, TopologyFilterBar filters by repo and status, wired into App.tsx topology route</done>
</task>

<task type="auto">
  <name>Task 2: Build IdeationWorkspace with flow diagram header</name>
  <files>
    packages/web/src/components/power/IdeationWorkspace.tsx,
    packages/web/src/components/power/FlowStepNode.tsx,
    packages/web/src/App.tsx
  </files>
  <read_first>
    packages/web/src/components/ideation/IdeationView.tsx,
    packages/web/src/components/ideation/IdeationPipeline.tsx,
    packages/web/src/components/ideation/IdeationStageNode.tsx,
    packages/web/src/App.tsx,
    .planning/phases/20-ryan-power-dashboard/20-UI-SPEC.md,
    DESIGN.md
  </read_first>
  <action>
1. **Create `FlowStepNode.tsx`** in `packages/web/src/components/power/`:
- Props: `label: string`, `color: string` (stage identity hex), `state: 'active' | 'complete' | 'pending'`
- Render: 40px circle (SVG or div with `w-10 h-10 rounded-full`) + label below (11px mono semibold uppercase)
- States:
  - `active`: `border-2` with `borderColor` set to `color` prop, plus `animate-[pulse-glow_2s_ease-in-out_infinite]` with the stage color as glow. Background `bg-surface`.
  - `complete`: `bg-[rgba(46,219,135,0.2)]` (pass green at 20%), checkmark SVG inside (simple path), label text-text-muted
  - `pending`: `border border-border`, label `text-text-muted/50` (half opacity)
- Keep it simple — CSS-only animation, no JS animation library

2. **Create `IdeationWorkspace.tsx`** in `packages/web/src/components/power/`:
- Props: `onLaunchExecution: (sessionId: string) => void` (pass through to IdeationView)
- Layout: vertical stack
- Top section: flow diagram header, `h-[80px] flex items-center justify-center gap-0 px-8`, `border-b border-border bg-surface`
- Flow steps: 4 `FlowStepNode` components in a horizontal row:
  - "Office Hours" (color: `#FFB020` — amber, reusing FLAG color since office hours is intake/triage)
  - "CEO Review" (color: `#FF8B3E`)
  - "Eng Review" (color: `#36C9FF`)
  - "Execution" (color: `#2EDB87`)
- Between each pair of FlowStepNodes, render a connector line: `<div className="w-12 h-[2px] bg-border" />` (simple horizontal line)
- For now, all steps show as `pending` state (active step detection would require reading ideation session state — that's a future enhancement)
- Below: render `<IdeationView onLaunchExecution={onLaunchExecution} />` (the existing component, unmodified)

3. **Update App.tsx**: Replace the existing `case 'ideation'` to render `<IdeationWorkspace onLaunchExecution={handleLaunchExecution} />` instead of `<IdeationView onLaunchExecution={handleLaunchExecution} />`. Import IdeationWorkspace.
  </action>
  <acceptance_criteria>
    - test -f packages/web/src/components/power/IdeationWorkspace.tsx returns 0
    - test -f packages/web/src/components/power/FlowStepNode.tsx returns 0
    - grep -c "FlowStepNode" packages/web/src/components/power/IdeationWorkspace.tsx returns at least 1
    - grep -c "IdeationView" packages/web/src/components/power/IdeationWorkspace.tsx returns at least 1
    - grep -c "Office Hours" packages/web/src/components/power/IdeationWorkspace.tsx returns at least 1
    - grep -c "CEO Review" packages/web/src/components/power/IdeationWorkspace.tsx returns at least 1
    - grep -c "Eng Review" packages/web/src/components/power/IdeationWorkspace.tsx returns at least 1
    - grep -c "Execution" packages/web/src/components/power/IdeationWorkspace.tsx returns at least 1
    - grep -c "pulse-glow" packages/web/src/components/power/FlowStepNode.tsx returns at least 1
    - grep "IdeationWorkspace" packages/web/src/App.tsx returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit -p packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>IdeationWorkspace wraps existing IdeationView with a horizontal flow diagram showing Office Hours -> CEO Review -> Eng Review -> Execution, FlowStepNode renders circle nodes with active/complete/pending states, App.tsx routes ideation view through IdeationWorkspace</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/pipelines | Pipeline data consumed for topology display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Information Disclosure | TopologyView | accept | Pipeline data already served via /api/pipelines which is behind auth middleware. No new data exposure. |
| T-20-05 | Denial of Service | TopologyView | accept | Client-side grouping of pipelines. Large number of pipelines would slow rendering but not crash server. Pagination deferred. |
</threat_model>

<verification>
- Topology view shows pipeline runs grouped by repo name
- Filter by repo and status works correctly
- IdeationWorkspace shows flow diagram header above existing ideation content
- FlowStepNode renders correctly in all 3 states
- All TypeScript compiles clean
- SSE real-time updates still work (no new SSE connections added)
</verification>

<success_criteria>
- TopologyView renders all pipelines grouped by repo with filtering
- TopologyFilterBar provides repo and status filtering
- IdeationWorkspace shows 4-step flow diagram (Office Hours, CEO Review, Eng Review, Execution)
- Existing IdeationView functionality preserved (passes onLaunchExecution through)
- App.tsx routes correctly to both new views
</success_criteria>

<output>
After completion, create `.planning/phases/20-ryan-power-dashboard/20-02-SUMMARY.md`
</output>
