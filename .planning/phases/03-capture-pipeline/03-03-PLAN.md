---
phase: 03-capture-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - packages/web/src/components/capture/capture-card.tsx
  - packages/web/src/components/capture/capture-correction.tsx
  - packages/web/src/components/loose-thoughts/loose-thoughts.tsx
  - packages/web/src/components/departure-board/project-row.tsx
  - packages/web/src/components/hero/hero-card.tsx
  - packages/web/src/hooks/use-captures.ts
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - CAPT-03
  - CAPT-04
  - CAPT-05
must_haves:
  truths:
    - "Captures appear woven into their assigned project cards on the dashboard"
    - "Unlinked captures (no project match) appear in a 'Loose Thoughts' section below the departure board"
    - "User can correct AI's project assignment by clicking a project badge and selecting from a dropdown"
    - "Correction via dropdown updates the capture's projectId immediately via PATCH"
    - "'Unlink' option in dropdown moves a capture from a project card to loose thoughts"
    - "Captures on project cards are lightweight -- don't clutter the departure board density"
  artifacts:
    - path: "packages/web/src/components/capture/capture-card.tsx"
      provides: "Single capture display with project badge and content preview"
      min_lines: 30
    - path: "packages/web/src/components/capture/capture-correction.tsx"
      provides: "Project reassignment dropdown triggered by clicking project badge"
      min_lines: 40
    - path: "packages/web/src/components/loose-thoughts/loose-thoughts.tsx"
      provides: "Section below departure board showing unlinked captures"
      min_lines: 30
  key_links:
    - from: "packages/web/src/components/departure-board/project-row.tsx"
      to: "packages/web/src/hooks/use-captures.ts"
      via: "Capture count badge on project row"
      pattern: "useCaptures|captureCount"
    - from: "packages/web/src/components/capture/capture-correction.tsx"
      to: "/api/captures/:id"
      via: "PATCH request to update projectId"
      pattern: "PATCH.*captures"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/loose-thoughts/loose-thoughts.tsx"
      via: "LooseThoughts rendered below DepartureBoard"
      pattern: "LooseThoughts"
---

<objective>
Weave captures into the dashboard: show captures on project cards, add a loose thoughts section for unlinked captures, and build the one-click project correction flow. This connects the capture pipeline (Plan 01) and capture UI (Plan 02) to the existing dashboard layout.

Purpose: Captures should appear where they belong -- on project cards -- not in a separate inbox. Unlinked captures get their own lightweight section. Correction is one click.

Output: Capture card component, project correction dropdown, loose thoughts section, updated project rows with capture indicators, and App.tsx integration.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture-pipeline/03-CONTEXT.md
@.planning/phases/03-capture-pipeline/03-RESEARCH.md
@.planning/phases/03-capture-pipeline/03-01-SUMMARY.md
@.planning/phases/03-capture-pipeline/03-02-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 (enrichment services) -->

From packages/api/src/db/schema.ts (after Plan 01):
```typescript
// Captures table now includes:
aiConfidence: real("ai_confidence"),
aiProjectSlug: text("ai_project_slug"),
aiReasoning: text("ai_reasoning"),
linkUrl: text("link_url"),
linkTitle: text("link_title"),
linkDescription: text("link_description"),
linkDomain: text("link_domain"),
linkImage: text("link_image"),
enrichedAt: integer("enriched_at", { mode: "timestamp" }),
```

<!-- Interfaces from Plan 02 (capture UI) -->

From packages/web/src/hooks/use-captures.ts (after Plan 02):
```typescript
interface CaptureItem {
  id: string;
  rawContent: string;
  type: "text" | "voice" | "link" | "image";
  status: "raw" | "pending_enrichment" | "enriched" | "archived";
  projectId: string | null;
  aiConfidence: number | null;
  linkUrl: string | null;
  linkTitle: string | null;
  linkDescription: string | null;
  linkDomain: string | null;
  createdAt: string;
  updatedAt: string;
}
export function useCaptures(projectId?: string): { captures: CaptureItem[]; total: number; loading: boolean; refetch: () => void };
```

<!-- Existing dashboard components -->

From packages/web/src/components/departure-board/project-row.tsx:
```typescript
interface ProjectRowProps {
  project: ProjectItem;
  isSelected: boolean;
  onSelect: (slug: string) => void;
}
// Row shows: name, host badge, branch, relative time, dirty indicator, tagline
```

From packages/web/src/components/hero/hero-card.tsx:
```typescript
// Hero card shows: project name, tagline, GSD state, recent commits mini-timeline
// Extended data from useProjectDetail hook
```

API endpoints available:
- GET /api/captures?projectId=xxx -- captures for a project
- GET /api/captures?limit=10 -- recent captures (filter client-side for null projectId = unlinked)
- PATCH /api/captures/:id -- update projectId for correction
- GET /api/projects -- full project list for correction dropdown
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build capture card and correction components</name>
  <files>
    packages/web/src/components/capture/capture-card.tsx,
    packages/web/src/components/capture/capture-correction.tsx
  </files>
  <action>
    **1. Create capture card** (`packages/web/src/components/capture/capture-card.tsx`):
    - Props: `capture: CaptureItem; projects: ProjectItem[]; onCorrected?: () => void`
    - Display:
      - Raw content text, truncated to 2 lines with `line-clamp-2`
      - If capture has linkUrl + linkTitle: show as a rich link card (small -- title, domain, maybe tiny favicon). If linkTitle is null, show raw URL as monospace text
      - Relative time (use existing `formatRelativeTime` from `../../lib/time.js`)
      - Status indicator: if status is "raw" or "pending_enrichment", show a subtle pulsing dot (terracotta/30) to indicate enrichment in progress
      - Project badge: small pill showing the assigned project name (from `projects` array matched by projectId). If unlinked (projectId null), show "Unlinked" in muted style.
    - Clicking the project badge opens the correction dropdown (CaptureCorrection)
    - Styling: compact, lightweight. Should not compete with the departure board's information density. Use smaller text (text-sm), muted colors, minimal vertical padding.

    **2. Create correction dropdown** (`packages/web/src/components/capture/capture-correction.tsx`):
    - Props: `captureId: string; currentProjectId: string | null; projects: ProjectItem[]; onCorrected: () => void; onClose: () => void`
    - Render as an absolutely positioned dropdown below the project badge
    - Show list of all projects (name + slug), plus "Unlink" option at the bottom
    - Clicking a project: PATCH `/api/captures/${captureId}` with `{ projectId: selectedProject.slug }`, then call `onCorrected()` and `onClose()`
    - Clicking "Unlink": PATCH with `{ projectId: null }`, same callbacks
    - Click outside or Esc closes the dropdown
    - Simple styling: bg-surface-elevated, shadow-lg, rounded-lg, max-h-60 overflow-y-auto (project list could be long)
    - This is per CONTEXT.md: "click project badge on capture, dropdown of all projects, select or Unlink"
  </action>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm typecheck</automated>
  </verify>
  <done>
    - CaptureCard displays capture content, link preview, relative time, status indicator, and project badge
    - CaptureCorrection dropdown shows all projects + Unlink, PATCHes on selection
    - Both components use existing warm theme tokens and are visually compact
  </done>
</task>

<task type="auto">
  <name>Task 2: Add captures to project cards and create loose thoughts section</name>
  <files>
    packages/web/src/components/departure-board/project-row.tsx,
    packages/web/src/components/hero/hero-card.tsx,
    packages/web/src/components/loose-thoughts/loose-thoughts.tsx,
    packages/web/src/hooks/use-captures.ts,
    packages/web/src/App.tsx
  </files>
  <action>
    **1. Add capture count badge to project rows** (`project-row.tsx`):
    - Add optional prop: `captureCount?: number`
    - If captureCount > 0, show a small count badge after the dirty indicator: a terracotta circle with count number (e.g., `3`), similar to notification badges
    - Keep it subtle -- a small `text-[10px]` badge, not competing with the core project status info
    - This is the "woven into project cards" requirement -- captures are discoverable via the badge, detail visible in hero card

    **2. Show captures in hero card** (`hero-card.tsx`):
    - When a project is selected as hero, fetch its captures using `useCaptures(detail?.slug)`
    - Below the existing commits mini-timeline, add a "Recent Captures" section showing up to 3 most recent captures as `CaptureCard` components
    - If the project has no captures, don't show the section (no empty state needed)
    - This is where captures are "woven in" -- they appear contextually when you expand a project
    - Import `CaptureCard` from `../capture/capture-card.js`
    - Pass full project list (from App.tsx via prop or from a new hook) for the correction dropdown

    **3. Create loose thoughts section** (`loose-thoughts.tsx`):
    - Props: `captures: CaptureItem[]; projects: ProjectItem[]; onCorrected: () => void`
    - Renders captures where projectId is null
    - Header: "Loose Thoughts" with a count badge
    - Show each capture as a `CaptureCard` (compact)
    - Smaller visual treatment than the departure board per CONTEXT.md: "visible but not competing with project status"
    - Styling: slightly indented, muted header, bg-surface/50 or similar recessed feel
    - If no unlinked captures, don't render the section at all

    **4. Update captures hook** (`use-captures.ts`):
    - Add `useUnlinkedCaptures(): { captures: CaptureItem[]; loading: boolean; refetch: () => void }`
    - Fetches `GET /api/captures?limit=50` and filters client-side for `projectId === null && status !== 'archived'`
    - (Alternatively, if Plan 01 added query support for null projectId, use that)
    - Add `useProjectCaptureCount(projectId: string): { count: number; loading: boolean }`
    - Lightweight hook that fetches `GET /api/captures?projectId=xxx&limit=1` and reads the `total` from response
    - Used by project rows for the badge count

    **5. Wire everything into App.tsx:**
    - Import `LooseThoughts` from `./components/loose-thoughts/loose-thoughts.js`
    - Fetch unlinked captures using `useUnlinkedCaptures()`
    - Pass `allProjects` (derived from groups) to LooseThoughts for correction dropdown
    - Render `<LooseThoughts>` below the DepartureBoard, above the ErrorBanner
    - Pass capture count data to DepartureBoard/ProjectRow (this may require threading through -- consider adding captureCount to project group rendering or having each ProjectRow fetch its own count)
    - Add `onCorrected` callback that refetches both project captures and unlinked captures

    **Design decision (Claude's discretion per CONTEXT.md):**
    Captures on project cards: Use a count badge on project rows + expanded capture list in the hero card. This keeps the departure board clean ("smarter in 3 seconds" density) while making captures discoverable and detailed when a project is selected. The hero card's recent captures section is the primary capture-viewing surface.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm typecheck && pnpm --filter @mission-control/web test</automated>
  </verify>
  <done>
    - Project rows show capture count badges (CAPT-04: captures woven into project cards)
    - Hero card shows recent captures for the selected project with correction capability
    - Loose thoughts section appears below departure board with unlinked captures (CAPT-05)
    - Clicking project badge on any capture opens correction dropdown (CAPT-03)
    - Selecting a project or "Unlink" PATCHes the capture and refreshes the view
    - Dashboard maintains "smarter in 3 seconds" density -- captures enhance, don't clutter
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes with no errors
- `pnpm --filter @mission-control/web test` passes all tests
- Captures appear on project cards via hero card expansion
- Unlinked captures appear in loose thoughts section
- Correction dropdown works: select project or unlink
</verification>

<success_criteria>
- User can correct AI's project assignment with one click via dropdown (CAPT-03 verified)
- Captures appear woven into project cards (count badge + hero expansion) (CAPT-04 verified)
- Unlinked captures show in "Loose Thoughts" section below departure board (CAPT-05 verified)
</success_criteria>

<output>
After completion, create `.planning/phases/03-capture-pipeline/03-03-SUMMARY.md`
</output>
