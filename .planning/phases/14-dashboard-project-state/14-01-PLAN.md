---
phase: 14-dashboard-project-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/projects.ts
  - packages/shared/src/index.ts
  - packages/api/src/routes/projects.ts
  - packages/api/src/index.ts
  - packages/api/src/__tests__/projects-route.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-06]
must_haves:
  truths:
    - "GET /api/projects returns a JSON array of all detected projects with GSD state, git status, and staleness classification"
    - "Projects with .planning/ directories are auto-detected across home dir (D-01)"
    - "Projects without .planning/ but listed in ~/CLAUDE.md or config still appear with git status (D-03)"
    - "Config file at ~/.gstackapp/projects.json allows add/remove/rename overrides (D-02)"
    - "Each project has a status field: active, stale, or ideating (D-09, DASH-06)"
    - "No background polling — data is read fresh on each request (D-04, D-05)"
  artifacts:
    - path: "packages/shared/src/schemas/projects.ts"
      provides: "Zod schemas for ProjectState, GsdState, GitStatus types"
      exports: ["projectSchema", "gsdStateSchema", "gitStatusSchema", "projectsResponseSchema"]
    - path: "packages/api/src/routes/projects.ts"
      provides: "GET /api/projects — filesystem scanner + git status + staleness algorithm"
      exports: ["default (Hono app)"]
    - path: "packages/api/src/__tests__/projects-route.test.ts"
      provides: "Unit tests for project scanning, STATE.md parsing, staleness algorithm"
  key_links:
    - from: "packages/api/src/routes/projects.ts"
      to: "packages/shared/src/schemas/projects.ts"
      via: "import { projectSchema } from '@gstackapp/shared'"
      pattern: "import.*from.*@gstackapp/shared"
    - from: "packages/api/src/index.ts"
      to: "packages/api/src/routes/projects.ts"
      via: ".route('/projects', projectsApp)"
      pattern: "route.*projects"
---

<objective>
Build the projects API endpoint that scans the filesystem for all projects, reads GSD STATE.md files, runs git status, computes staleness, and returns structured project data.

Purpose: This is the data backbone of the dashboard — every frontend component depends on this endpoint returning accurate, fresh project state on each request.
Output: GET /api/projects endpoint, shared Zod schemas, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dashboard-project-state/14-CONTEXT.md
@.planning/phases/14-dashboard-project-state/14-RESEARCH.md
@.planning/phases/14-dashboard-project-state/14-UI-SPEC.md
@DESIGN.md
@packages/api/src/index.ts
@packages/api/src/routes/repos.ts
@packages/shared/src/index.ts

<interfaces>
<!-- Existing patterns the executor needs -->

From packages/api/src/index.ts:
```typescript
// Route mounting pattern — new routes chain onto apiRoutes
const apiRoutes = new Hono()
  .route('/pipelines', pipelinesApp)
  .route('/repos', reposApp)
  // ... add .route('/projects', projectsApp) here
```

From packages/api/src/routes/repos.ts:
```typescript
// Standard Hono route pattern
import { Hono } from 'hono'
const reposApp = new Hono()
reposApp.get('/', (c) => { ... return c.json(data) })
export default reposApp
```

From packages/shared/src/index.ts:
```typescript
// Re-export pattern
export * from './schemas/verdicts'
export * from './schemas/pipeline'
// ... add export * from './schemas/projects'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared schemas for project state types</name>
  <files>packages/shared/src/schemas/projects.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/index.ts
    - packages/shared/src/schemas/pipeline.ts (for schema pattern reference)
    - .planning/phases/14-dashboard-project-state/14-RESEARCH.md (Zod schema examples)
  </read_first>
  <behavior>
    - gsdProgressSchema validates: total_phases (number), completed_phases (number), total_plans (number, optional), completed_plans (number, optional), percent (number)
    - gsdStateSchema validates: milestone (string, optional), milestone_name (string, optional), status (string, optional), stopped_at (string, optional), last_activity (string, optional), progress (gsdProgressSchema, optional)
    - gitStatusSchema validates: branch (string, nullable), uncommitted (number), ahead (number), behind (number), lastCommitDate (string, nullable), lastCommitMessage (string, nullable)
    - projectSchema validates: name (string), path (string), description (string, optional), gsdState (gsdStateSchema, nullable), gitStatus (gitStatusSchema, nullable), status (enum: active/stale/ideating), hasDesignDocs (boolean)
    - projectsResponseSchema validates array of projectSchema
  </behavior>
  <action>
Create `packages/shared/src/schemas/projects.ts` with Zod schemas per D-09 and the RESEARCH.md schema examples. Define and export: `gsdProgressSchema`, `gsdStateSchema`, `gitStatusSchema`, `projectSchema`, `projectsResponseSchema`. Also export inferred TypeScript types: `GsdProgress`, `GsdState`, `GitStatus`, `ProjectState`.

Add `export * from './schemas/projects'` to `packages/shared/src/index.ts`.

Per D-09, the `status` field is `z.enum(['active', 'stale', 'ideating'])`.

CRITICAL: Use `git add packages/shared/src/schemas/projects.ts packages/shared/src/index.ts` for commits. NEVER use `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit -p packages/shared/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "gsdStateSchema" packages/shared/src/schemas/projects.ts
    - grep -q "projectSchema" packages/shared/src/schemas/projects.ts
    - grep -q "gitStatusSchema" packages/shared/src/schemas/projects.ts
    - grep -q "z.enum.*active.*stale.*ideating" packages/shared/src/schemas/projects.ts
    - grep -q "schemas/projects" packages/shared/src/index.ts
    - Commit only touches files listed in files_modified
  </acceptance_criteria>
  <done>Zod schemas for project state, git status, and staleness classification are exported from @gstackapp/shared and type-check cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Projects API route with filesystem scanning, git status, and staleness algorithm</name>
  <files>packages/api/src/routes/projects.ts, packages/api/src/index.ts, packages/api/src/__tests__/projects-route.test.ts</files>
  <read_first>
    - packages/api/src/routes/repos.ts (route pattern)
    - packages/api/src/index.ts (route mounting)
    - packages/api/src/lib/config.ts (config pattern)
    - .planning/phases/14-dashboard-project-state/14-RESEARCH.md (scanning patterns, staleness algorithm, YAML parsing)
  </read_first>
  <behavior>
    - parseStateMd extracts GSD state from YAML frontmatter: "---\nmilestone: v2.0\nstatus: executing\nlast_activity: 2026-04-08\nprogress:\n  total_phases: 4\n  completed_phases: 2\n  percent: 50\n---" -> { milestone: "v2.0", status: "executing", last_activity: "2026-04-08", progress: { total_phases: 4, completed_phases: 2, percent: 50 } }
    - parseStateMd returns null for invalid/missing frontmatter
    - parseStateMd handles quoted date strings ("2026-04-08T14:55:22.425Z" -> 2026-04-08T14:55:22.425Z)
    - computeStatus returns 'ideating' when hasDesignDocs=true AND hasPlanning=false
    - computeStatus returns 'active' when lastActivity is within 3 days
    - computeStatus returns 'stale' when lastActivity > 3 days AND uncommitted > 0
    - computeStatus returns 'active' (default) when lastActivity > 3 days but uncommitted = 0
    - GET /api/projects returns JSON array of projects with all fields populated
  </behavior>
  <action>
Create `packages/api/src/routes/projects.ts` as a Hono app with GET `/` endpoint:

1. **Project discovery** (per D-01, D-02, D-03):
   - Read config from `~/.gstackapp/projects.json` if it exists (Zod-validated). Config schema: `{ include?: string[], exclude?: string[], overrides?: Record<string, { displayName?: string, group?: string }> }`. If file doesn't exist, use empty defaults.
   - Parse `~/CLAUDE.md` to extract project names from "Active Development" sections using regex: `/^\s*-\s*\*\*(\w[\w-]*)\/?\*\*\s*-\s*(.+)$/gm`
   - Scan `~/` for directories containing `.planning/` (maxdepth 2 — check `~/*/` and `~/projects/*/` style paths, not recursive). Use `readdirSync` + `existsSync`, NOT recursive glob.
   - Merge: config includes + CLAUDE.md projects + .planning/ discoveries. Config excludes remove entries.
   - Also check `~/.gstack/projects/` for design doc directories to identify `hasDesignDocs`.

2. **Data gathering** (per D-04, D-05 — fresh reads, no caching):
   - For each project path, read `.planning/STATE.md` if it exists. Parse YAML frontmatter with `parseStateMd()` — hand-rolled parser extracting specific fields (no yaml library). Handle quoted strings by stripping surrounding quotes.
   - Run `simple-git(projectPath).status()` and `.log({ maxCount: 1 })` for git data. Use bounded parallelism: process 10 projects at a time with `Promise.all` batching.
   - Compute staleness per D-09: active (activity within 3 days), stale (no activity 3+ days AND uncommitted > 0), ideating (has design docs but no .planning/).

3. **Path safety** (per RESEARCH security domain):
   - All project paths resolved with `path.resolve()` and verified to be under `os.homedir()` before reading. Reject any path that resolves outside home.
   - Use `fs.realpathSync()` to prevent symlink escape.

4. **Response**: Return `c.json(projects)` array validated against `projectsResponseSchema`.

Mount in `packages/api/src/index.ts`: add `import projectsApp from './routes/projects'` and chain `.route('/projects', projectsApp)` onto `apiRoutes`.

Create `packages/api/src/__tests__/projects-route.test.ts` with Vitest tests:
- Test `parseStateMd` with valid frontmatter, empty input, malformed YAML, quoted dates
- Test `computeStatus` for all 4 cases (active, stale, ideating, default-active)
- Test path validation rejects paths outside home directory
- Test GET endpoint returns expected shape (mock filesystem with `vi.mock('node:fs')`)

Export `parseStateMd` and `computeStatus` as named exports for testability.

CRITICAL: Use `git add packages/api/src/routes/projects.ts packages/api/src/index.ts packages/api/src/__tests__/projects-route.test.ts` for commits. NEVER use `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx vitest run packages/api/src/__tests__/projects-route.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "parseStateMd" packages/api/src/routes/projects.ts
    - grep -q "computeStatus" packages/api/src/routes/projects.ts
    - grep -q "simple-git\|simpleGit" packages/api/src/routes/projects.ts
    - grep -q "realpathSync\|realpath" packages/api/src/routes/projects.ts
    - grep -q "route.*projects.*projectsApp" packages/api/src/index.ts
    - grep -q "parseStateMd" packages/api/src/__tests__/projects-route.test.ts
    - grep -q "computeStatus" packages/api/src/__tests__/projects-route.test.ts
    - Commit only touches files listed in files_modified
  </acceptance_criteria>
  <done>GET /api/projects returns all detected projects with GSD state, git status, and staleness. Tests pass for STATE.md parsing, staleness algorithm, and path safety validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem reads | API reads arbitrary paths under home directory based on config/CLAUDE.md |
| Config file | ~/.gstackapp/projects.json is user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | projects.ts path resolution | mitigate | Resolve all paths with path.resolve(), verify under homedir() with realpathSync() to prevent symlink escape |
| T-14-02 | Information Disclosure | projects.ts filesystem scan | mitigate | Only scan directories under home dir, never expose file contents beyond STATE.md frontmatter fields |
| T-14-03 | Tampering | projects.json config | mitigate | Validate config with Zod schema, reject malformed input, resolve paths before use |
| T-14-04 | Denial of Service | git status parallelism | mitigate | Bounded parallelism (10 concurrent), short timeout per git operation to prevent hanging |
</threat_model>

<verification>
1. `cd /Users/ryanstern/gstackapp && npx vitest run packages/api/src/__tests__/projects-route.test.ts --reporter=verbose` — all tests pass
2. `cd /Users/ryanstern/gstackapp && npx tsc --noEmit -p packages/shared/tsconfig.json` — shared types compile
3. `curl http://localhost:3000/api/projects` — returns JSON array with project objects containing name, path, gsdState, gitStatus, status fields
</verification>

<success_criteria>
- GET /api/projects returns structured project data for all detected projects
- Projects from ~/CLAUDE.md, .planning/ scan, and config file are merged correctly
- GSD STATE.md YAML frontmatter is parsed without external YAML library
- Git status shows branch, uncommitted count, ahead/behind, last commit date
- Staleness algorithm correctly classifies active/stale/ideating per D-09
- Path traversal/symlink escape is prevented
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-dashboard-project-state/14-01-SUMMARY.md`
</output>
