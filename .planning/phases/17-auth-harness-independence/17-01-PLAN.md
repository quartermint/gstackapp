---
phase: 17-auth-harness-independence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/db/schema.ts
  - packages/api/src/auth/tailscale.ts
  - packages/api/src/auth/magic-link.ts
  - packages/api/src/auth/roles.ts
  - packages/api/src/auth/middleware.ts
  - packages/api/src/routes/auth.ts
  - packages/api/src/index.ts
  - packages/api/src/__tests__/auth-tailscale.test.ts
  - packages/api/src/__tests__/auth-magic-link.test.ts
  - packages/api/src/__tests__/auth-roles.test.ts
  - packages/api/src/__tests__/session-isolation.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup:
  - service: sendgrid
    why: "Magic link email delivery for non-tailnet users"
    env_vars:
      - name: SENDGRID_API_KEY
        source: "SendGrid Dashboard -> Settings -> API Keys -> Create API Key (Mail Send permission)"
      - name: SENDGRID_FROM_EMAIL
        source: "Use whichever sender email is already verified in SendGrid (check Sender Authentication)"
      - name: MAGIC_LINK_SECRET
        source: "Generate with: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
      - name: PUBLIC_URL
        source: "The Tailscale Funnel URL, e.g., https://ryans-mac-mini.tail857f5c.ts.net"
      - name: ADMIN_EMAILS
        source: "Comma-separated admin emails, e.g., ryan@quartermint.com"
      - name: OPERATOR_EMAILS
        source: "Comma-separated operator emails, e.g., ryn@example.com,bella@example.com"

must_haves:
  truths:
    - "A request from a Tailscale 100.x IP is auto-identified via LocalAPI whois and lands on dashboard without login"
    - "A non-tailnet user receives a magic link email, clicks it, and gets an authenticated session cookie"
    - "Each authenticated user is assigned admin or operator role based on their email in the allowlist"
    - "Operators see only their own session data; admin sees all users' data"
  artifacts:
    - path: "packages/api/src/auth/tailscale.ts"
      provides: "Tailscale LocalAPI whois client over Unix socket"
      exports: ["whoisByAddr"]
    - path: "packages/api/src/auth/magic-link.ts"
      provides: "Magic link token generation, hashing, verification, SendGrid email"
      exports: ["generateMagicLinkToken", "verifyMagicLinkToken", "sendMagicLinkEmail"]
    - path: "packages/api/src/auth/roles.ts"
      provides: "Email-to-role mapping from env vars"
      exports: ["resolveRole", "isKnownUser"]
    - path: "packages/api/src/auth/middleware.ts"
      provides: "Hono auth middleware (tailscale -> cookie -> 401)"
      exports: ["authMiddleware"]
    - path: "packages/api/src/routes/auth.ts"
      provides: "POST /auth/magic-link and GET /auth/verify routes"
      exports: ["default"]
    - path: "packages/api/src/db/schema.ts"
      provides: "users, magicLinkTokens, userSessions tables"
      contains: "pgTable('users'"
  key_links:
    - from: "packages/api/src/auth/middleware.ts"
      to: "packages/api/src/auth/tailscale.ts"
      via: "whoisByAddr(remoteAddr)"
      pattern: "whoisByAddr"
    - from: "packages/api/src/auth/middleware.ts"
      to: "packages/api/src/db/schema.ts"
      via: "userSessions table lookup"
      pattern: "userSessions"
    - from: "packages/api/src/routes/auth.ts"
      to: "packages/api/src/auth/magic-link.ts"
      via: "generateMagicLinkToken + sendMagicLinkEmail"
      pattern: "sendMagicLinkEmail"
    - from: "packages/api/src/index.ts"
      to: "packages/api/src/auth/middleware.ts"
      via: "app.use('/api/*', authMiddleware)"
      pattern: "authMiddleware"
---

<objective>
Build the dual-path authentication system: Tailscale auto-detect for tailnet users (zero-friction) and SendGrid magic link for external users. Add database tables for users, sessions, and magic link tokens. Wire auth middleware into all API routes.

Purpose: Foundation for all multi-user features — every subsequent plan depends on knowing who the user is and what role they have.
Output: Auth middleware active on all /api/* routes, users table populated on first auth, session isolation enforced.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-auth-harness-independence/17-CONTEXT.md
@.planning/phases/17-auth-harness-independence/17-RESEARCH.md

@packages/api/src/db/schema.ts
@packages/api/src/index.ts
@packages/api/src/routes/sse.ts
@packages/api/src/events/bus.ts

<interfaces>
<!-- Existing Hono app structure from packages/api/src/index.ts -->
```typescript
// Route mounting pattern — new routes chain into apiRoutes
const apiRoutes = new Hono()
  .route('/pipelines', pipelinesApp)
  // ... existing routes
  .route('/autonomous', autonomousApp)

const app = new Hono()
  .use('/api/*', honoLogger())
  .route('/', webhookApp)
  .route('/', healthApp)
  .route('/api', apiRoutes)

export type AppType = typeof app
```

<!-- Existing schema pattern from packages/api/src/db/schema.ts -->
```typescript
import { pgTable, text, integer, serial, boolean, timestamp, real, index, uniqueIndex } from 'drizzle-orm/pg-core'
// All tables use: nanoid for text IDs, timestamp with timezone, $defaultFn(() => new Date())
```

<!-- Existing DB client from packages/api/src/db/client.ts -->
```typescript
import { db } from '../db/client'  // Drizzle instance, Neon Postgres
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database schema + Tailscale whois + role resolver</name>
  <files>
    packages/api/src/db/schema.ts,
    packages/api/src/auth/tailscale.ts,
    packages/api/src/auth/roles.ts,
    packages/api/src/__tests__/auth-tailscale.test.ts,
    packages/api/src/__tests__/auth-roles.test.ts
  </files>
  <read_first>
    packages/api/src/db/schema.ts,
    packages/api/src/db/client.ts,
    packages/api/src/routes/autonomous.ts,
    .planning/phases/17-auth-harness-independence/17-RESEARCH.md
  </read_first>
  <behavior>
    - Test: whoisByAddr('100.64.1.1') returns { userId, loginName, displayName, nodeName } when socket responds with valid JSON
    - Test: whoisByAddr('192.168.1.1') returns null (non-Tailscale IP, skips socket call)
    - Test: whoisByAddr('100.64.1.1') returns null when socket times out or errors
    - Test: resolveRole('ryan@quartermint.com') returns 'admin' when ADMIN_EMAILS='ryan@quartermint.com'
    - Test: resolveRole('bella@example.com') returns 'operator' when OPERATOR_EMAILS='bella@example.com'
    - Test: resolveRole('unknown@example.com') returns null (not in any allowlist)
    - Test: isKnownUser('ryan@quartermint.com') returns true
    - Test: isKnownUser('stranger@example.com') returns false
  </behavior>
  <action>
    **Schema additions to packages/api/src/db/schema.ts** (append after findingEmbeddings table):

    Add 3 new tables per D-03 and D-04:

    1. `users` table: `id` (text PK, nanoid), `email` (text, unique, not null), `displayName` (text), `role` (text, not null — 'admin' | 'operator'), `source` (text, not null — 'tailscale' | 'magic-link'), `tailscaleNodeName` (text), `lastLoginAt` (timestamp with tz), `createdAt` (timestamp with tz, default now).

    2. `magicLinkTokens` table: `id` (text PK, nanoid), `email` (text, not null), `tokenHash` (text, not null), `expiresAt` (timestamp with tz, not null), `usedAt` (timestamp with tz), `createdAt` (timestamp with tz, default now).

    3. `userSessions` table: `id` (text PK — this IS the cookie value), `userId` (text, not null, references users.id), `expiresAt` (timestamp with tz, not null), `createdAt` (timestamp with tz, default now).

    **Tailscale whois client** (packages/api/src/auth/tailscale.ts):

    Export `whoisByAddr(addr: string): Promise<TailscaleWhoisResult | null>`. Only call the socket if addr starts with '100.' (Tailscale CGNAT range). Use `node:http` `request()` with `socketPath: '/var/run/tailscaled.socket'`, path `/localapi/v0/whois?addr=${addr}:0`. Parse `UserProfile.LoginName`, `UserProfile.DisplayName`, `UserProfile.ID`, `Node.ComputedName`. Timeout at 2000ms. Return null on any error. Export the `TailscaleWhoisResult` interface.

    **Role resolver** (packages/api/src/auth/roles.ts):

    Export `resolveRole(email: string): 'admin' | 'operator' | null`. Read `ADMIN_EMAILS` and `OPERATOR_EMAILS` from `process.env`, split by comma, trim whitespace. Match case-insensitively. Admin check first, then operator. Return null if not found.

    Export `isKnownUser(email: string): boolean` — returns true if resolveRole returns non-null.

    Per D-03: `ADMIN_EMAILS=ryan@quartermint.com`, `OPERATOR_EMAILS=ryn@...,bella@...,andrew@...` (configured via env vars).

    **Tests**: Mock the Unix socket using a local HTTP server in tests. Mock process.env for role tests. Tests must run without actual Tailscale daemon.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npm run test --workspace=@gstackapp/api -- --run -t "tailscale|role"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "pgTable('users'" packages/api/src/db/schema.ts
    - grep -q "pgTable('magic_link_tokens'" packages/api/src/db/schema.ts
    - grep -q "pgTable('user_sessions'" packages/api/src/db/schema.ts
    - grep -q "export async function whoisByAddr" packages/api/src/auth/tailscale.ts
    - grep -q "export function resolveRole" packages/api/src/auth/roles.ts
    - grep -q "export function isKnownUser" packages/api/src/auth/roles.ts
    - grep -q "/var/run/tailscaled.socket" packages/api/src/auth/tailscale.ts
    - Tests pass for tailscale whois and role resolution
  </acceptance_criteria>
  <done>
    Three new DB tables defined in schema.ts. Tailscale whois client returns user identity for 100.x IPs via LocalAPI socket. Role resolver maps emails to admin/operator from env vars. All unit tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Magic link token flow + auth routes + auth middleware</name>
  <files>
    packages/api/src/auth/magic-link.ts,
    packages/api/src/auth/middleware.ts,
    packages/api/src/routes/auth.ts,
    packages/api/src/index.ts,
    packages/api/src/__tests__/auth-magic-link.test.ts,
    packages/api/src/__tests__/session-isolation.test.ts
  </files>
  <read_first>
    packages/api/src/auth/tailscale.ts,
    packages/api/src/auth/roles.ts,
    packages/api/src/db/schema.ts,
    packages/api/src/db/client.ts,
    packages/api/src/index.ts,
    .planning/phases/17-auth-harness-independence/17-RESEARCH.md
  </read_first>
  <behavior>
    - Test: generateMagicLinkToken('test@example.com') returns { token (64-char hex), hash (64-char hex), expiresAt (15 min from now) }
    - Test: verifyMagicLinkToken(token, correctHash) returns true
    - Test: verifyMagicLinkToken('wrong-token', correctHash) returns false
    - Test: POST /auth/magic-link with unknown email returns 403
    - Test: POST /auth/magic-link with known operator email returns 200
    - Test: GET /auth/verify with valid token sets httpOnly cookie and returns 200
    - Test: GET /auth/verify with expired token returns 401
    - Test: GET /auth/verify with already-used token returns 401
    - Test: authMiddleware passes through for valid session cookie
    - Test: authMiddleware returns 401 for missing/invalid cookie (non-tailscale IP)
    - Test: Request scoped to userId only returns that user's data
  </behavior>
  <action>
    **Magic link module** (packages/api/src/auth/magic-link.ts):

    1. `generateMagicLinkToken(email: string)`: Use `randomBytes(32).toString('hex')` for raw token. HMAC-SHA256 hash with `MAGIC_LINK_SECRET` env var. Return `{ token, hash, expiresAt: new Date(Date.now() + 15 * 60 * 1000) }`.

    2. `verifyMagicLinkToken(token: string, storedHash: string)`: Compute HMAC, compare with `timingSafeEqual`. Return boolean.

    3. `sendMagicLinkEmail(email: string, token: string)`: Use `@sendgrid/mail` (install: `npm install @sendgrid/mail --workspace=packages/api`). Build link: `${PUBLIC_URL}/auth/verify?token=${token}&email=${encodeURIComponent(email)}`. Send from `SENDGRID_FROM_EMAIL`. Subject: "Sign in to gstackapp". Include 15-min expiry notice. In dev mode (no SENDGRID_API_KEY), log the link to console instead of sending email.

    **Auth routes** (packages/api/src/routes/auth.ts):

    `POST /magic-link`: Zod validate `{ email: string }`. Check `isKnownUser(email)` — return 403 if not. Generate token, store hash in `magicLinkTokens` table (nanoid for id), send email. Return 200 `{ sent: true }`.

    `GET /verify`: Query params `token` and `email`. Look up unexpired, unused token for that email in `magicLinkTokens`. Verify with `verifyMagicLinkToken`. If valid: mark token `usedAt = new Date()`, upsert user in `users` table (create if first login, update lastLoginAt), create `userSessions` row (nanoid as id = cookie value, 7-day expiry per D-02), set httpOnly secure cookie `gstack_session` via `setCookie()` from `hono/cookie` (httpOnly: true, secure: NODE_ENV==='production', sameSite: 'Lax', maxAge: 7 * 24 * 60 * 60, path: '/'). Redirect to `/`.

    **Auth middleware** (packages/api/src/auth/middleware.ts):

    Use `createMiddleware` from `hono/factory`. Two paths per D-01:

    Path 1 — Tailscale: Get remote IP from `c.req.header('x-forwarded-for')` or socket address. Call `whoisByAddr(ip)`. If returned, call `resolveRole(tsUser.loginName)`. If role found, upsert user in DB, set `c.set('user', { id, email, role, source: 'tailscale' })`, call `next()`.

    Path 2 — Cookie: Get `gstack_session` cookie. Look up in `userSessions` table. Check not expired. If valid, load user from `users` table, set `c.set('user', { id, email, role, source: 'magic-link' })`, call `next()`.

    Neither: Return `c.json({ error: 'Unauthorized' }, 401)`.

    **Wire into index.ts**: Import `authMiddleware`. Add it to `/api/*` routes AFTER the logger but BEFORE route handlers. Exclude `/api/auth/*` and `/api/health` from auth check (add path exclusion in middleware or mount auth routes before middleware). Also mount `authApp` on `/api/auth`.

    **Session isolation** per D-04: Export a helper `getUserScope(c: Context)` from middleware.ts that returns `{ userId: string, role: 'admin' | 'operator' }`. Operators always get `WHERE user_id = ?` scoping. Admin gets no scoping (sees all). This helper will be used by route handlers in Plan 02.

    **Tests**: Mock SendGrid with vi.mock. Mock DB calls for integration tests. Test token lifecycle: generate -> store -> verify -> delete. Test middleware with mocked Tailscale and cookie paths. Test session isolation returns only user's own records.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npm run test --workspace=@gstackapp/api -- --run -t "magic-link|middleware|session"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function generateMagicLinkToken" packages/api/src/auth/magic-link.ts
    - grep -q "export function verifyMagicLinkToken" packages/api/src/auth/magic-link.ts
    - grep -q "timingSafeEqual" packages/api/src/auth/magic-link.ts
    - grep -q "export const authMiddleware" packages/api/src/auth/middleware.ts
    - grep -q "gstack_session" packages/api/src/auth/middleware.ts
    - grep -q "httpOnly.*true" packages/api/src/routes/auth.ts || grep -q "httpOnly" packages/api/src/routes/auth.ts
    - grep -q "authMiddleware" packages/api/src/index.ts
    - grep -q "getUserScope" packages/api/src/auth/middleware.ts
    - npm run test --workspace=@gstackapp/api -- --run passes
  </acceptance_criteria>
  <done>
    Magic link tokens generated with HMAC-SHA256, verified with timingSafeEqual, single-use with 15-min expiry. Auth routes handle magic link request and verification. Auth middleware checks Tailscale first, then cookie, rejects unauthenticated. Session cookie is httpOnly, secure in production, 7-day expiry. getUserScope helper enforces operator isolation. All tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> Tailscale Funnel -> Hono API | Untrusted HTTP from any origin hits auth middleware |
| Tailscale tailnet -> Hono API | Semi-trusted: user is on tailnet but identity needs verification via whois |
| Browser cookie -> Session lookup | Cookie value is an opaque token; must validate against DB |
| Email inbox -> Magic link token | Token delivered via email; attacker could intercept |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Spoofing | authMiddleware | mitigate | Tailscale whois over Unix socket (authoritative identity); cookie verified against userSessions table with expiry check |
| T-17-02 | Spoofing | magic-link.ts | mitigate | HMAC-SHA256 tokens (32 bytes entropy), 15-min expiry, single-use (delete after verify), timingSafeEqual comparison |
| T-17-03 | Spoofing | magic-link.ts | mitigate | Rate limit magic link requests (max 3 per email per 15 min via DB count check) |
| T-17-04 | Tampering | auth routes | mitigate | httpOnly + secure + sameSite=Lax cookie prevents XSS/CSRF theft; session ID is opaque nanoid |
| T-17-05 | Repudiation | auth middleware | mitigate | All auth events (login, magic link request, token verify) logged via pino with userId, email, source, timestamp |
| T-17-06 | Information Disclosure | auth middleware (D-04) | mitigate | getUserScope enforces WHERE user_id=? on all operator queries; admin bypass explicit |
| T-17-07 | Denial of Service | magic-link.ts | accept | SendGrid rate limits (100 free/day) naturally cap email flood; known-user check (isKnownUser) blocks unknown emails |
| T-17-08 | Elevation of Privilege | roles.ts | mitigate | Hardcoded allowlist in env vars (D-03); no self-registration; unknown emails get 403 |
</threat_model>

<verification>
1. Auth middleware is mounted on all /api/* routes (except /api/auth/* and /api/health)
2. Tailscale whois returns user identity for 100.x IPs
3. Magic link flow: request -> email -> click -> session cookie -> authenticated
4. Role assignment matches env var allowlists
5. Session isolation: operator queries scoped to own userId
6. All tests pass: `npm run test --workspace=@gstackapp/api -- --run`
</verification>

<success_criteria>
- Auth middleware active on all protected routes
- Tailscale users auto-identified without login page
- Magic link tokens are HMAC-signed, single-use, 15-min expiry
- Session cookies are httpOnly, 7-day expiry
- Users table populated on first auth
- getUserScope helper available for downstream plans
- All unit and integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-auth-harness-independence/17-01-SUMMARY.md`
</output>
