---
phase: 02-dashboard-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/main.tsx
  - packages/web/src/components/layout/dashboard-layout.tsx
  - packages/web/src/components/hero/hero-card.tsx
  - packages/web/src/components/hero/commit-timeline.tsx
  - packages/web/src/components/departure-board/departure-board.tsx
  - packages/web/src/components/departure-board/project-group.tsx
  - packages/web/src/components/departure-board/project-row.tsx
  - packages/web/src/components/ui/host-badge.tsx
  - packages/web/src/components/ui/gsd-badge.tsx
  - packages/web/src/components/ui/dirty-indicator.tsx
  - packages/web/src/components/ui/loading-skeleton.tsx
  - packages/web/src/components/ui/theme-toggle.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-10
  - DASH-11

must_haves:
  truths:
    - "User sees all projects grouped by Active/Idle/Stale on page load"
    - "Each project row shows name, tagline, host badge, branch, last activity, dirty indicator"
    - "Most recently active project displays as expanded hero card with commit timeline"
    - "Clicking any project row swaps it into the hero position instantly"
    - "Dashboard is readable on mobile with condensed layout"
    - "Warm light mode is the default visual identity"
    - "User can toggle to warm dark mode and preference persists"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Root component wiring layout, hero, and departure board"
      min_lines: 40
    - path: "packages/web/src/components/layout/dashboard-layout.tsx"
      provides: "Page shell with header, theme toggle, health indicator"
      exports: ["DashboardLayout"]
    - path: "packages/web/src/components/hero/hero-card.tsx"
      provides: "Expanded project hero with commit timeline and GSD badge"
      exports: ["HeroCard"]
    - path: "packages/web/src/components/hero/commit-timeline.tsx"
      provides: "Vertical list of 3-5 commits with relative timestamps"
      exports: ["CommitTimeline"]
    - path: "packages/web/src/components/departure-board/departure-board.tsx"
      provides: "Groups container rendering Active/Idle/Stale sections"
      exports: ["DepartureBoard"]
    - path: "packages/web/src/components/departure-board/project-row.tsx"
      provides: "Clickable project row with metadata"
      exports: ["ProjectRow"]
    - path: "packages/web/src/components/ui/theme-toggle.tsx"
      provides: "Dark/light mode toggle button"
      exports: ["ThemeToggle"]
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/hooks/use-projects.ts"
      via: "useProjects hook for grouped project data"
      pattern: "useProjects"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/hooks/use-project-detail.ts"
      via: "useProjectDetail hook for hero card data"
      pattern: "useProjectDetail"
    - from: "packages/web/src/components/departure-board/project-row.tsx"
      to: "packages/web/src/App.tsx"
      via: "onClick callback to set selected project slug"
      pattern: "onSelect|onClick"
    - from: "packages/web/src/components/hero/hero-card.tsx"
      to: "packages/web/src/components/hero/commit-timeline.tsx"
      via: "Renders CommitTimeline with commits array prop"
      pattern: "CommitTimeline"
    - from: "packages/web/src/components/layout/dashboard-layout.tsx"
      to: "packages/web/src/hooks/use-theme.ts"
      via: "useTheme hook for toggle and current mode"
      pattern: "useTheme"
---

<objective>
Build the visible dashboard: hero card, departure board with project rows, responsive layout, and theme toggle. Wire everything together so the user gets the "smarter in 3 seconds" experience.

Purpose: This is the phase deliverable -- the first time the user sees Mission Control as a real product, not a scaffold. Every component renders real data from the API through the hooks and utilities built in Plan 01.
Output: Fully functional dashboard with warm visual identity at all breakpoints, interactive hero card, and dark/light toggle.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-dashboard-core/02-CONTEXT.md
@.planning/phases/02-dashboard-core/02-RESEARCH.md
@.planning/phases/02-dashboard-core/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01. Executor uses these directly -- no codebase exploration needed. -->

From packages/web/src/lib/grouping.ts (Plan 01):
```typescript
export type ActivityGroup = "active" | "idle" | "stale";

export interface ProjectItem {
  slug: string;
  name: string;
  tagline: string | null;
  host: "local" | "mac-mini";
  branch: string | null;
  dirty: boolean | null;
  dirtyFiles: string[];
  lastCommitHash: string | null;
  lastCommitMessage: string | null;
  lastCommitTime: string | null;
  lastCommitDate: string | null;
  lastScannedAt: string | null;
}

export interface GroupedProjects {
  active: ProjectItem[];
  idle: ProjectItem[];
  stale: ProjectItem[];
}

export function groupProjectsByActivity(projects: ProjectItem[]): GroupedProjects;
```

From packages/web/src/lib/time.ts (Plan 01):
```typescript
export function formatRelativeTime(isoDate: string | null | undefined): string;
```

From packages/web/src/hooks/use-theme.ts (Plan 01):
```typescript
export function useTheme(): {
  theme: "light" | "dark";
  toggle: () => void;
};
```

From packages/web/src/hooks/use-projects.ts (Plan 01):
```typescript
export function useProjects(): {
  groups: GroupedProjects | null;
  loading: boolean;
  error: string | null;
};
```

From packages/web/src/hooks/use-project-detail.ts (Plan 01):
```typescript
interface ProjectDetail {
  slug: string;
  name: string;
  tagline: string | null;
  host: "local" | "mac-mini";
  branch: string | null;
  dirty: boolean | null;
  dirtyFiles: string[];
  commits: Array<{ hash: string; message: string; relativeTime: string; date: string }>;
  gsdState: { status: string; stoppedAt: string | null; percent: number | null } | null;
}

export function useProjectDetail(slug: string | null): {
  detail: ProjectDetail | null;
  loading: boolean;
};
```

From packages/web/src/app.css (Plan 01 theme tokens):
```
Tailwind utility classes available:
- bg-surface, bg-surface-warm, bg-surface-elevated (light backgrounds)
- bg-surface-dark, bg-surface-warm-dark, bg-surface-elevated-dark (dark backgrounds)
- text-text-primary, text-text-secondary, text-text-muted (light text)
- text-text-primary-dark, text-text-secondary-dark, text-text-muted-dark (dark text)
- bg-terracotta, text-terracotta (accent)
- bg-amber-warm, text-amber-warm (highlight)
- text-sage, bg-sage (success/active status)
- text-gold-status (warning/idle status)
- text-rust, bg-rust (dirty/stale status)
- font-sans, font-mono (font stacks)
- dark:* variant for dark mode overrides
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard UI components</name>
  <files>
    packages/web/src/components/layout/dashboard-layout.tsx,
    packages/web/src/components/hero/hero-card.tsx,
    packages/web/src/components/hero/commit-timeline.tsx,
    packages/web/src/components/departure-board/departure-board.tsx,
    packages/web/src/components/departure-board/project-group.tsx,
    packages/web/src/components/departure-board/project-row.tsx,
    packages/web/src/components/ui/host-badge.tsx,
    packages/web/src/components/ui/gsd-badge.tsx,
    packages/web/src/components/ui/dirty-indicator.tsx,
    packages/web/src/components/ui/loading-skeleton.tsx,
    packages/web/src/components/ui/theme-toggle.tsx
  </files>
  <action>
    Create all dashboard components. Each component receives typed props and renders with Tailwind v4 utilities from the warm theme. All components use `dark:` variant for dark mode support.

    **Layout:**

    1. **`dashboard-layout.tsx`** -- Page shell wrapping all content:
       - Full-width wrapper with `bg-surface dark:bg-surface-dark text-text-primary dark:text-text-primary-dark font-sans min-h-screen`
       - Header with "Mission Control" title (text-2xl font-bold), health indicator dot (green if API responded), and ThemeToggle button
       - Max width container (max-w-5xl mx-auto) with responsive padding (px-4 sm:px-6 lg:px-8 py-6)
       - Props: `{ children: React.ReactNode, healthOk: boolean }`

    **Hero Card:**

    2. **`hero-card.tsx`** -- Expanded hero for selected project:
       - Props: `{ detail: ProjectDetail | null, loading: boolean }`
       - When loading: show loading skeleton (pulse animation)
       - When null: show nothing (initial state before first load)
       - Card with `bg-surface-elevated dark:bg-surface-elevated-dark rounded-xl p-6 shadow-sm` and warm border
       - Project name (text-xl font-bold), tagline (text-text-secondary), host badge, branch (font-mono text-sm)
       - GSD badge if gsdState exists
       - Dirty indicator if dirty
       - Renders CommitTimeline for the commits array
       - Responsive: on mobile (<640px) condense to name + last commit only. Hide tagline and branch

    3. **`commit-timeline.tsx`** -- Vertical commit list:
       - Props: `{ commits: Array<{ hash: string; message: string; relativeTime: string; date: string }> }`
       - Vertical list with a subtle left border line (border-l-2 border-terracotta/30)
       - Each commit: small dot on the line, hash in mono (text-xs font-mono text-text-muted), message (text-sm), relative time (text-xs text-text-muted)
       - Use `formatRelativeTime(commit.date)` for time display (client-rendered, not the raw relativeTime from git)
       - Show up to 5 commits, no "show more"

    **Departure Board:**

    4. **`departure-board.tsx`** -- Container for all groups:
       - Props: `{ groups: GroupedProjects, selectedSlug: string | null, onSelect: (slug: string) => void }`
       - Renders three `ProjectGroup` components in order: active, idle, stale
       - Skip rendering a group if it has zero projects (no empty "Active" header with nothing in it)
       - No collapsible sections per CONTEXT.md -- all groups always expanded

    5. **`project-group.tsx`** -- Single activity group:
       - Props: `{ title: string, count: number, projects: ProjectItem[], selectedSlug: string | null, onSelect: (slug: string) => void, variant: "active" | "idle" | "stale" }`
       - Group header: title text (uppercase, text-xs font-semibold tracking-wider text-text-secondary), count badge
       - Color accent on header varies by variant: terracotta for active, gold-status for idle, text-muted for stale
       - List of ProjectRow components

    6. **`project-row.tsx`** -- Individual clickable project row:
       - Props: `{ project: ProjectItem, isSelected: boolean, onSelect: (slug: string) => void }`
       - Clickable (cursor-pointer, role="button")
       - Selected state: `bg-surface-warm dark:bg-surface-warm-dark` background, subtle left border accent
       - Two-line layout per CONTEXT.md:
         - Line 1: project name (font-medium), host badge, branch (font-mono text-xs), relative time (text-xs text-text-muted), dirty indicator
         - Line 2: tagline (text-sm text-text-secondary, truncated)
       - Hover: subtle background change
       - Click fires onSelect(project.slug) -- no animation, no confirmation per CONTEXT.md
       - **Responsive (mobile <640px):** Hide tagline, branch, host badge. Show only: name, relative time, dirty indicator. Use `hidden sm:inline` pattern

    **UI Components:**

    7. **`host-badge.tsx`** -- Local vs Mac Mini indicator:
       - Props: `{ host: "local" | "mac-mini" }`
       - Small pill: "local" in muted style, "mac-mini" in slightly more visible style
       - text-xs rounded-full px-2 py-0.5

    8. **`gsd-badge.tsx`** -- GSD phase state badge:
       - Props: `{ gsdState: { status: string; stoppedAt: string | null; percent: number | null } }`
       - Small badge showing status text, e.g., "Phase 2: Planning" derived from stoppedAt field, or just status if no stoppedAt
       - bg-amber-warm/10 text-amber-warm text-xs rounded px-2 py-0.5

    9. **`dirty-indicator.tsx`** -- Uncommitted changes indicator:
       - Props: `{ dirty: boolean | null, fileCount?: number }`
       - When dirty: small rust-colored dot or asterisk with optional tooltip showing file count
       - When not dirty or null: render nothing
       - text-rust font-bold

    10. **`loading-skeleton.tsx`** -- Skeleton loading states:
        - Export `HeroSkeleton` -- placeholder matching hero card dimensions with pulse animation
        - Export `BoardSkeleton` -- placeholder matching departure board with 3-5 row skeletons
        - Use `animate-pulse bg-surface-warm dark:bg-surface-warm-dark rounded` patterns

    11. **`theme-toggle.tsx`** -- Dark/light mode toggle button:
        - Props: `{ theme: "light" | "dark", onToggle: () => void }`
        - Simple button with sun/moon icon (inline SVG, not a library)
        - Sun icon for light mode (click to switch to dark), moon for dark mode
        - Accessible: aria-label="Switch to dark/light mode"
        - Subtle styling: no background, just icon with hover color change

    CRITICAL: All components must use the Tailwind utility classes from the theme (bg-surface, text-text-primary, etc.) with dark: variants. Do NOT use hardcoded hex colors in className strings. Do NOT use Tailwind v3 color names (gray-100, etc.) -- use the custom theme tokens.

    CRITICAL: Text truncation -- apply `truncate` (Tailwind utility) and `min-w-0` on flex children for project names and branch names to prevent mobile overflow (per research pitfall #5).
  </action>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm typecheck</automated>
  </verify>
  <done>
    All 11 component files exist with typed props. Components use warm theme tokens with dark: variants. Responsive classes applied. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx with state management, data flow, and responsive layout</name>
  <files>
    packages/web/src/App.tsx,
    packages/web/src/main.tsx
  </files>
  <action>
    **Rewrite `packages/web/src/App.tsx`** to be the real dashboard:

    1. **State management:**
       - `selectedSlug: string | null` -- starts null, set to most recently active project on data load
       - Use `useProjects()` hook for grouped project data
       - Use `useProjectDetail(selectedSlug)` for hero card data
       - Use `useTheme()` for dark/light mode
       - Also fetch `/api/health` for the health indicator (simple useState + useEffect)

    2. **Initial hero selection:**
       - When `useProjects()` returns data and `selectedSlug` is null, auto-select the first project in the `active` group (most recently active per sorted order)
       - If no active projects, select first in idle, then stale
       - If no projects at all, selectedSlug stays null

    3. **Layout composition:**
       ```
       <DashboardLayout healthOk={healthOk}>
         {loading ? <HeroSkeleton /> : <HeroCard detail={detail} loading={detailLoading} />}
         {loading ? <BoardSkeleton /> : groups && (
           <DepartureBoard
             groups={groups}
             selectedSlug={selectedSlug}
             onSelect={setSelectedSlug}
           />
         )}
         {error && <ErrorBanner message={error} />}
         {!loading && groups && totalProjects === 0 && <EmptyState />}
       </DashboardLayout>
       ```

    4. **Error state:** Simple banner at bottom with error message and retry hint. Style: bg-rust/10 text-rust rounded p-4.

    5. **Empty state:** When no projects exist, show a centered message: "No projects configured. Add projects to mc.config.json and restart the API." Style warmly, not sterile.

    6. **Responsive structure:**
       - Hero card and departure board stack vertically (no sidebars)
       - Hero has margin-bottom (mb-6 sm:mb-8) before departure board
       - The layout already handles max-width via DashboardLayout

    7. **`main.tsx`** -- keep as-is, no changes needed. It already renders `<App />` into root.

    IMPORTANT: The hero swap MUST be instant -- clicking a project row calls `setSelectedSlug(slug)` which triggers `useProjectDetail` to fetch the new project. Show a brief loading state in the hero card while fetching, but do NOT unmount the hero card or show a full-page loader. The departure board remains interactive during hero loading.

    IMPORTANT: Do NOT add React Router. This is a single-page dashboard, not a multi-page app.

    IMPORTANT: The "smarter in 3 seconds" goal means everything must be above the fold on desktop. Do NOT add excessive spacing or padding that pushes the departure board below the fold.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm typecheck && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    App.tsx orchestrates all components with real data flow. Hero defaults to most recently active project. Click on any project row swaps hero instantly. Loading and error states render appropriately. Empty state shown when no projects configured. TypeScript compiles. Production build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete dashboard</name>
  <files>packages/web/src/App.tsx</files>
  <action>
    Human verifies the complete Phase 2 dashboard visually at all breakpoints.
    What was built: Complete dashboard with departure board (Active/Idle/Stale groups), hero card with commit timeline and GSD badge, warm visual identity (light default + dark mode), responsive layout, and interactive hero swapping.
  </action>
  <verify>
    1. Start the dev server: `cd ~/mission-control && pnpm dev`
    2. Open http://localhost:5173 in a desktop browser (>1024px wide)
       - Verify: Warm light mode (cream/parchment background, NOT dark, NOT sterile white)
       - Verify: Hero card at top shows most recently active project with commit timeline
       - Verify: Below hero, departure board shows projects grouped under Active/Idle/Stale headers
       - Verify: Each project row shows name, tagline, host badge, branch, relative time, dirty indicator
       - Verify: Everything is above the fold (no scrolling needed for overview)
    3. Click a different project row
       - Verify: Hero card swaps instantly to show that project's details (no animation delay)
    4. Click the theme toggle (sun/moon icon in header)
       - Verify: Dashboard switches to warm dark mode (dark brown backgrounds, warm cream text)
       - Refresh the page -- verify dark mode persists (no flash of light mode)
    5. Resize browser to mobile width (<640px)
       - Verify: Project rows show only name, time, dirty indicator (no tagline, branch, or host badge)
       - Verify: Hero card is condensed but present
       - Verify: No horizontal scrolling
    6. Resize to tablet (640-1024px)
       - Verify: Full layout visible, just narrower spacing
  </verify>
  <done>User approves dashboard appearance and interactivity at all breakpoints. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
Phase 2 acceptance checks:
- `pnpm test` -- all API and web tests pass
- `pnpm typecheck` -- no type errors
- `pnpm build` -- production build succeeds
- Visual: warm light default, dark mode toggle, responsive at all breakpoints
- Functional: hero swaps on row click, groups by activity, commit timeline renders
- Performance: dashboard loads in under 1 second (no additional dependencies added)
</verification>

<success_criteria>
- Dashboard loads showing all projects grouped by Active/Idle/Stale (DASH-01)
- Each project row shows name, tagline, host badge, branch, time, dirty indicator (DASH-02)
- Hero card displays commit timeline and GSD badge for selected project (DASH-03)
- Clicking any project row swaps hero position instantly (DASH-04)
- Mobile layout hides non-essential metadata, no horizontal scroll (DASH-10)
- Warm light default, dark toggle with persistence, Arc browser energy (DASH-11)
- All tests pass, typecheck clean, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-core/02-02-SUMMARY.md`
</output>
