---
phase: 12-session-ingestion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/settings.json
autonomous: false
requirements: [SESS-01, SESS-06]

must_haves:
  truths:
    - "Claude Code settings.json has HTTP hooks for SessionStart, PostToolUse (Write|Edit), and Stop pointing to MC API on Mac Mini"
    - "New hooks coexist with existing command hooks (PreToolUse, PostToolUse, Stop, SessionStart) without replacing them"
    - "Hook timeout is 5 seconds to prevent blocking Claude Code if MC is unreachable"
  artifacts:
    - path: "~/.claude/settings.json"
      provides: "HTTP hook configuration for session ingestion"
      contains: "http://100.123.8.125:3000/api/sessions"
  key_links:
    - from: "~/.claude/settings.json"
      to: "packages/api/src/routes/sessions.ts"
      via: "HTTP POST to hook endpoints"
      pattern: "100.123.8.125:3000/api/sessions"
---

<objective>
Configure Claude Code HTTP hooks in settings.json to send session lifecycle events to the MC API on Mac Mini. These hooks are the client-side configuration that enables SESS-01 (session reporting).

Purpose: Without this configuration, Claude Code sessions are invisible to MC. The hooks send SessionStart, PostToolUse (Write|Edit only), and Stop events as HTTP POSTs to the session API.

Output: Updated ~/.claude/settings.json with 3 new HTTP hook entries.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-session-ingestion/CONTEXT.md
@.planning/phases/12-session-ingestion/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HTTP hooks for session ingestion to Claude Code settings.json</name>
  <files>~/.claude/settings.json</files>
  <read_first>
    - ~/.claude/settings.json (MUST read current state -- has 6+ existing hooks that MUST be preserved)
    - .planning/phases/12-session-ingestion/12-RESEARCH.md (Pattern 5: HTTP Hook Configuration section for exact JSON structure)
  </read_first>
  <action>
**Modify `~/.claude/settings.json` to add 3 HTTP hook entries. CRITICAL: preserve ALL existing hooks.**

The current settings.json has these hook arrays:
- `hooks.PreToolUse`: 3 entries (Bash safety, Write safety, Task log)
- `hooks.PostToolUse`: 2 entries (context-warning.sh, gsd-context-monitor.js)
- `hooks.Stop`: 1 entry (session-summary.sh)
- `hooks.SessionStart`: 3 entries (gsd-check-update.js, worklog-digest.sh, risks-digest.sh)

**Add to `hooks.SessionStart` array (append as new entry):**
```json
{
  "hooks": [
    {
      "type": "http",
      "url": "http://100.123.8.125:3000/api/sessions/hook/start",
      "timeout": 5
    }
  ]
}
```

**Add to `hooks.PostToolUse` array (append as new entry):**
```json
{
  "matcher": "Write|Edit",
  "hooks": [
    {
      "type": "http",
      "url": "http://100.123.8.125:3000/api/sessions/hook/heartbeat",
      "timeout": 5
    }
  ]
}
```

**Add to `hooks.Stop` array (append as new entry):**
```json
{
  "hooks": [
    {
      "type": "http",
      "url": "http://100.123.8.125:3000/api/sessions/hook/stop",
      "timeout": 5
    }
  ]
}
```

After modification:
- `hooks.SessionStart` should have 4 entries (3 existing + 1 new HTTP)
- `hooks.PostToolUse` should have 3 entries (2 existing + 1 new HTTP with Write|Edit matcher)
- `hooks.Stop` should have 2 entries (1 existing + 1 new HTTP)
- `hooks.PreToolUse` should be UNCHANGED (3 entries)
- All other settings.json keys (env, permissions, statusLine, enabledPlugins, etc.) UNCHANGED
  </action>
  <verify>
    <automated>cat ~/.claude/settings.json | node -e "const s=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const ok = s.hooks.SessionStart.length >= 4 && s.hooks.PostToolUse.length >= 3 && s.hooks.Stop.length >= 2 && JSON.stringify(s.hooks).includes('100.123.8.125:3000/api/sessions'); console.log(ok ? 'PASS' : 'FAIL: hook counts wrong')"</automated>
  </verify>
  <acceptance_criteria>
    - ~/.claude/settings.json hooks.SessionStart array contains an entry with type "http" and url "http://100.123.8.125:3000/api/sessions/hook/start"
    - ~/.claude/settings.json hooks.PostToolUse array contains an entry with matcher "Write|Edit" and type "http" and url containing "hook/heartbeat"
    - ~/.claude/settings.json hooks.Stop array contains an entry with type "http" and url containing "hook/stop"
    - All 3 new HTTP hook entries have timeout: 5
    - Existing hooks.PreToolUse array has exactly 3 entries (Bash, Write, Task) -- unchanged
    - Existing hooks.SessionStart still contains gsd-check-update.js, worklog-digest.sh, risks-digest.sh
    - Existing hooks.PostToolUse still contains context-warning.sh and gsd-context-monitor.js
    - Existing hooks.Stop still contains session-summary.sh
    - settings.json is valid JSON (node -e "JSON.parse(require('fs').readFileSync(...))")
  </acceptance_criteria>
  <done>Claude Code settings.json has 3 HTTP hooks pointing to MC API on Mac Mini (:3000). New hooks coexist with existing command hooks. 5-second timeout prevents blocking if MC is unreachable.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify HTTP hooks coexist with existing Claude Code hooks</name>
  <files>~/.claude/settings.json</files>
  <action>No automated action -- human verifies that new hooks coexist with existing hooks in a new Claude Code session.</action>
  <what-built>HTTP hooks configured in ~/.claude/settings.json for session ingestion. When MC API is running on Mac Mini, Claude Code will automatically POST session lifecycle events (start, heartbeat on Write/Edit, stop) to the session API.</what-built>
  <how-to-verify>
    1. Open a new Claude Code session (the current session won't pick up hook changes)
    2. Check console output -- if MC API is running on Mac Mini (http://100.123.8.125:3000), hooks will POST to it
    3. If MC API is NOT running yet, hooks will timeout silently (5s timeout, non-blocking)
    4. Verify hooks don't interfere with existing behavior: safety hooks, context monitor, GSD status line should all still work
    5. Run `cat ~/.claude/settings.json | python3 -m json.tool` to verify valid JSON with all hooks present
  </how-to-verify>
  <verify>User confirms hooks work correctly in a new session</verify>
  <done>User confirms hooks are configured and coexist with existing hooks without interference.</done>
  <resume-signal>Type "approved" if hooks look correct, or describe any issues with existing hook behavior</resume-signal>
</task>

</tasks>

<verification>
- `cat ~/.claude/settings.json | python3 -m json.tool` outputs valid JSON
- `grep -c "100.123.8.125:3000/api/sessions" ~/.claude/settings.json` returns 3
- Existing hooks (bash-safety.sh, write-safety.sh, context-warning.sh, session-summary.sh, gsd-check-update.js) are all still present
</verification>

<success_criteria>
- Settings.json has 3 HTTP hooks for session ingestion (start, heartbeat, stop)
- All existing hooks preserved and functional
- Hooks use 5-second timeout
- PostToolUse hook narrowed to Write|Edit only (no Bash/Read/Grep flooding)
</success_criteria>

<output>
After completion, create `.planning/phases/12-session-ingestion/12-03-SUMMARY.md`
</output>
