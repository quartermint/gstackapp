---
phase: 15-ideation-funnel-autonomous-gsd
plan: 05
type: execute
wave: 3
depends_on:
  - 15-03
  - 15-04
files_modified:
  - packages/web/src/components/session/SessionTabBar.tsx
  - packages/web/src/components/session/SessionTab.tsx
  - packages/web/src/components/ideation/RepoScaffoldForm.tsx
  - packages/web/src/components/ideation/StackSelector.tsx
  - packages/web/src/hooks/useSessionTabs.ts
  - packages/web/src/components/layout/Shell.tsx
  - packages/web/src/components/layout/Sidebar.tsx
  - packages/web/src/App.tsx
autonomous: false
requirements:
  - SESS-02
  - IDEA-04
must_haves:
  truths:
    - "User can open multiple concurrent sessions as tabs at the top of the main content area"
    - "Each tab shows project name and status indicator (thinking=cyan pulse, waiting=amber, idle=muted)"
    - "Switching tabs preserves full conversation state without data loss"
    - "User can scaffold a new repo from ideation output via modal form"
    - "Sidebar includes Ideation nav item and DecisionQueue section"
    - "App routes include ideation and autonomous views"
  artifacts:
    - path: "packages/web/src/components/session/SessionTabBar.tsx"
      provides: "Horizontal tab strip for concurrent session management"
      exports: ["SessionTabBar"]
    - path: "packages/web/src/hooks/useSessionTabs.ts"
      provides: "Hook managing multi-tab session state with per-tab type tracking"
      exports: ["useSessionTabs"]
    - path: "packages/web/src/components/ideation/RepoScaffoldForm.tsx"
      provides: "Modal form for creating new repo from ideation output"
      exports: ["RepoScaffoldForm"]
    - path: "packages/web/src/App.tsx"
      provides: "Updated app with ideation, autonomous, and multi-tab routing"
      exports: ["App"]
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/ideation/IdeationView.tsx"
      via: "AppView routing switch case"
      pattern: "IdeationView"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/autonomous/AutonomousView.tsx"
      via: "AppView routing switch case"
      pattern: "AutonomousView"
    - from: "packages/web/src/components/layout/Shell.tsx"
      to: "packages/web/src/components/session/SessionTabBar.tsx"
      via: "Grid row insertion between sidebar and main content"
      pattern: "SessionTabBar"
---

<objective>
Multi-tab sessions, repo scaffolding modal, and full app integration -- wiring all Phase 15 components into the existing Shell, Sidebar, and App.

Purpose: Complete the user experience by adding concurrent session tabs (SESS-02), repo scaffolding from ideation (IDEA-04), and integrating all new views (ideation, autonomous) into the app shell with proper navigation. This is the wiring plan that connects Plans 01-04 into a cohesive product.

Output: SessionTabBar, RepoScaffoldForm, updated Shell with tab bar, updated Sidebar with ideation nav + decision queue, updated App with new view routing.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ideation-funnel-autonomous-gsd/15-CONTEXT.md
@.planning/phases/15-ideation-funnel-autonomous-gsd/15-UI-SPEC.md
@.planning/phases/15-ideation-funnel-autonomous-gsd/15-03-SUMMARY.md
@.planning/phases/15-ideation-funnel-autonomous-gsd/15-04-SUMMARY.md

@packages/web/src/App.tsx
@packages/web/src/components/layout/Shell.tsx
@packages/web/src/components/layout/Sidebar.tsx
@packages/web/src/hooks/useSession.ts
@DESIGN.md

<interfaces>
<!-- Key types from existing codebase + Plans 03/04 -->

From packages/web/src/components/layout/Sidebar.tsx:
```typescript
export type AppView = 'projects' | 'pr-reviews' | 'trends' | 'repos' | 'design-docs' | 'session'
```

From packages/web/src/components/layout/Shell.tsx:
```typescript
interface ShellProps {
  children: ReactNode; activeView: AppView; onNavigate: (view: AppView) => void;
  sessions?: Session[]; activeSessionId?: string | null;
  onSelectSession?: (id: string) => void; onNewSession?: () => void;
  onSelectProject?: (name: string) => void;
}
```

From packages/web/src/hooks/useSession.ts:
```typescript
export interface Session { id: string; title: string | null; status: string; projectPath: string | null; ... }
```

From Plan 03 (IdeationView):
```typescript
export function IdeationView({ onLaunchExecution }: { onLaunchExecution?: (sessionId: string) => void }): JSX.Element
```

From Plan 04 (AutonomousView):
```typescript
export function AutonomousView({ projectPath, ideationSessionId, onComplete }: {...}): JSX.Element
```

From Plan 04 (DecisionQueue):
```typescript
export function DecisionQueue({ gates, onDecide }: {...}): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionTabBar + useSessionTabs hook + RepoScaffoldForm + StackSelector</name>
  <files>packages/web/src/components/session/SessionTabBar.tsx, packages/web/src/components/session/SessionTab.tsx, packages/web/src/hooks/useSessionTabs.ts, packages/web/src/components/ideation/RepoScaffoldForm.tsx, packages/web/src/components/ideation/StackSelector.tsx</files>
  <read_first>
    - packages/web/src/hooks/useSession.ts (existing session types and hooks)
    - packages/web/src/components/session/SessionListItem.tsx (existing session display pattern)
    - packages/web/src/App.tsx (current session management to extend)
    - DESIGN.md (design tokens)
    - .planning/phases/15-ideation-funnel-autonomous-gsd/15-UI-SPEC.md (tab bar specs, scaffold form specs, copywriting)
  </read_first>
  <action>
CRITICAL: Use `git add <specific-files>` for commits. NEVER use `git add -A`, `git add .`, or `git commit -a`.

1. **Create packages/web/src/hooks/useSessionTabs.ts** (per D-15, D-16, D-17):

   - `interface SessionTab { id: string; title: string; projectPath?: string; status: 'thinking' | 'waiting' | 'idle'; type: 'ideation' | 'autonomous' | 'conversation' }`

   - State: `tabs` (SessionTab[]), `activeTabId` (string|null)

   - `addTab(tab: Omit<SessionTab, 'status'>): string` — Adds new tab with 'idle' status, sets as active. Returns tab id.
   - `removeTab(id: string)` — Removes tab. If active, switch to previous or next tab. Per D-17: switching preserves state (React state per SessionView, not stored here).
   - `setActiveTab(id: string)` — Per D-17: just sets activeTabId. Each tab's content is rendered by its own component tree.
   - `updateTabStatus(id: string, status: SessionTab['status'])` — Updates status indicator per D-16.
   - `updateTabTitle(id: string, title: string)` — Updates tab title (e.g., when ideation starts, tab title becomes the idea excerpt).

   - Return: `{ tabs, activeTabId, addTab, removeTab, setActiveTab, updateTabStatus, updateTabTitle }`

2. **Create packages/web/src/components/session/SessionTab.tsx** — Per UI spec:

   - Props: `{ name: string; status: 'thinking' | 'waiting' | 'idle'; active: boolean; onClick: () => void; onClose: () => void }`
   - Tab: min-width 120px, max-width 200px, height 40px
   - Active: 2px bottom border --color-accent, text --color-accent (per UI spec)
   - Inactive: text --color-text-muted, hover bg --color-surface-hover
   - Status dot: 8px circle, cyan pulse (#36C9FF) for thinking, amber (#FFB020) for waiting, muted (#6F7C90) for idle (per D-16)
   - Name: truncated at 20 chars with ellipsis (per UI spec)
   - Close button: 16x16 X, visible on hover only, --color-text-muted (per UI spec)
   - Tab motion: slide-in 200ms ease-out on create, slide-out 150ms ease-in on close (per motion contract)

3. **Create packages/web/src/components/session/SessionTabBar.tsx** (per D-15):

   - Props: `{ tabs: SessionTab[]; activeTabId: string | null; onSelect: (id: string) => void; onClose: (id: string) => void; onNew: () => void }`
   - Height: 40px, background --color-surface, bottom border 1px --color-border
   - Tab spacing: flush (0px gap, 1px border between)
   - Horizontal scroll with fade gradient at right edge when tabs overflow (per interaction flow)
   - New tab button: "+" at right end, same height, --color-text-muted, hover --color-text-primary
   - Renders SessionTab for each tab entry

4. **Create packages/web/src/components/ideation/StackSelector.tsx** (per D-18):

   - Props: `{ value: string; onChange: (stack: string) => void }`
   - Radio-button group: 4 options: React, Python, Swift, Go
   - Each option: card-style radio with inline SVG icon + label
   - Selected: --color-accent border, --color-accent text
   - Unselected: --color-border border, --color-text-muted text
   - Layout: 2x2 grid on small, 4-column on wider

5. **Create packages/web/src/components/ideation/RepoScaffoldForm.tsx** (per D-18, D-19, D-20):

   - Props: `{ ideationContext?: { name?: string; description?: string; stack?: string; excerpt?: string }; onScaffold: (result: { path: string; filesCreated: string[] }) => void; onClose: () => void }`

   - Modal overlay per UI spec: centered, max-width 560px, --color-surface bg, 1px --color-border, border-radius 12px
   - Backdrop: opacity 0 -> 0.5 on open, modal scale 0.95 -> 1, 200ms ease-out (per motion contract)

   - Form fields (per D-19: pre-populated from ideation context):
     - Name: text input, placeholder "my-project", regex validated /^[a-z0-9-]+$/
     - Description: textarea, max 2000 chars
     - Stack: StackSelector component
     - All pre-populated from ideationContext prop if available

   - "Scaffold Repository" accent CTA button (per copywriting contract)
   - Loading state: button spinner, disabled
   - Success: calls onScaffold callback, onClose
   - Error: inline error message below form in error semantic color (per interaction flow)
   - Per copywriting: "Repository creation failed -- {error}. Check the name and try again."

   - Submits POST to `/api/scaffold/scaffold` with { name, stack, description, ideationSessionId }
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useSessionTabs" packages/web/src/hooks/useSessionTabs.ts
    - grep -q "SessionTab" packages/web/src/components/session/SessionTab.tsx
    - grep -q "SessionTabBar" packages/web/src/components/session/SessionTabBar.tsx
    - grep -q "thinking\|waiting\|idle" packages/web/src/components/session/SessionTab.tsx
    - grep -q "RepoScaffoldForm" packages/web/src/components/ideation/RepoScaffoldForm.tsx
    - grep -q "Scaffold Repository" packages/web/src/components/ideation/RepoScaffoldForm.tsx
    - grep -q "StackSelector" packages/web/src/components/ideation/StackSelector.tsx
  </acceptance_criteria>
  <done>SessionTabBar renders multi-tab strip with status indicators per D-15, D-16. RepoScaffoldForm modal handles template-based repo creation per D-18, D-19, D-20. StackSelector provides 4-option radio group.</done>
</task>

<task type="auto">
  <name>Task 2: Shell + Sidebar + App integration wiring</name>
  <files>packages/web/src/components/layout/Shell.tsx, packages/web/src/components/layout/Sidebar.tsx, packages/web/src/App.tsx</files>
  <read_first>
    - packages/web/src/components/layout/Shell.tsx (current shell to extend)
    - packages/web/src/components/layout/Sidebar.tsx (current sidebar to extend -- read FULL file)
    - packages/web/src/App.tsx (current app routing to extend)
    - packages/web/src/hooks/useSessionTabs.ts (from Task 1)
    - packages/web/src/components/session/SessionTabBar.tsx (from Task 1)
    - packages/web/src/components/ideation/IdeationView.tsx (from Plan 03)
    - packages/web/src/components/autonomous/AutonomousView.tsx (from Plan 04)
    - packages/web/src/components/decision/DecisionQueue.tsx (from Plan 04)
    - .planning/phases/15-ideation-funnel-autonomous-gsd/15-UI-SPEC.md (Shell extension spec)
  </read_first>
  <action>
CRITICAL: Use `git add <specific-files>` for commits. NEVER use `git add -A`, `git add .`, or `git commit -a`.

1. **Update packages/web/src/components/layout/Sidebar.tsx**:

   - Extend `AppView` type: add `'ideation' | 'autonomous'` to the union. Final: `'projects' | 'pr-reviews' | 'trends' | 'repos' | 'design-docs' | 'session' | 'ideation' | 'autonomous'`

   - Add "Ideation" NavButton in the primary navigation section (after "Sessions" or "Projects" -- place it prominently as it's the main new feature)

   - Add DecisionQueue section at bottom of sidebar (above version number if present). Import DecisionQueue from '../decision/DecisionQueue'. Only render when gates are present (passed via new props).

   - New props: `decisionGates?: Array<DecisionGate>; onDecideGate?: (gateId: string, optionId: string) => void`

2. **Update packages/web/src/components/layout/Shell.tsx**:

   - Add SessionTabBar between sidebar and main content. Per UI spec: grid changes to `grid-rows-[40px_1fr_40px]` when tabs are present, otherwise keep `grid-rows-[1fr_40px]`.

   - New props: `tabs?: SessionTab[]; activeTabId?: string | null; onSelectTab?: (id: string) => void; onCloseTab?: (id: string) => void; onNewTab?: () => void; decisionGates?: Array<any>; onDecideGate?: (gateId: string, optionId: string) => void`

   - Pass decisionGates and onDecideGate through to Sidebar.

   - SessionTabBar renders only when tabs array has items.

3. **Update packages/web/src/App.tsx** -- Major integration wiring:

   - Import new components: IdeationView, AutonomousView, RepoScaffoldForm, useSessionTabs, useDecisionGates
   - Import updated AppView type

   - Add `useSessionTabs()` hook for multi-tab management
   - Add `useDecisionGates()` hook for global decision gate state (shared across all autonomous sessions)
   - Add state: `autonomousProjectPath` (string|null), `autonomousIdeationId` (string|null), `showScaffoldForm` (boolean), `scaffoldContext` (object|null)

   - **Ideation flow wiring**:
     a. "Ideation" nav -> setView('ideation')
     b. IdeationView's `onLaunchExecution` callback:
        - Sets autonomousProjectPath and autonomousIdeationId
        - Opens scaffold form if no project path yet (idea-first needs a repo)
        - OR navigates to autonomous view if project path is known
     c. After scaffold: Creates new tab of type 'autonomous', navigates to autonomous view

   - **Multi-tab wiring** (per D-15, D-17):
     a. New tab button creates new ideation tab (idea-first default)
     b. Tab selection sets activeTabId, which determines which content renders
     c. Each tab type ('ideation' | 'autonomous' | 'conversation') renders its respective view
     d. Tab status updates come from the hooks (useIdeation -> 'thinking' while running, useAutonomous -> 'thinking' while executing, etc.)

   - **renderContent switch** -- Add cases:
     ```
     case 'ideation': return <IdeationView onLaunchExecution={handleLaunchExecution} />
     case 'autonomous': return <AutonomousView projectPath={autonomousProjectPath!} ideationSessionId={autonomousIdeationId} onComplete={handleExecutionComplete} />
     ```

   - Pass tabs, tab handlers, decision gates to Shell

   - **RepoScaffoldForm** -- Render as modal overlay when showScaffoldForm is true. On scaffold success, create new autonomous tab and navigate.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ideation\|autonomous" packages/web/src/components/layout/Sidebar.tsx
    - grep -q "SessionTabBar" packages/web/src/components/layout/Shell.tsx
    - grep -q "DecisionQueue" packages/web/src/components/layout/Sidebar.tsx
    - grep -q "IdeationView" packages/web/src/App.tsx
    - grep -q "AutonomousView" packages/web/src/App.tsx
    - grep -q "useSessionTabs" packages/web/src/App.tsx
    - grep -q "useDecisionGates" packages/web/src/App.tsx
    - grep -q "RepoScaffoldForm" packages/web/src/App.tsx
  </acceptance_criteria>
  <done>Shell includes SessionTabBar for multi-tab sessions. Sidebar has Ideation nav + DecisionQueue. App routes to IdeationView and AutonomousView. Full flow wired: idea -> ideation -> scaffold -> autonomous execution. Multi-tab state managed via useSessionTabs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of full ideation + autonomous flow</name>
  <files>packages/web/src/App.tsx</files>
  <action>Human visual verification of the complete Phase 15 UI integration. No code changes -- this task validates that all prior work renders correctly in the browser.</action>
  <what-built>Complete ideation funnel and autonomous GSD pipeline -- from typing an idea to watching autonomous execution with real-time pipeline visualization, decision gates, and multi-tab sessions.</what-built>
  <how-to-verify>
    1. Start dev server: `cd /Users/ryanstern/gstackapp && npm run dev` (or equivalent)
    2. Open browser to localhost

    **Multi-tab sessions (SESS-02):**
    3. Verify SessionTabBar appears at top of main content
    4. Click "+" to create a new tab -- verify it appears with status indicator

    **Ideation flow (IDEA-01, IDEA-02, IDEA-03):**
    5. Click "Ideation" in sidebar to navigate to ideation view
    6. Verify empty state: "Start with an idea" heading with description
    7. Type an idea in the textarea -- verify "Start Ideation" button activates
    8. (If backend running with API key) Click "Start Ideation" -- verify pipeline nodes animate through stages
    9. Verify artifact cards appear in right panel as stages complete
    10. Click a completed stage node -- verify artifact detail shows

    **Repo scaffolding (IDEA-04):**
    11. After ideation completes, click "Scaffold Repository" -- verify modal opens
    12. Verify stack selector shows React/Python/Swift/Go options
    13. Verify form is pre-populated from ideation context

    **Autonomous execution (AUTO-01, AUTO-02, AUTO-03):**
    14. Navigate to autonomous view (via "Launch Execution" or directly)
    15. Verify vertical phase pipeline renders with phase nodes
    16. Verify commit stream panel on right side
    17. If decision gates exist, verify they appear in sidebar with amber badge

    **Design compliance:**
    18. Verify dark mode throughout (no light backgrounds)
    19. Verify accent color (#C6FF3B) on primary CTAs only
    20. Verify stage spectral colors match spec (orange, cyan, purple)
  </how-to-verify>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>All Phase 15 UI components render correctly, design system compliance confirmed, ideation and autonomous flows work end-to-end in the browser.</done>
  <resume-signal>Type "approved" or describe issues with specific component/view</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Scaffold form submits project name and config |
| Client state | Multi-tab session state managed in React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-17 | Tampering | RepoScaffoldForm | mitigate | Client validates name with /^[a-z0-9-]+$/ before submit. Server re-validates with Zod (defense in depth). |
| T-15-18 | Denial of Service | SessionTabBar | mitigate | Limit max open tabs to 10 (practical limit for single user). Beyond 10, show "Close a tab first" hint. |
| T-15-19 | Information Disclosure | useSessionTabs | accept | Tab state is ephemeral React state. No sensitive data persisted client-side beyond what's already in the session API. |
</threat_model>

<verification>
- App compiles: `npx tsc --noEmit --project packages/web/tsconfig.json`
- Sidebar shows "Ideation" nav item
- Shell renders SessionTabBar when tabs exist
- App renders IdeationView for 'ideation' view
- App renders AutonomousView for 'autonomous' view
- RepoScaffoldForm modal opens and closes
- Full existing test suite passes: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- Multiple concurrent sessions as tabs per SESS-02, D-15, D-16, D-17
- Tabs show project name and status (thinking/waiting/idle) per D-16
- Switching tabs preserves conversation state per D-17
- Repo scaffolding from ideation output per IDEA-04, D-18, D-19, D-20
- Full app integration: sidebar nav, shell tab bar, view routing all wired
- Visual checkpoint confirms design compliance
</success_criteria>

<output>
After completion, create `.planning/phases/15-ideation-funnel-autonomous-gsd/15-05-SUMMARY.md`
</output>
