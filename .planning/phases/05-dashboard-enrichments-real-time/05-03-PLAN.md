---
phase: 05-dashboard-enrichments-real-time
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/web/src/hooks/use-health.ts
  - packages/web/src/components/health/health-panel.tsx
  - packages/web/src/components/departure-board/project-row.tsx
  - packages/web/src/components/departure-board/previously-on.tsx
  - packages/web/src/components/layout/dashboard-layout.tsx
  - packages/web/src/App.tsx
  - packages/web/src/lib/stale-nudge.ts
autonomous: false
requirements: [DASH-06, DASH-07, DASH-08]

must_haves:
  truths:
    - "Projects idle 2+ weeks with uncommitted work receive a subtle amber/gold visual nudge on their row"
    - "'Previously on...' breadcrumbs expand inline on each project row showing last 3-5 commits and GSD state"
    - "Mac Mini health dot in header is clickable, expanding a panel with CPU, memory, disk, uptime, and service status"
    - "Health panel shows green/amber/red overall status matching the header dot"
    - "Stale nudge tooltip explains why the project is highlighted"
  artifacts:
    - path: "packages/web/src/hooks/use-health.ts"
      provides: "Hook to poll /api/health/system every 30 seconds"
      exports: ["useHealth"]
    - path: "packages/web/src/components/health/health-panel.tsx"
      provides: "Expandable health details panel anchored to header dot"
      exports: ["HealthPanel"]
    - path: "packages/web/src/components/departure-board/previously-on.tsx"
      provides: "Inline expandable commit breadcrumbs for project rows"
      exports: ["PreviouslyOn"]
    - path: "packages/web/src/lib/stale-nudge.ts"
      provides: "Pure function to detect stale projects needing nudge"
      exports: ["isStaleWithDirty", "getStaleNudgeMessage"]
  key_links:
    - from: "packages/web/src/hooks/use-health.ts"
      to: "/api/health/system"
      via: "30-second polling interval"
      pattern: "fetch.*api/health/system"
    - from: "packages/web/src/components/departure-board/project-row.tsx"
      to: "packages/web/src/lib/stale-nudge.ts"
      via: "isStaleWithDirty check for conditional styling"
      pattern: "isStaleWithDirty"
    - from: "packages/web/src/components/layout/dashboard-layout.tsx"
      to: "packages/web/src/components/health/health-panel.tsx"
      via: "Click handler on health dot toggles panel"
      pattern: "HealthPanel"
---

<objective>
Add dashboard row enrichments and health panel: subtle amber stale nudges on project rows with dirty files idle 2+ weeks, inline "Previously on..." expandable breadcrumbs showing recent commits and GSD state per project row, and a clickable health dot that expands into a full system metrics panel. Finish with a visual verification checkpoint.

Purpose: Stale nudges give gentle "hey" reminders about forgotten work. "Previously on..." provides instant project context restoration by showing what you were doing without opening a terminal. Health panel gives ambient awareness of Mac Mini system state and service health on demand.

Output: Extended project rows with expand chevron and stale styling, health panel component, useHealth polling hook, stale nudge utility, visual checkpoint verification.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-enrichments-real-time/05-CONTEXT.md
@.planning/phases/05-dashboard-enrichments-real-time/05-RESEARCH.md
@.planning/phases/05-dashboard-enrichments-real-time/05-01-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 05-01 that this plan consumes -->

From packages/api/src/routes/health.ts (extended in 05-01):
```typescript
// GET /api/health/system
// Response: {
//   overallStatus: "healthy" | "degraded" | "unhealthy",
//   cpu: { loadAvg1m: number, loadAvg5m: number, cores: number },
//   memory: { totalMB: number, freeMB: number, usedPercent: number },
//   disk: { totalGB: number, usedGB: number, usedPercent: number },
//   uptime: number,  // seconds
//   services: { name: string, status: "up" | "down" | "unknown" }[]
// }
```

<!-- Existing interfaces this plan extends -->

From packages/web/src/components/departure-board/project-row.tsx:
```typescript
interface ProjectRowProps {
  project: ProjectItem;
  isSelected: boolean;
  onSelect: (slug: string) => void;
  captureCount?: number;
}
```

From packages/web/src/components/layout/dashboard-layout.tsx:
```typescript
interface DashboardLayoutProps {
  children: ReactNode;
  healthOk: boolean;
  theme: "light" | "dark";
  onThemeToggle: () => void;
  staleCount?: number;
  onTriageClick?: () => void;
}
// Health dot is currently a static colored span
```

From packages/web/src/lib/grouping.ts:
```typescript
export interface ProjectItem {
  slug: string; name: string; tagline: string | null;
  dirty: boolean | null; dirtyFiles: string[];
  lastCommitDate: string | null;
  // Also includes: commits from project detail (not on list item)
}
```

From packages/web/src/hooks/use-project-detail.ts:
```typescript
// Project detail includes commits array:
// detail.commits: { hash: string, message: string, relativeTime: string, date: string }[]
// detail.gsdState: { status: string, stoppedAt: string | null, percent: number | null } | null
```

From packages/web/src/App.tsx:
```typescript
// Projects are available via: allProjects (flat array of all ProjectItem)
// Detail is available via: detail (from useProjectDetail for selected slug)
// Health is currently: healthOk (boolean from /api/health fetch)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stale nudge utility, "Previously on..." breadcrumbs, and project row extensions</name>
  <files>
    packages/web/src/lib/stale-nudge.ts,
    packages/web/src/components/departure-board/previously-on.tsx,
    packages/web/src/components/departure-board/project-row.tsx
  </files>
  <behavior>
    - Test 1: isStaleWithDirty returns true when lastCommitDate > 14 days ago AND dirtyFiles.length > 0
    - Test 2: isStaleWithDirty returns false when lastCommitDate < 14 days ago
    - Test 3: isStaleWithDirty returns false when dirtyFiles is empty
    - Test 4: isStaleWithDirty returns false when lastCommitDate is null
    - Test 5: getStaleNudgeMessage returns descriptive message with days idle
  </behavior>
  <action>
    1. Create `packages/web/src/lib/stale-nudge.ts`:
       - Export `isStaleWithDirty(project: { lastCommitDate: string | null, dirty: boolean | null, dirtyFiles: string[] }): boolean`
         - Return false if lastCommitDate is null or dirty is not true
         - Calculate days idle: `(Date.now() - new Date(lastCommitDate).getTime()) / (24 * 60 * 60 * 1000)`
         - Return `daysIdle > 14 && dirtyFiles.length > 0`
       - Export `getStaleNudgeMessage(project: { lastCommitDate: string | null, dirtyFiles: string[] }): string`
         - Calculate days idle, return: `"uncommitted changes -- ${Math.floor(daysIdle)} days idle"`

    2. Create `packages/web/src/components/departure-board/previously-on.tsx`:
       - Props: `{ commits: { hash: string, message: string, relativeTime: string }[], gsdState: { status: string, stoppedAt: string | null, percent: number | null } | null }`
       - Render last 3-5 commits as a compact list:
         - Each commit: short hash (mono font, muted), message (truncated), relative time (muted, right-aligned)
         - Subtle left border accent (terracotta/30 or similar, lighter than hero)
       - If gsdState exists, render a small GSD status line at the top (e.g., "GSD: Phase 3 -- 60% complete")
       - Compact spacing -- this is inline within the departure board, not a large section
       - Smooth expand/collapse transition (Claude's discretion): use CSS transition on max-height or grid-template-rows for animation

    3. Extend `packages/web/src/components/departure-board/project-row.tsx`:
       - Import `isStaleWithDirty` and `getStaleNudgeMessage` from lib/stale-nudge.js
       - Import `PreviouslyOn` component
       - Add new props to ProjectRowProps:
         - `commits?: { hash: string, message: string, relativeTime: string }[]` (from project detail)
         - `gsdState?: { status: string, stoppedAt: string | null, percent: number | null } | null`
       - Add local state: `const [expanded, setExpanded] = useState(false)`
       - Stale nudge: if `isStaleWithDirty(project)` is true:
         - Replace `border-transparent` with `border-amber-500/60` (or warm amber from theme tokens)
         - Add subtle background tint: `bg-amber-500/5`
         - Add `title` attribute on the row div with `getStaleNudgeMessage(project)`
       - Expand chevron: add a small chevron icon (inline SVG, rotate on expand) to the right of the row content, only shown if commits exist
         - Click handler: `setExpanded(prev => !prev)` -- must stop propagation to not trigger row selection
       - Below the row content, conditionally render `<PreviouslyOn>` when expanded
       - Collapsed by default per locked decision
  </action>
  <verify>
    <automated>pnpm --filter @mission-control/web test -- --grep "stale" && pnpm typecheck</automated>
  </verify>
  <done>Stale projects with idle 2+ weeks AND dirty files show amber/gold left border and subtle background tint with tooltip explaining why. Each project row has an expand chevron that reveals "Previously on..." breadcrumbs showing last 3-5 commits and GSD state inline. All collapsed by default.</done>
</task>

<task type="auto">
  <name>Task 2: Health polling hook, health panel, dashboard layout extension, and App.tsx wiring</name>
  <files>
    packages/web/src/hooks/use-health.ts,
    packages/web/src/components/health/health-panel.tsx,
    packages/web/src/components/layout/dashboard-layout.tsx,
    packages/web/src/App.tsx
  </files>
  <action>
    1. Create `packages/web/src/hooks/use-health.ts`:
       - Define `SystemHealth` interface matching API response shape (cpu, memory, disk, uptime, services, overallStatus)
       - Poll `/api/health/system` every 30 seconds (per locked decision)
       - Use `setInterval` in useEffect with cleanup
       - Also fetch on mount (immediate first poll)
       - Return `{ health: SystemHealth | null, loading: boolean, overallStatus: "healthy" | "degraded" | "unhealthy" | "unreachable" }`
       - If fetch fails, set overallStatus to "unreachable"
       - Map overallStatus to health dot color: healthy=green (bg-sage), degraded=amber (bg-amber-500), unhealthy/unreachable=red (bg-rust)

    2. Create `packages/web/src/components/health/health-panel.tsx`:
       - Props: `{ health: SystemHealth, onClose: () => void }`
       - Absolute-positioned dropdown panel anchored below the health dot (top-right area)
       - Dismiss on: click outside (useEffect + document click listener), Escape key, or clicking the dot again
       - Content sections:
         - **System**: CPU load (1m/5m avg), cores count
         - **Memory**: used/total MB, progress bar showing usedPercent
         - **Disk**: used/total GB, progress bar showing usedPercent
         - **Uptime**: formatted as days/hours (e.g., "3d 14h")
         - **Services**: list each service with green/red dot indicator (up/down/unknown)
       - Use warm theme colors, compact layout
       - If services array is empty, show "No services configured" message
       - Small close button or just rely on click-outside

    3. Extend `packages/web/src/components/layout/dashboard-layout.tsx`:
       - Add new props to DashboardLayoutProps:
         - `healthStatus?: "healthy" | "degraded" | "unhealthy" | "unreachable"` (replaces `healthOk: boolean`)
         - `onHealthClick?: () => void`
         - `healthPanelOpen?: boolean`
         - `healthData?: SystemHealth | null`
         - `onHealthPanelClose?: () => void`
       - Keep backward compat: if `healthStatus` not provided, fall back to `healthOk` for the dot color
       - Make health dot clickable: wrap in button, call `onHealthClick` on click
       - Health dot color mapping: healthy -> bg-sage, degraded -> bg-amber-500, unhealthy/unreachable -> bg-rust
       - Render `<HealthPanel>` when `healthPanelOpen` is true, positioned relative to the header

    4. Wire into `packages/web/src/App.tsx`:
       - Import `useHealth` hook
       - Call `useHealth()` to get health data and overallStatus
       - Add `healthPanelOpen` state (boolean)
       - Replace the existing `healthOk` fetch with the `useHealth` hook's data:
         - Remove the manual `useEffect(() => { fetch("/api/health")... })` block
         - Pass `healthStatus={overallStatus}` to DashboardLayout
       - Pass health panel props to DashboardLayout:
         - `onHealthClick={() => setHealthPanelOpen(prev => !prev)}`
         - `healthPanelOpen`
         - `healthData={health}`
         - `onHealthPanelClose={() => setHealthPanelOpen(false)}`
       - Pass `commits` and `gsdState` to project rows:
         - The departure board needs to pass commit/gsd data through to project rows
         - For the selected project (hero detail), `detail.commits` and `detail.gsdState` are available
         - For non-selected projects, the list endpoint doesn't include full commits -- so "Previously on..." only shows when the project has been selected/viewed (data is cached in useProjectDetail)
         - Alternative: pass project detail data down through DepartureBoard -> ProjectGroup -> ProjectRow. The DepartureBoard component needs a new prop for per-project detail data.
         - Simplest approach: pass a `projectDetails` map (slug -> {commits, gsdState}) built from the useProjectDetail cache, or just pass the current selectedSlug's detail commits to the matching row.
         - PRAGMATIC CHOICE: Only the selected project row shows the expand chevron and "Previously on..." since detail data is already loaded for it. Other rows get the chevron but show "Select to load history" or simply don't show the chevron until they've been selected. This avoids N+1 API calls.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build</automated>
  </verify>
  <done>Health panel shows CPU, memory, disk, uptime, and per-service status on click. Header health dot color reflects overall system status (green/amber/red). 30-second polling. "Previously on..." breadcrumbs show on selected project row. Stale nudge styling active on qualifying project rows. App.tsx wired with all new hooks and components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all Phase 5 dashboard enrichments</name>
  <files></files>
  <action>
    Verify all Phase 5 features visually in the browser. This checkpoint covers the combined output of plans 05-01, 05-02, and 05-03.

    What was built:
    - Sprint heatmap (DASH-05): GitHub-style contribution grid above departure board
    - "Previously on..." breadcrumbs (DASH-06): expandable commit history per project row
    - Stale project nudges (DASH-07): amber/gold visual treatment on idle projects with dirty files
    - Mac Mini health panel (DASH-08): clickable health dot expanding system metrics
    - SSE real-time updates (DASH-09): live dashboard updates without page refresh

    Verification steps:
    1. Open dashboard at http://localhost:5173 (run `pnpm dev` if not running)
    2. HEATMAP: Verify sprint heatmap is visible above the departure board showing commit intensity per project. Hover cells to see date + commit count tooltip. Check that only projects with recent commits appear.
    3. STALE NUDGE: If any project has been idle 2+ weeks with dirty files, verify it shows amber/gold left border and background tint. Hover to see tooltip message.
    4. PREVIOUSLY ON: Click the expand chevron on the selected project row. Verify it shows last 3-5 commits (hash + message + time). If the project has GSD state, verify it shows.
    5. HEALTH PANEL: Click the health dot in the header. Verify panel shows CPU, memory, disk, uptime, and any configured services. Click outside or press Escape to dismiss.
    6. SSE REAL-TIME: Open browser DevTools Network tab, verify EventSource connection to /api/events is established. Type a capture in the capture field and submit. Verify the capture appears without manual refresh (SSE triggers refetch).
    7. MOBILE: Resize browser to mobile width. Verify heatmap scrolls horizontally and dashboard remains usable.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>All 5 Phase 5 features verified visually: heatmap shows commit patterns, stale nudges are subtle but visible, "Previously on..." breadcrumbs expand correctly, health panel shows metrics, SSE pushes updates in real-time. User approved.</done>
</task>

</tasks>

<verification>
- `pnpm test` -- all tests pass
- `pnpm typecheck` -- no type errors
- `pnpm build` -- production build succeeds
- All 5 dashboard enrichments visible and functional
- SSE connection established and triggers real-time updates
</verification>

<success_criteria>
- Projects idle 2+ weeks with uncommitted work show subtle amber/gold visual nudge (not alarming, just "hey")
- Tooltip on hover explains why project is highlighted
- "Previously on..." breadcrumbs expand inline per project row showing last 3-5 commits and GSD state
- Collapsed by default, click chevron to expand
- Health dot in header clickable, expands panel with full system metrics
- Health dot color: green (all up), amber (degraded), red (unreachable)
- 30-second health poll interval
- All enrichments respect warm color system and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-enrichments-real-time/05-03-SUMMARY.md`
</output>
