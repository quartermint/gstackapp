---
phase: 06-onboarding-quality-trends
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/web/src/components/trends/QualityScoreChart.tsx
  - packages/web/src/components/trends/VerdictRateChart.tsx
  - packages/web/src/components/trends/FindingTrendChart.tsx
  - packages/web/src/components/trends/TrendsView.tsx
  - packages/web/src/components/trends/RepoSelector.tsx
  - packages/web/src/hooks/useTrends.ts
  - packages/web/src/hooks/useRepos.ts
  - packages/web/src/api/client.ts
  - packages/web/src/components/layout/Sidebar.tsx
  - packages/web/src/App.tsx
autonomous: false

requirements: [TRND-01, TRND-02, TRND-03]

must_haves:
  truths:
    - "User can navigate to a Trends view from the sidebar"
    - "Quality score line chart shows per-repo scores over time with electric lime accent line"
    - "Per-stage verdict rates render as stacked area charts with stage spectral colors"
    - "Finding frequency trend chart shows critical/notable/minor counts over time"
    - "Charts follow DESIGN.md dark theme — transparent background, #2A2F3A grid, #8B95A7 axis text"
    - "Repos with fewer than 2 completed pipeline runs show a contextual empty state"
  artifacts:
    - path: "packages/web/src/components/trends/QualityScoreChart.tsx"
      provides: "Recharts LineChart for quality score over time per D-08"
      min_lines: 30
    - path: "packages/web/src/components/trends/VerdictRateChart.tsx"
      provides: "Recharts stacked AreaChart for pass/flag/block per stage per D-09"
      min_lines: 30
    - path: "packages/web/src/components/trends/FindingTrendChart.tsx"
      provides: "Recharts AreaChart for finding frequency per D-10"
      min_lines: 30
    - path: "packages/web/src/components/trends/TrendsView.tsx"
      provides: "Container view with repo selector and all three charts"
      min_lines: 50
    - path: "packages/web/src/hooks/useTrends.ts"
      provides: "TanStack Query hooks for trend endpoints"
      exports: ["useQualityScores", "useVerdictRates", "useFindingTrends"]
  key_links:
    - from: "packages/web/src/hooks/useTrends.ts"
      to: "/api/trends/scores"
      via: "fetch in TanStack Query"
      pattern: "fetch.*api/trends/scores"
    - from: "packages/web/src/hooks/useTrends.ts"
      to: "/api/trends/verdicts"
      via: "fetch in TanStack Query"
      pattern: "fetch.*api/trends/verdicts"
    - from: "packages/web/src/hooks/useTrends.ts"
      to: "/api/trends/findings"
      via: "fetch in TanStack Query"
      pattern: "fetch.*api/trends/findings"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/trends/TrendsView.tsx"
      via: "view routing state"
      pattern: "TrendsView"
    - from: "packages/web/src/components/layout/Sidebar.tsx"
      to: "App.tsx view state"
      via: "onClick handler passed as prop or callback"
      pattern: "Trends"
---

<objective>
Quality trend chart components for the dashboard, showing per-repo quality scores, per-stage verdict rates, and finding frequency over time.

Purpose: Returning users see how their repos' code quality is changing over time. Charts use Recharts with DESIGN.md styling (stage spectral colors, dark theme, electric lime accent). Trend charts are the primary retention surface — positive reinforcement for code quality habits.

Output: Three Recharts chart components, a TrendsView container, trend data hooks, repo selector, and dashboard routing integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-onboarding-quality-trends/06-CONTEXT.md
@.planning/phases/06-onboarding-quality-trends/06-RESEARCH.md
@.planning/phases/06-onboarding-quality-trends/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 06-01 (dependency): Trend API endpoints -->
GET /api/trends/scores?repoId=N response:
```typescript
Array<{ date: string; score: number }>
// date is 'YYYY-MM-DD', score is 0-100
```

GET /api/trends/verdicts?repoId=N&stage=S response:
```typescript
Array<{ date: string; pass: number; flag: number; block: number; skip: number }>
```

GET /api/trends/findings?repoId=N response:
```typescript
Array<{ date: string; critical: number; notable: number; minor: number }>
```

GET /api/repos response:
```typescript
Array<{ id: number; fullName: string; defaultBranch: string; installationId: number; createdAt: string }>
```

From packages/web/src/lib/constants.ts:
```typescript
export const STAGE_COLORS: Record<Stage, string> = {
  ceo: '#FF8B3E', eng: '#36C9FF', design: '#B084FF', qa: '#2EDB87', security: '#FF5A67'
}
export const STAGE_LABELS: Record<Stage, string> = {
  ceo: 'CEO Review', eng: 'Eng Review', design: 'Design Review', qa: 'QA', security: 'Security'
}
export const VERDICT_COLORS: Record<Verdict | 'RUNNING', string> = {
  PASS: '#2EDB87', FLAG: '#FFB020', BLOCK: '#FF5A67', SKIP: '#6F7C90', RUNNING: '#36C9FF'
}
```

From packages/web/src/api/client.ts:
```typescript
export const queryKeys = {
  pipelines: { all, list, detail },
  repos: { all, list },
  onboarding: { status }, // added by Plan 02
} as const
```

DESIGN.md chart styling tokens:
- Grid: #2A2F3A (--color-border)
- Axis text: #8B95A7 (--color-text-muted)
- Tooltip bg: #13161C (--color-surface)
- Tooltip border: #2A2F3A (--color-border)
- Tooltip text: #EDEDED (--color-text-primary)
- Quality score line: #C6FF3B (--color-accent, electric lime)
- Chart background: transparent (sits on surface)

From packages/web/src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trend data hooks, repo selector, and three chart components</name>
  <files>
    packages/web/src/hooks/useTrends.ts
    packages/web/src/hooks/useRepos.ts
    packages/web/src/api/client.ts
    packages/web/src/components/trends/QualityScoreChart.tsx
    packages/web/src/components/trends/VerdictRateChart.tsx
    packages/web/src/components/trends/FindingTrendChart.tsx
    packages/web/src/components/trends/RepoSelector.tsx
  </files>
  <read_first>
    packages/web/src/hooks/usePipelineFeed.ts
    packages/web/src/api/client.ts
    packages/web/src/lib/constants.ts
    packages/web/src/lib/cn.ts
    packages/web/src/app.css
    packages/web/src/components/shared/EmptyState.tsx
    DESIGN.md
    packages/web/package.json
  </read_first>
  <action>
    **1. Extend query keys** (`packages/web/src/api/client.ts`):
    Add trends keys to the queryKeys object:
    ```typescript
    trends: {
      all: ['trends'] as const,
      scores: (repoId: number) => [...queryKeys.trends.all, 'scores', repoId] as const,
      verdicts: (repoId: number, stage: string) => [...queryKeys.trends.all, 'verdicts', repoId, stage] as const,
      findings: (repoId: number) => [...queryKeys.trends.all, 'findings', repoId] as const,
    },
    ```

    **2. Create useRepos hook** (`packages/web/src/hooks/useRepos.ts`):
    Simple TanStack Query hook fetching GET /api/repos. Returns `{ data: Repo[], isLoading, error }`. Interface:
    ```typescript
    interface Repo { id: number; fullName: string; defaultBranch: string; installationId: number; createdAt: string }
    ```

    **3. Create useTrends hooks** (`packages/web/src/hooks/useTrends.ts`):
    Three hooks following the established usePipelineFeed pattern (direct fetch, typed interfaces):
    - `useQualityScores(repoId: number | null)` — fetches /api/trends/scores?repoId=N, enabled when repoId is truthy
    - `useVerdictRates(repoId: number | null, stage: string)` — fetches /api/trends/verdicts?repoId=N&stage=S
    - `useFindingTrends(repoId: number | null)` — fetches /api/trends/findings?repoId=N

    All hooks use `staleTime: 60_000` (trend data doesn't change rapidly).

    **4. Create QualityScoreChart** (`packages/web/src/components/trends/QualityScoreChart.tsx`):
    Per D-08: Recharts LineChart showing quality score over time.
    - ResponsiveContainer wrapping a LineChart
    - Parent div MUST have explicit height: `h-[240px]` (per Pitfall 1: ResponsiveContainer height collapse)
    - CartesianGrid: stroke="#2A2F3A", strokeDasharray="3 3", vertical={false}
    - XAxis: dataKey="date", stroke="#8B95A7", fontSize 11, fontFamily "Geist", tickLine={false}
    - YAxis: domain [0, 100], stroke="#8B95A7", fontSize 11, axisLine={false}, width 32
    - Tooltip: bg=#13161C, border=#2A2F3A, borderRadius 8, color=#EDEDED, fontSize 13
    - Line: type="monotone", dataKey="score", stroke="#C6FF3B" (electric lime per D-11), strokeWidth 2, dot r=3, activeDot r=5
    - Props: `data: Array<{date: string; score: number}>`, `isEmpty: boolean` (shows EmptyState when true)
    - When data has < 2 points: show empty state "Need at least 2 reviews to show trends" (per Pitfall 6)

    **5. Create VerdictRateChart** (`packages/web/src/components/trends/VerdictRateChart.tsx`):
    Per D-09: Recharts stacked AreaChart showing pass/flag/block rates per stage.
    - AreaChart with stackOffset="expand" (normalize to 100%)
    - Three Area components with stackId="1":
      - pass: stroke="#2EDB87", fill="rgba(46, 219, 135, 0.3)"
      - flag: stroke="#FFB020", fill="rgba(255, 176, 32, 0.3)"
      - block: stroke="#FF5A67", fill="rgba(255, 90, 103, 0.3)"
    - Same axis/grid/tooltip styling as QualityScoreChart
    - h-[200px] parent container
    - Props: `data: Array<{date, pass, flag, block, skip}>`, `stage: Stage`, `isEmpty: boolean`
    - Per D-11: Use stage spectral color from STAGE_COLORS for the chart heading/border accent

    **6. Create FindingTrendChart** (`packages/web/src/components/trends/FindingTrendChart.tsx`):
    Per D-10: Recharts AreaChart showing finding frequency over time.
    - Three stacked Area components:
      - critical: stroke="#FF5A67", fill="rgba(255, 90, 103, 0.2)" (uses verdict-block color for consistency)
      - notable: stroke="#FFB020", fill="rgba(255, 176, 32, 0.2)"
      - minor: stroke="#6F7C90", fill="rgba(111, 124, 144, 0.2)"
    - Same dark theme axis/grid/tooltip styling
    - h-[200px] parent container
    - Props: `data: Array<{date, critical, notable, minor}>`, `isEmpty: boolean`

    **7. Create RepoSelector** (`packages/web/src/components/trends/RepoSelector.tsx`):
    Dropdown for selecting which repo's trends to view. Styled per DESIGN.md:
    - bg-surface, border-border, text-text-primary, font-body
    - Shows repo fullName (owner/repo format)
    - Auto-selects first repo if only one connected
    - Props: `repos: Repo[]`, `selectedId: number | null`, `onSelect: (id: number) => void`

    All chart components should extract common Recharts theme config into a shared constant object at top of each file (or in a shared `chartTheme.ts` utility) to avoid repetition:
    ```typescript
    const CHART_THEME = {
      grid: '#2A2F3A',
      axis: '#8B95A7',
      tooltip: { bg: '#13161C', border: '#2A2F3A', text: '#EDEDED' },
    }
    ```
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "useQualityScores" packages/web/src/hooks/useTrends.ts returns match
    - grep -r "useVerdictRates" packages/web/src/hooks/useTrends.ts returns match
    - grep -r "useFindingTrends" packages/web/src/hooks/useTrends.ts returns match
    - grep -r "ResponsiveContainer" packages/web/src/components/trends/QualityScoreChart.tsx returns match
    - grep -r "LineChart" packages/web/src/components/trends/QualityScoreChart.tsx returns match
    - grep -r "#C6FF3B" packages/web/src/components/trends/QualityScoreChart.tsx returns match (electric lime)
    - grep -r "AreaChart" packages/web/src/components/trends/VerdictRateChart.tsx returns match
    - grep -r "stackOffset" packages/web/src/components/trends/VerdictRateChart.tsx returns match (stacked normalize)
    - grep -r "AreaChart" packages/web/src/components/trends/FindingTrendChart.tsx returns match
    - grep -r "#2A2F3A" packages/web/src/components/trends/QualityScoreChart.tsx returns match (grid color per DESIGN.md)
    - grep -r "h-\[240px\]" packages/web/src/components/trends/QualityScoreChart.tsx returns match (explicit height per Pitfall 1)
    - grep -r "useRepos" packages/web/src/hooks/useRepos.ts returns match
    - grep -r "trends" packages/web/src/api/client.ts returns match (query keys added)
  </acceptance_criteria>
  <done>
    Three Recharts chart components render with DESIGN.md dark theme styling. Trend data hooks fetch from Plan 01 API endpoints. Repo selector allows choosing which repo to view. Charts show empty state for insufficient data. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: TrendsView container, sidebar navigation, and App.tsx routing</name>
  <files>
    packages/web/src/components/trends/TrendsView.tsx
    packages/web/src/components/layout/Sidebar.tsx
    packages/web/src/App.tsx
  </files>
  <read_first>
    packages/web/src/App.tsx
    packages/web/src/components/layout/Sidebar.tsx
    packages/web/src/components/layout/Shell.tsx
    packages/web/src/hooks/useTrends.ts
    packages/web/src/hooks/useRepos.ts
    packages/web/src/components/trends/QualityScoreChart.tsx
    packages/web/src/components/trends/VerdictRateChart.tsx
    packages/web/src/components/trends/FindingTrendChart.tsx
    packages/web/src/components/trends/RepoSelector.tsx
    packages/web/src/lib/constants.ts
    packages/web/src/lib/cn.ts
    DESIGN.md
  </read_first>
  <action>
    **1. Create TrendsView container** (`packages/web/src/components/trends/TrendsView.tsx`):
    Full page view containing all trend charts for a selected repo. Layout per DESIGN.md (left-anchored, grid-disciplined, no centered content):

    - Top section: heading "Quality Trends" (font-display) + RepoSelector dropdown
    - Section 1: QualityScoreChart (quality score over time per D-08) — full width
      - Label: "Quality Score" — font-mono 11px uppercase tracking
    - Section 2: Per-stage verdict rates (D-09) — render one VerdictRateChart per stage (5 charts), in a 2-column grid (lg:grid-cols-2), each with the stage label and stage color accent on the heading
      - Labels: "CEO Review", "Eng Review", etc. from STAGE_LABELS
    - Section 3: FindingTrendChart (finding frequency per D-10) — full width
      - Label: "Finding Frequency"

    State management:
    - `const [selectedRepoId, setSelectedRepoId] = useState<number | null>(null)` — auto-select first repo
    - Use useRepos() to get repo list, useEffect to auto-select first when loaded
    - Use all three trend hooks with selectedRepoId
    - Handle loading states with Skeleton components
    - Handle empty state: if no repos, show EmptyState with "Connect a repository to see quality trends"
    - Handle insufficient data: pass `isEmpty={data.length < 2}` to chart components (per Pitfall 6)

    Dense, functional layout per CONTEXT.md specifics: "Charts should be dense and functional, not enterprise-bloated. Linear-style density." Use tight spacing: gap-4 between sections, gap-3 between chart cards.

    **2. Add Trends navigation to Sidebar** (`packages/web/src/components/layout/Sidebar.tsx`):
    Currently Sidebar has hardcoded "Dashboard" and "Repositories" links. Update:
    - Accept an `activeView` prop (`'dashboard' | 'trends'`) and an `onNavigate` callback
    - "Dashboard" link: active when activeView === 'dashboard', calls onNavigate('dashboard')
    - "Trends" link: new nav item, active when activeView === 'trends', calls onNavigate('trends')
    - Active state styling: text-accent bg-accent-muted (existing pattern)
    - Inactive state styling: text-text-muted hover:text-text-primary hover:bg-surface-hover (existing pattern)
    - Keep "Repositories" link for now (not wired, stays as-is)

    **3. Add view routing to App.tsx** (`packages/web/src/App.tsx`):
    Simple state-based view routing (no react-router needed for 2 views):
    - Add `const [view, setView] = useState<'dashboard' | 'trends'>('dashboard')`
    - Pass `activeView={view}` and `onNavigate={setView}` to Sidebar (via Shell or directly)
    - Conditional render: when view === 'dashboard', show existing PipelineHero + PRFeed/PRDetail. When view === 'trends', show TrendsView.
    - Onboarding wizard check takes priority over view routing (wizard shown when step !== 'complete' and not dismissed, regardless of view).
    - Update Shell component to pass navigation props through to Sidebar. Update Shell's interface to accept `activeView` and `onNavigate` props, forwarding them to the Sidebar it renders.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "TrendsView" packages/web/src/components/trends/TrendsView.tsx returns match
    - grep -r "QualityScoreChart" packages/web/src/components/trends/TrendsView.tsx returns match
    - grep -r "VerdictRateChart" packages/web/src/components/trends/TrendsView.tsx returns match
    - grep -r "FindingTrendChart" packages/web/src/components/trends/TrendsView.tsx returns match
    - grep -r "RepoSelector" packages/web/src/components/trends/TrendsView.tsx returns match
    - grep -r "Trends" packages/web/src/components/layout/Sidebar.tsx returns match (nav item)
    - grep -r "activeView" packages/web/src/components/layout/Sidebar.tsx returns match (prop)
    - grep -r "TrendsView" packages/web/src/App.tsx returns match (view routing)
    - grep -r "dashboard.*trends" packages/web/src/App.tsx returns match (view state)
  </acceptance_criteria>
  <done>
    TrendsView renders all three chart types with repo selector. Sidebar has working Trends navigation. App.tsx routes between dashboard and trends views. Charts use DESIGN.md stage spectral colors per D-11. TypeScript compiles clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify quality trend charts visually</name>
  <what-built>Quality trend charts (quality score, per-stage verdict rates, finding frequency) accessible from sidebar navigation, with dark DESIGN.md theme and stage spectral colors</what-built>
  <how-to-verify>
    1. Start dev servers: `npm run dev` (or `npm run dev:all`)
    2. Visit http://localhost:5173
    3. Click "Trends" in the sidebar
    4. If no repos or pipeline data: verify you see contextual empty states ("Connect a repository to see quality trends" or "Need at least 2 reviews")
    5. If pipeline data exists (from prior testing): verify charts render with correct styling:
       - Quality score chart: electric lime (#C6FF3B) line, dark grid, 0-100 Y axis
       - Verdict rate charts: five per-stage charts with pass(green)/flag(amber)/block(red) stacked areas
       - Finding frequency: stacked area with critical(red)/notable(amber)/minor(gray)
    6. Verify: Clicking "Dashboard" in sidebar returns to the main pipeline view
    7. Verify: Repo selector dropdown works (if multiple repos connected)
    8. Verify: Charts match DESIGN.md aesthetic — dark surfaces, muted grid lines, correct typography
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with chart rendering or navigation</resume-signal>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles
cd packages/web && npx tsc --noEmit

# All trend components exist
ls packages/web/src/components/trends/

# Hooks exist
ls packages/web/src/hooks/useTrends.ts packages/web/src/hooks/useRepos.ts

# Sidebar has Trends nav
grep -r "Trends" packages/web/src/components/layout/Sidebar.tsx

# App.tsx routes between views
grep -r "TrendsView" packages/web/src/App.tsx
```
</verification>

<success_criteria>
- Quality score line chart renders per-repo data over time per D-08
- Per-stage verdict rate charts render as stacked areas per D-09
- Finding frequency chart shows severity trends per D-10
- All charts use DESIGN.md colors per D-11 (stage spectral, verdict colors, electric lime accent)
- Sidebar navigation works between Dashboard and Trends views
- Repo selector allows viewing trends for different repos per D-07
- Empty states shown for insufficient data per Pitfall 6
- Charts have explicit parent height to avoid Pitfall 1
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-quality-trends/06-03-SUMMARY.md`
</output>
