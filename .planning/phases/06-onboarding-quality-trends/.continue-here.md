---
phase: post-v1-push-reviews
task: 2
total_tasks: 9
status: in_progress
last_updated: 2026-04-02T12:52:02.424Z
---

<current_state>
Plan 2 (Push Reviews) execution in progress. Tasks 1-2 completed by Sonnet agent but NOT YET COMMITTED — changes are correct in working tree, need `git add` + `git commit`. Tasks 3-9 not started.
</current_state>

<completed_work>

## Deploy (independent)
- Mac Mini deployed via remote-executor agent
- Public URL: https://ryans-mac-mini.tail857f5c.ts.net
- LaunchAgent: com.quartermint.gstackapp (KeepAlive)
- Running Plan 1 code (provider abstraction)

## Plan 2 Tasks Done (uncommitted)
- Task 1: ReviewUnitTypeSchema added to `packages/shared/src/schemas/pipeline.ts`
- Task 2: `reviewUnits` table added to Drizzle schema, `reviewUnitId` column added to `pipelineRuns`, db-init.ts updated, test-db.ts updated

## Plans Written
- Plan 2: `docs/superpowers/plans/2026-03-31-push-reviews.md` (9 tasks)
- Plan 3: `docs/superpowers/plans/2026-03-31-backfill-cli.md` (5 tasks, depends on Plan 2)
</completed_work>

<remaining_work>

## Plan 2: Push Reviews (7 tasks remaining)
- Task 3: `ensureReviewUnit()` + update `tryCreatePipelineRun()` in idempotency.ts
- Task 4: Push webhook handler in handlers.ts + update PR handler
- Task 5: Generalize orchestrator PipelineInput for PR + push (diff fetching, comment dispatch)
- Task 6: `postCommitComment()` in comment.ts for push reviews
- Task 7: Update pipelines API route to join through review_units
- Task 8: Frontend PR/Push type badges (PRCard, PRDetail, PipelineListItem type)
- Task 9: Run db-init migration + full test suite + e2e verify

## Plan 3: Backfill CLI (5 tasks, after Plan 2)
- Task 1: Session grouper pure function
- Task 2: package.json script
- Task 3: Full CLI with arg parsing, semaphore, dry-run
- Task 4: Small-scope integration test
- Task 5: Full 30-day backfill execution

## Manual Steps
- Update GitHub App webhook URL to Funnel URL
- Add push event to GitHub App subscribed events
- Redeploy to Mac Mini after Plan 2 ships
</remaining_work>

<decisions_made>

- Sonnet implements, Opus reviews for token conservation
- Tasks 1+2 batched into single Sonnet agent (tightly coupled schema changes)
- review_unit_id added alongside pr_id (not replacing) for backwards compat
- postCommitComment for push reviews, updatePRComment for PR reviews (same rendered markdown)
- Push reviews only for default branch, skip force-pushes and empty pushes
</decisions_made>

<blockers>
None. Sonnet agent hit a 500 API error mid-execution but completed the work.
</blockers>

<context>
The execution model is subagent-driven-development: Sonnet agents implement each task, Opus reviews inline between tasks. This was chosen to conserve tokens while maintaining quality. Plan files contain complete code for every step — agents can follow them mechanically.

Key product insight driving this work: best builders ship to main, not PRs. Push reviews + backfill = the product works for its actual users from day one. No competitor (CodeRabbit, Qodo, Graphite) reviews pushes to main.
</context>

<next_action>
1. Commit the uncommitted Task 1+2 changes (4 files)
2. Dispatch Sonnet agent for Task 3 (ensureReviewUnit + tryCreatePipelineRun)
3. Continue through Tasks 4-9 with sonnet-implements/opus-reviews pattern
</next_action>
