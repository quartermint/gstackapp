---
phase: 04-dashboard-pipeline-visualization
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - packages/web/src/components/pipeline/PipelineHero.tsx
  - packages/web/src/components/pipeline/PipelineTopology.tsx
  - packages/web/src/components/pipeline/StageNode.tsx
  - packages/web/src/components/pipeline/StageConnector.tsx
  - packages/web/src/hooks/usePipeline.ts
  - packages/web/src/App.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "The pipeline visualization occupies 60%+ of viewport height as the hero view"
    - "Five stages render as horizontally connected flow nodes in left-to-right order"
    - "Each stage node displays its spectral identity color (CEO=#FF8B3E, Eng=#36C9FF, Design=#B084FF, QA=#2EDB87, Security=#FF5A67)"
    - "Active/RUNNING stages pulse with a 2s ease-in-out infinite glow animation using the stage's spectral color"
    - "Completed stages animate from 20% opacity to 100% over 400ms ease (dim-to-bright reveal)"
    - "SVG connector lines between stages show animated signal flow traces (linear, 2.5s loop)"
    - "Pipeline data is fetched via TanStack Query and updates in real-time via SSE"
  artifacts:
    - path: "packages/web/src/components/pipeline/PipelineHero.tsx"
      provides: "Hero view container with 60vh minimum height"
      exports: ["PipelineHero"]
    - path: "packages/web/src/components/pipeline/PipelineTopology.tsx"
      provides: "5-node horizontal topology layout"
      exports: ["PipelineTopology"]
    - path: "packages/web/src/components/pipeline/StageNode.tsx"
      provides: "Individual stage node with dim-to-bright and pulse animations"
      exports: ["StageNode"]
    - path: "packages/web/src/components/pipeline/StageConnector.tsx"
      provides: "SVG connector line with animated trace"
      exports: ["StageConnector"]
    - path: "packages/web/src/hooks/usePipeline.ts"
      provides: "TanStack Query hook for pipeline data"
      exports: ["usePipelineList", "usePipelineDetail"]
  key_links:
    - from: "packages/web/src/components/pipeline/StageNode.tsx"
      to: "packages/web/src/lib/constants.ts"
      via: "STAGE_COLORS and VERDICT_COLORS imports for node styling"
      pattern: "STAGE_COLORS\\[stage\\]"
    - from: "packages/web/src/hooks/usePipeline.ts"
      to: "packages/web/src/api/client.ts"
      via: "client.api.pipelines and queryKeys for TanStack Query"
      pattern: "client\\.api\\.pipelines"
    - from: "packages/web/src/components/pipeline/PipelineHero.tsx"
      to: "packages/web/src/hooks/usePipeline.ts"
      via: "usePipelineList() for latest pipeline data"
      pattern: "usePipelineList"
---

<objective>
Build the pipeline hero visualization -- the signature UI feature that makes the review process visible.

Purpose: The pipeline topology IS the product. This plan creates the hero view showing 5 cognitive stages as connected flow nodes with real-time animations driven by SSE events. When a pipeline is running, users see stages pulse and light up as they complete. This is what makes gstackapp feel like an operations room.

Output: A working pipeline hero view at 60%+ viewport height with animated stage nodes, connector traces, and real-time SSE-driven state updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-pipeline-visualization/04-CONTEXT.md
@.planning/phases/04-dashboard-pipeline-visualization/04-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- From Plan 04-02 (scaffold — must be completed first) -->

From packages/web/src/lib/constants.ts:
```typescript
export const STAGE_COLORS: Record<Stage, string> = {
  ceo: '#FF8B3E', eng: '#36C9FF', design: '#B084FF', qa: '#2EDB87', security: '#FF5A67'
}
export const STAGE_LABELS: Record<Stage, string> = {
  ceo: 'CEO Review', eng: 'Eng Review', design: 'Design Review', qa: 'QA', security: 'Security'
}
export const VERDICT_COLORS: Record<Verdict | 'RUNNING', string> = {
  PASS: '#2EDB87', FLAG: '#FFB020', BLOCK: '#FF5A67', SKIP: '#6F7C90', RUNNING: '#36C9FF'
}
```

From packages/web/src/api/client.ts:
```typescript
export const client = hc<AppType>('/')
export const queryKeys = {
  pipelines: {
    all: ['pipelines'] as const,
    list: () => [...queryKeys.pipelines.all, 'list'] as const,
    detail: (id: string) => [...queryKeys.pipelines.all, 'detail', id] as const,
  },
}
```

From packages/web/src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From packages/web/src/app.css (custom keyframes):
```css
@keyframes pulse-glow {
  0%, 100% { box-shadow: 0 0 8px 2px var(--glow-color); }
  50% { box-shadow: 0 0 24px 6px var(--glow-color); }
}
@keyframes trace-flow {
  0% { stroke-dashoffset: 100; }
  100% { stroke-dashoffset: 0; }
}
```

From packages/web/src/components/shared/VerdictBadge.tsx:
```typescript
export function VerdictBadge({ verdict }: { verdict: Verdict | 'RUNNING' | 'PENDING' }): JSX.Element
```

From packages/web/src/components/shared/EmptyState.tsx:
```typescript
export function EmptyState({ title, description, icon }: Props): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline data hooks and stage node/connector components</name>
  <read_first>
    - packages/web/src/api/client.ts (Hono RPC client and queryKeys factory)
    - packages/web/src/lib/constants.ts (STAGE_COLORS, STAGE_LABELS, VERDICT_COLORS)
    - packages/web/src/lib/cn.ts (className utility)
    - packages/web/src/app.css (custom keyframes: pulse-glow, trace-flow)
    - packages/web/src/types/sse.ts (PipelineSSEEvent type)
    - packages/api/src/routes/pipelines.ts (API response shape for type alignment)
    - DESIGN.md (Motion section — dim-to-bright, running pulse, pipeline trace specifications)
  </read_first>
  <files>
    packages/web/src/hooks/usePipeline.ts
    packages/web/src/components/pipeline/StageNode.tsx
    packages/web/src/components/pipeline/StageConnector.tsx
  </files>
  <action>
    **Create packages/web/src/hooks/usePipeline.ts** — TanStack Query hooks for pipeline data:
    - `usePipelineList()` — `useQuery` with `queryKey: queryKeys.pipelines.list()`, fetches via `client.api.pipelines.$get()`, returns the parsed JSON response. Per D-15.
    - `usePipelineDetail(id: string)` — `useQuery` with `queryKey: queryKeys.pipelines.detail(id)`, fetches via `client.api.pipelines[':id'].$get({ param: { id } })`, enabled only when `id` is truthy. Per D-15.
    - Both hooks return the standard TanStack Query result (data, isLoading, error, etc.)
    - Type the response data based on the API route's actual return shape. Use `Awaited<ReturnType<typeof res.json>>` pattern for type inference from Hono RPC.

    **Create packages/web/src/components/pipeline/StageNode.tsx** — Individual stage node:
    - Props: `{ stage: Stage, verdict: Verdict | 'RUNNING' | 'PENDING', summary?: string }`
    - Layout: A card-like node with `w-36 h-44` (144x176px), `rounded-lg bg-surface border border-border` (per DESIGN.md border radius lg: 12px)
    - Top edge: 3px colored bar using the stage's spectral identity color from `STAGE_COLORS[stage]`
    - Content: Stage label (`STAGE_LABELS[stage]`) in `font-mono text-[11px] uppercase tracking-[0.06em]`, verdict badge (`VerdictBadge` component)
    - **Dim-to-bright reveal (D-05):** Use CSS transition on opacity. PENDING state: `opacity-20`. RUNNING state: `opacity-100`. PASS/FLAG/BLOCK/SKIP state: `opacity-100`. Add `transition-opacity duration-[400ms] ease-out` to the root element. CRITICAL per Pitfall 4: use STABLE React keys (stage name, not array index) so the DOM element persists and the CSS transition fires.
    - **Running pulse (D-06):** When verdict is `'RUNNING'`, add inline style `{ '--glow-color': STAGE_COLORS[stage] } as React.CSSProperties` and class `animate-[pulse-glow_2s_ease-in-out_infinite]`. The `pulse-glow` keyframe is defined in app.css.
    - **Completed state:** When verdict is PASS/FLAG/BLOCK/SKIP, full opacity, no animation. Show verdict badge with color.
    - Use `cn()` utility for conditional class composition.

    **Create packages/web/src/components/pipeline/StageConnector.tsx** — SVG connector between nodes:
    - Props: `{ active: boolean, color?: string }` (active = pipeline is running or stage preceding is complete)
    - Renders an SVG element `<svg width="48" height="2" className="mx-1">` with a horizontal line path
    - **Pipeline trace animation (D-04):** When `active`, the line has `stroke-dasharray: 8 8` and `animation: trace-flow 2.5s linear infinite` (class from app.css). Stroke color is `var(--color-accent)` (electric lime) when active.
    - When inactive: static dashed line in border color (`#2A2F3A`)
    - Per Pitfall 6: keep SVG path simple (straight horizontal line), use CSS keyframes for animation.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/hooks/usePipeline.ts exports usePipelineList and usePipelineDetail hooks using TanStack Query + Hono RPC client
    - packages/web/src/components/pipeline/StageNode.tsx renders stage node with spectral color top bar, opacity transition for dim-to-bright, and pulse-glow animation for RUNNING state
    - packages/web/src/components/pipeline/StageConnector.tsx renders SVG connector with trace-flow animation when active
    - StageNode uses stable key pattern (stage name) and CSS transition-opacity for dim-to-bright
    - StageNode uses --glow-color CSS variable with animate-[pulse-glow_2s_ease-in-out_infinite] for running pulse
    - TypeScript passes for packages/web
  </acceptance_criteria>
  <done>Pipeline data hooks fetch from API with type safety, stage nodes animate between PENDING/RUNNING/completed states, connectors show animated trace</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline topology layout and hero container wired into App</name>
  <read_first>
    - packages/web/src/components/pipeline/StageNode.tsx (just created)
    - packages/web/src/components/pipeline/StageConnector.tsx (just created)
    - packages/web/src/hooks/usePipeline.ts (just created)
    - packages/web/src/components/shared/EmptyState.tsx (for no-pipeline state)
    - packages/web/src/components/shared/Skeleton.tsx (for loading state)
    - packages/web/src/App.tsx (current placeholder to replace)
    - DESIGN.md (Layout section — 60%+ viewport height, left-anchored, continuous pipeline rail)
  </read_first>
  <files>
    packages/web/src/components/pipeline/PipelineTopology.tsx
    packages/web/src/components/pipeline/PipelineHero.tsx
    packages/web/src/App.tsx
  </files>
  <action>
    **Create packages/web/src/components/pipeline/PipelineTopology.tsx** — 5-node horizontal layout:
    - Props: `{ stages: Array<{ stage: Stage, verdict: Verdict | 'RUNNING' | 'PENDING', summary?: string }> }`
    - Layout: `flex items-center justify-center` — 5 StageNode components with StageConnector between each pair (4 connectors total)
    - Per D-01: horizontal left-to-right flow
    - Per D-03: 5 stages as connected flow nodes
    - Stage order MUST be: CEO -> Eng -> Design -> QA -> Security (left to right)
    - Connector `active` prop: true when the preceding stage has a verdict that is not 'PENDING' (i.e., the stage has started or completed)
    - Map over the fixed 5-stage order, merge with actual stage data from props. If no data for a stage, default to verdict 'PENDING'.

    **Create packages/web/src/components/pipeline/PipelineHero.tsx** — Hero container:
    - Uses `usePipelineList()` to fetch the latest pipeline run
    - Display the MOST RECENT pipeline run (first item from the reverse-chronological list)
    - Container: `min-h-[60vh] flex flex-col items-center justify-center bg-background` per D-02 (60%+ viewport height)
    - Header above topology: pipeline status, PR title and repo name in `font-display text-2xl` with `tracking-[-0.02em]`
    - If loading: render skeleton placeholders (5 skeleton rectangles in a row)
    - If no pipelines exist: render EmptyState with title "No reviews yet" and description "Open a PR on a connected repository to trigger your first review"
    - If data exists: render PipelineTopology with the stage results from the latest run
    - Map API response `stages` array (which has `{ stage, verdict }` per stage) to the PipelineTopology props format. Include all 5 stages even if some are PENDING.

    **Update packages/web/src/App.tsx** — Wire hero into the app:
    - Replace the placeholder div with `<PipelineHero />` as the main content inside Shell
    - Keep `useSSEQuerySync()` call at the top
    - Import PipelineHero from `./components/pipeline/PipelineHero`
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/components/pipeline/PipelineTopology.tsx renders 5 StageNode components with 4 StageConnector components between them in CEO->Eng->Design->QA->Security order
    - packages/web/src/components/pipeline/PipelineHero.tsx uses usePipelineList(), shows most recent pipeline, occupies min-h-[60vh]
    - PipelineHero handles loading state with skeletons and empty state with EmptyState component
    - packages/web/src/App.tsx renders PipelineHero inside Shell and calls useSSEQuerySync()
    - TypeScript passes for packages/web
  </acceptance_criteria>
  <done>Pipeline hero view occupies 60%+ viewport, shows 5 connected stage nodes with real-time animation, empty and loading states handled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Pipeline hero visualization looks and animates correctly</name>
  <files>packages/web/src/components/pipeline/PipelineHero.tsx</files>
  <action>
    Human visual verification of pipeline hero.

    What was built: Pipeline hero visualization with 5 connected stage nodes, spectral identity colors, dim-to-bright reveal animation, running pulse animation, and SVG trace connectors. Real-time updates via SSE.

    How to verify:
    1. Start both servers: `npm run dev` (API on 3000) and `npm run dev --workspace=packages/web` (Vite on 5173)
    2. Open http://localhost:5173/ in a browser
    3. Verify dark background (#0B0D11), sidebar with "gstackapp" in electric lime (#C6FF3B)
    4. Verify pipeline hero takes 60%+ of viewport height
    5. If no pipeline data: verify "No reviews yet" empty state
    6. If pipeline data exists: verify 5 stage nodes in horizontal row (CEO -> Eng -> Design -> QA -> Security)
    7. Verify each node has its spectral identity color bar at top (amber, cyan, violet, green, coral)
    8. Verify connector lines between stages with dashed pattern
    9. Verify bottom intelligence strip is visible at the bottom
    10. Check DESIGN.md compliance: typography (General Sans headings, Geist body), spacing, border radius

    Resume signal: Type "approved" or describe visual issues to fix
  </action>
  <verify>
    <automated>curl -s http://localhost:5173/ -o /dev/null -w '%{http_code}'</automated>
  </verify>
  <done>User confirms pipeline hero visualization renders correctly with proper colors, layout, and animations</done>
</task>

</tasks>

<verification>
- Pipeline hero view renders at 60%+ viewport height
- 5 stage nodes display in horizontal CEO -> Eng -> Design -> QA -> Security order
- Each node uses its spectral identity color
- Animations are CSS-driven (check for transition-opacity, animate-[pulse-glow], trace-flow in DOM)
- SSE events from API trigger TanStack Query invalidation (check Network tab for refetches after SSE events)
- Empty state shows when no pipeline data
- Loading skeletons show during fetch
</verification>

<success_criteria>
- Pipeline hero occupies 60%+ viewport height (DASH-01)
- 5 stages as connected flow nodes with spectral colors (DASH-02)
- Dim-to-bright reveal animation on stage completion (DASH-04)
- Running pulse animation on active stages (DASH-05)
- User confirms visual correctness at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-pipeline-visualization/04-03-SUMMARY.md`
</output>
