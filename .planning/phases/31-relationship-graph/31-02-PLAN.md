---
phase: 31-relationship-graph
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - packages/web/package.json
  - packages/web/src/components/graph/relationship-graph.tsx
  - packages/web/src/components/graph/graph-canvas.tsx
  - packages/web/src/components/graph/graph-node.tsx
  - packages/web/src/components/graph/graph-edge.tsx
  - packages/web/src/components/graph/graph-tooltip.tsx
  - packages/web/src/hooks/use-force-simulation.ts
  - packages/web/src/components/ui/loading-skeleton.tsx
  - packages/web/src/components/layout/dashboard-layout.tsx
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - INTEL-07
  - INTEL-08
must_haves:
  truths:
    - "User can navigate to the graph view via the nav bar tab"
    - "Graph displays all projects as nodes with health-colored dots and host-colored borders"
    - "Graph shows dependency connections as directed edges between nodes"
    - "Hovering a node shows a tooltip with project name, health status, and dependency count (D-01)"
    - "Clicking a node highlights its full dependency chain and dims unrelated nodes (D-02)"
    - "User can drag nodes to rearrange and zoom/pan to navigate (D-03)"
    - "Dragged nodes stay pinned in place (D-04)"
    - "d3-force is NOT in the main dashboard bundle (lazy-loaded via React.lazy)"
  artifacts:
    - path: "packages/web/src/components/graph/relationship-graph.tsx"
      provides: "Main graph page component (default export for React.lazy)"
      exports: ["default"]
    - path: "packages/web/src/components/graph/graph-canvas.tsx"
      provides: "SVG container managing simulation and rendering nodes/edges"
      exports: ["GraphCanvas"]
    - path: "packages/web/src/components/graph/graph-node.tsx"
      provides: "Individual SVG node circle with label, health dot, host border"
      exports: ["GraphNodeElement"]
    - path: "packages/web/src/components/graph/graph-edge.tsx"
      provides: "SVG edge line with arrow marker"
      exports: ["GraphEdgeElement"]
    - path: "packages/web/src/components/graph/graph-tooltip.tsx"
      provides: "Hover tooltip positioned near the node"
      exports: ["GraphTooltip"]
    - path: "packages/web/src/hooks/use-force-simulation.ts"
      provides: "React hook managing d3-force simulation lifecycle"
      exports: ["useForceSimulation"]
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/graph/relationship-graph.tsx"
      via: "React.lazy dynamic import"
      pattern: "lazy.*import.*relationship-graph"
    - from: "packages/web/src/components/graph/graph-canvas.tsx"
      to: "packages/web/src/hooks/use-force-simulation.ts"
      via: "hook call"
      pattern: "useForceSimulation"
    - from: "packages/web/src/hooks/use-force-simulation.ts"
      to: "d3-force"
      via: "ESM import"
      pattern: "from.*d3-force"
    - from: "packages/web/src/components/graph/relationship-graph.tsx"
      to: "packages/web/src/lib/graph-data.ts"
      via: "import buildGraphData and getHighlightChain"
      pattern: "import.*graph-data"
    - from: "packages/web/src/components/layout/dashboard-layout.tsx"
      to: "View type"
      via: "type union includes graph"
      pattern: "dashboard.*network.*graph"
---

<objective>
Install d3-force, build the interactive graph visualization components, wire the graph view into the dashboard navigation with lazy loading, and verify d3-force is code-split into a separate chunk.

Purpose: This delivers the visible, interactive graph that fulfills INTEL-07 (interactive dependency visualization) and INTEL-08 (d3-force, lazy-loaded, code-split). All user-facing decisions D-01 through D-10 are implemented here.
Output: Working graph view accessible via nav bar tab, force-directed layout, hover tooltips, click-to-highlight, drag/pin, zoom/pan. d3-force in a separate Vite chunk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-relationship-graph/31-CONTEXT.md
@.planning/phases/31-relationship-graph/31-RESEARCH.md
@.planning/phases/31-relationship-graph/31-01-SUMMARY.md

@packages/web/src/App.tsx
@packages/web/src/components/layout/dashboard-layout.tsx
@packages/web/src/lib/grouping.ts
@packages/web/src/lib/health-colors.ts
@packages/web/src/components/ui/host-badge.tsx
@packages/web/src/components/ui/loading-skeleton.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/web/src/lib/graph-data.ts (created in Plan 01):
```typescript
export interface GraphNode {
  id: string;
  name: string;
  host: "local" | "mac-mini" | "github";
  riskLevel: "healthy" | "warning" | "critical" | "unmonitored";
  dependencyCount: number;
  x?: number;
  y?: number;
  fx?: number | null;
  fy?: number | null;
  vx?: number;
  vy?: number;
  index?: number;
}

export interface GraphEdge {
  source: string;
  target: string;
}

export function buildGraphData(projects: ProjectItem[]): { nodes: GraphNode[]; edges: GraphEdge[] };
export function getHighlightChain(slug: string, projects: ProjectItem[]): Set<string>;
```

From packages/web/src/lib/grouping.ts:
```typescript
export interface ProjectItem {
  slug: string;
  name: string;
  host: "local" | "mac-mini" | "github";
  riskLevel: "healthy" | "warning" | "critical" | "unmonitored";
  dependsOn: string[];
  // ... other fields
}
```

From packages/web/src/App.tsx:
```typescript
type View = "dashboard" | "network";  // Must become "dashboard" | "network" | "graph"
```

From packages/web/src/components/layout/dashboard-layout.tsx:
```typescript
type View = "dashboard" | "network";  // Must become "dashboard" | "network" | "graph"
// Line 171: (["dashboard", "network"] as const) — must add "graph"
```

From packages/web/src/components/ui/host-badge.tsx:
```typescript
// Host color tokens for node border colors (D-06):
const hostStyles: Record<string, string> = {
  local: "bg-warm-gray/8 ...",
  "mac-mini": "bg-terracotta/8 ...",
  github: "bg-indigo-500/8 ...",
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install d3-force, create graph components and useForceSimulation hook</name>
  <files>
    packages/web/package.json,
    packages/web/src/hooks/use-force-simulation.ts,
    packages/web/src/components/graph/relationship-graph.tsx,
    packages/web/src/components/graph/graph-canvas.tsx,
    packages/web/src/components/graph/graph-node.tsx,
    packages/web/src/components/graph/graph-edge.tsx,
    packages/web/src/components/graph/graph-tooltip.tsx,
    packages/web/src/__tests__/components/relationship-graph.test.tsx
  </files>
  <read_first>
    packages/web/package.json,
    packages/web/src/lib/graph-data.ts,
    packages/web/src/lib/health-colors.ts,
    packages/web/src/components/ui/host-badge.tsx,
    packages/web/src/components/ui/loading-skeleton.tsx,
    .planning/phases/31-relationship-graph/31-RESEARCH.md
  </read_first>
  <behavior>
    - useForceSimulation hook creates a d3-force simulation from nodes and edges, returns positioned nodes via React state
    - GraphCanvas renders an SVG with edges and nodes using positions from the simulation
    - GraphNodeElement renders a circle with host-colored border stroke and a small health-colored dot
    - GraphEdgeElement renders a line with an arrowhead marker
    - GraphTooltip shows project name, health status, dependency count on hover (D-01)
    - Clicking a node calls onSelect callback (for highlight chain computation) (D-02)
    - Dragging a node sets fx/fy to pin it in place (D-03, D-04)
    - Zoom/pan via mouse wheel and drag on empty space (D-03)
    - RelationshipGraph is the page-level component: calls buildGraphData, manages highlight state via getHighlightChain, renders GraphCanvas
    - RelationshipGraph has a default export (required for React.lazy)
  </behavior>
  <action>
    1. Install d3-force: `pnpm --filter @mission-control/web add d3-force && pnpm --filter @mission-control/web add -D @types/d3-force`

    2. Create `packages/web/src/hooks/use-force-simulation.ts`:
       - Import forceSimulation, forceLink, forceManyBody, forceCenter, forceCollide from "d3-force"
       - Import type Simulation from "d3-force"
       - Accept (nodes: GraphNode[], edges: GraphEdge[], width: number, height: number)
       - Clone nodes before passing to simulation to avoid mutating input (Research Pitfall 1)
       - Use simulation parameters from research: chargeStrength -200, linkDistance 120, collideRadius 45, centerStrength 0.05, alphaDecay 0.02, velocityDecay 0.4
       - Use `.id(d => d.id)` on forceLink to avoid index-based linking (Research Pitfall 3)
       - Throttle setPositions with requestAnimationFrame to avoid render storm (Research anti-pattern)
       - Return { positions, simulationRef } where positions is GraphNode[] with x/y filled in
       - Cleanup: sim.stop() on unmount
       - Memoize nodes/edges arrays via useMemo in the caller to avoid re-creating simulation (Research Pitfall 2)

    3. Create `packages/web/src/components/graph/graph-node.tsx`:
       - SVG group: circle (radius 20) with host-colored stroke (D-06) and white/dark fill
       - Small health dot: circle (radius 4) positioned at top-right of main circle, filled with risk color (D-05)
       - Host border colors: local="#9c8b7e" (warm-gray), mac-mini="#d4713a" (terracotta), github="#6366f1" (indigo-500)
       - Risk fill colors: healthy="#6b8f71" (sage), warning="#c49b2a" (gold-status), critical="#b7410e" (rust), unmonitored="#9c8b7e" (warm-gray)
       - Text label below circle: project name, font-size 10px, fill matches text color
       - Props: node (GraphNode with x/y), highlighted (boolean), dimmed (boolean), onMouseEnter, onMouseLeave, onMouseDown (for drag)
       - When dimmed: opacity 0.2. When highlighted: opacity 1.0, stroke-width 3. Default: opacity 0.8, stroke-width 2.

    4. Create `packages/web/src/components/graph/graph-edge.tsx`:
       - SVG line from source position to target position
       - Arrow marker at target end (small triangle via SVG <marker> and <defs>)
       - Stroke color: warm-gray (#9c8b7e) at 40% opacity. When highlighted: full opacity. When dimmed: 10% opacity.
       - Props: sourceX, sourceY, targetX, targetY, highlighted (boolean), dimmed (boolean)
       - Shorten the line by node radius (20px) at both ends so arrows don't overlap circles

    5. Create `packages/web/src/components/graph/graph-tooltip.tsx`:
       - Absolutely positioned div near the hovered node's screen position
       - Shows: project name (bold), health status text (colored), "N dependencies" count
       - Dark background with light text, rounded corners, small shadow, font-size 12px
       - Props: node (GraphNode), x (screen position), y (screen position)
       - Per D-01: tooltip with project name, health status, dependency count
       - Per D-07: minimal and clean

    6. Create `packages/web/src/components/graph/graph-canvas.tsx`:
       - SVG element filling container width/height
       - Uses useForceSimulation hook to get positioned nodes
       - Renders edges first (behind nodes), then nodes
       - Manages zoom/pan state: { x, y, scale } transform on a wrapping <g> element (D-03)
       - Mouse wheel: adjust scale (clamp 0.3-3.0). Mouse drag on empty SVG: adjust x/y.
       - Node drag: on mousedown on a node, switch to node-drag mode. Convert mouse coords to graph space via `(mouseX - transform.x) / transform.scale` (Research Pitfall 5). Set node.fx/fy during drag, keep pinned on drag end (D-04). Reheat simulation during drag (alphaTarget 0.3), cool on drag end (alphaTarget 0).
       - Props: nodes (GraphNode[]), edges (GraphEdge[]), highlightedSlugs (Set<string> | null), onNodeHover (slug | null), onNodeClick (slug), width, height

    7. Create `packages/web/src/components/graph/relationship-graph.tsx`:
       - Default export (required for React.lazy)
       - Receives `projects: ProjectItem[]` prop
       - Calls buildGraphData(projects) with useMemo to get nodes/edges
       - Manages selectedSlug state for highlight chain
       - Calls getHighlightChain(selectedSlug, projects) with useMemo to get highlighted set
       - Manages hoveredNode state for tooltip
       - Uses a ref + ResizeObserver for responsive container sizing (fills available width, height ~500px min)
       - Renders GraphCanvas + GraphTooltip (when hoveredNode is set)
       - Click on empty space clears selection (resets highlight)
       - Title bar: "Project Relationships" with subtitle "N projects, M connections"

    8. Create `packages/web/src/__tests__/components/relationship-graph.test.tsx`:
       - Smoke test: renders without crashing with empty projects array
       - Smoke test: renders with mock projects, verify SVG element exists
       - Note: d3-force simulation is hard to unit test (async, animation); these are render smoke tests

    Per D-07: minimal, clean graph with details on hover/click.
    Per D-08: only import d3-force module, not full d3.
  </action>
  <verify>
    <automated>pnpm --filter @mission-control/web test -- relationship-graph</automated>
  </verify>
  <acceptance_criteria>
    - d3-force and @types/d3-force appear in packages/web/package.json dependencies/devDependencies
    - packages/web/src/hooks/use-force-simulation.ts exists and imports from "d3-force"
    - packages/web/src/components/graph/relationship-graph.tsx has a default export
    - packages/web/src/components/graph/graph-canvas.tsx exports GraphCanvas
    - packages/web/src/components/graph/graph-node.tsx exports GraphNodeElement
    - packages/web/src/components/graph/graph-edge.tsx exports GraphEdgeElement
    - packages/web/src/components/graph/graph-tooltip.tsx exports GraphTooltip
    - No import of "d3" (full package) anywhere -- only "d3-force"
    - Host border colors match: local=#9c8b7e, mac-mini=#d4713a, github=#6366f1
    - Risk fill colors match: healthy=#6b8f71, warning=#c49b2a, critical=#b7410e, unmonitored=#9c8b7e
    - graph-canvas.tsx calls useForceSimulation
    - relationship-graph.tsx imports buildGraphData and getHighlightChain from graph-data
    - Test passes: pnpm --filter @mission-control/web test -- relationship-graph
  </acceptance_criteria>
  <done>
    All graph components render correctly. Force simulation positions nodes. Hover shows tooltip. Click highlights chain. Drag pins nodes. Zoom/pan works. All smoke tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire graph view into App navigation with lazy loading and verify code split</name>
  <files>
    packages/web/src/App.tsx,
    packages/web/src/components/layout/dashboard-layout.tsx,
    packages/web/src/components/ui/loading-skeleton.tsx
  </files>
  <read_first>
    packages/web/src/App.tsx,
    packages/web/src/components/layout/dashboard-layout.tsx,
    packages/web/src/components/ui/loading-skeleton.tsx,
    packages/web/src/components/graph/relationship-graph.tsx
  </read_first>
  <action>
    1. Add GraphSkeleton to `packages/web/src/components/ui/loading-skeleton.tsx`:
       - Export a new `GraphSkeleton` component
       - Renders a placeholder matching the graph area: centered pulsing circle with radiating lines, similar style to HeroSkeleton/BoardSkeleton
       - Use animate-pulse, bg-surface-warm, rounded shapes

    2. Update `packages/web/src/components/layout/dashboard-layout.tsx`:
       - Change View type from `"dashboard" | "network"` to `"dashboard" | "network" | "graph"` (per D-10: graph is a separate view)
       - Update the nav bar map on line 171: change `(["dashboard", "network"] as const)` to `(["dashboard", "network", "graph"] as const)`
       - Add label mapping: "graph" displays as "Graph" in the nav button

    3. Update `packages/web/src/App.tsx`:
       - Add imports: `import { lazy, Suspense } from "react"` (lazy and Suspense from React)
       - Add lazy import: `const RelationshipGraph = lazy(() => import("./components/graph/relationship-graph.js"))`
       - Import GraphSkeleton from loading-skeleton
       - Change View type from `"dashboard" | "network"` to `"dashboard" | "network" | "graph"`
       - Update the view rendering block (around line 224):
         ```
         {view === "graph" ? (
           <Suspense fallback={<GraphSkeleton />}>
             <RelationshipGraph projects={allProjects} />
           </Suspense>
         ) : view === "network" ? (
           <NetworkPage />
         ) : (
           // ... existing dashboard content
         )}
         ```
       - Do NOT import relationship-graph.tsx eagerly. The lazy() call handles code splitting per INTEL-08.
       - Do NOT import d3-force or graph-data types in App.tsx (type imports are OK since they're erased at compile time, but unnecessary).

    4. Verify code split works:
       - Run `pnpm --filter @mission-control/web build`
       - Check `ls -la packages/web/dist/assets/` -- there should be a separate chunk containing d3-force (look for a chunk file that wasn't there before)
       - The main index chunk should NOT grow by ~40KB+ (d3-force size)

    Per D-09: lazy-loaded via React.lazy, code-split, d3-force not in main bundle.
    Per D-10: graph is a separate route/view, not embedded in departure board.
  </action>
  <verify>
    <automated>pnpm --filter @mission-control/web build && pnpm typecheck && pnpm --filter @mission-control/web test</automated>
  </verify>
  <acceptance_criteria>
    - View type in App.tsx includes "graph": grep for '"dashboard" | "network" | "graph"' in App.tsx
    - View type in dashboard-layout.tsx includes "graph": grep for '"dashboard" | "network" | "graph"' in dashboard-layout.tsx
    - Nav bar renders 3 tabs: grep for '["dashboard", "network", "graph"]' in dashboard-layout.tsx
    - React.lazy import exists: grep for 'lazy.*import.*relationship-graph' in App.tsx
    - Suspense wrapper exists: grep for 'Suspense.*GraphSkeleton' in App.tsx
    - GraphSkeleton exported from loading-skeleton.tsx
    - d3-force is NOT imported eagerly in App.tsx (no 'd3-force' import in App.tsx)
    - Build succeeds: pnpm --filter @mission-control/web build exits 0
    - Typecheck passes: pnpm typecheck exits 0
    - All tests pass: pnpm --filter @mission-control/web test exits 0
    - Separate chunk file exists in dist/assets/ containing d3-force code
  </acceptance_criteria>
  <done>
    Graph view is accessible via the "Graph" tab in the nav bar. Clicking "Graph" lazy-loads the relationship graph component with a loading skeleton. d3-force is in a separate Vite chunk, not in the main bundle. All types check, all tests pass, build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` -- all types valid
2. `pnpm test` -- all tests pass (including new graph-data and relationship-graph tests)
3. `pnpm --filter @mission-control/web build` -- build succeeds
4. Verify code split: d3-force chunk exists separately from main bundle in dist/assets/
5. Manual: navigate to Graph tab, see force-directed layout, hover for tooltip, click to highlight chain, drag to pin, zoom/pan
</verification>

<success_criteria>
- Interactive force-directed graph renders all projects as nodes with dependency edges (INTEL-07)
- Nodes show health dots colored by riskLevel (D-05) and borders colored by host (D-06)
- Hover tooltip shows project name, health status, dependency count (D-01)
- Click highlights full upstream + downstream chain, dims unrelated nodes (D-02)
- Drag repositions and pins nodes (D-03, D-04)
- Zoom/pan navigation works (D-03)
- Graph accessible via nav bar "Graph" tab (D-10)
- d3-force lazy-loaded via React.lazy, code-split into separate Vite chunk (INTEL-08, D-08, D-09)
- Minimal clean design with details on hover/click (D-07)
- All tests pass, typecheck passes, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/31-relationship-graph/31-02-SUMMARY.md`
</output>
