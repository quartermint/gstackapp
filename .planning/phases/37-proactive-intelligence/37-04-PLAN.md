---
phase: 37-proactive-intelligence
plan: 04
type: execute
wave: 3
depends_on: ["37-02", "37-03"]
files_modified:
  - packages/web/src/components/insights/insight-card.tsx
  - packages/web/src/components/insights/insight-triage.tsx
  - packages/web/src/components/whats-new/insight-badges.tsx
  - packages/web/src/App.tsx
autonomous: false
requirements:
  - PROACT-01
  - PROACT-02
  - PROACT-06

must_haves:
  truths:
    - "Clicking an insight badge expands to show detail cards with dismiss/snooze actions"
    - "Stale capture insights link to the existing triage view for resolution"
    - "Dismissed insights disappear immediately (optimistic) and don't return"
    - "Snoozed insights disappear for the specified duration"
    - "Activity patterns visible as insight cards without manual analysis"
    - "All proactive intelligence generated locally (no external API dependency)"
  artifacts:
    - path: "packages/web/src/components/insights/insight-card.tsx"
      provides: "Individual insight detail card with actions"
      contains: "InsightCard"
    - path: "packages/web/src/components/insights/insight-triage.tsx"
      provides: "Stale capture triage inline component"
      contains: "InsightTriage"
  key_links:
    - from: "packages/web/src/components/whats-new/insight-badges.tsx"
      to: "packages/web/src/components/insights/insight-card.tsx"
      via: "renders InsightCard on badge click"
      pattern: "InsightCard"
    - from: "packages/web/src/components/insights/insight-card.tsx"
      to: "packages/web/src/hooks/use-insights.ts"
      via: "dismiss/snooze callbacks"
      pattern: "onDismiss|onSnooze"
---

<objective>
Build the insight detail cards, stale capture triage UX, and verify the full proactive intelligence flow end-to-end.

Purpose: This plan completes the UX loop. Plan 01 built persistence, Plan 02 built generation, Plan 03 built the strip. This plan adds the detail view when users click insight badges -- showing full insight cards with dismiss/snooze actions (per D-08), stale capture triage actions (per D-04), and a visual checkpoint to verify the complete flow.

Output: InsightCard component, InsightTriage component, updated badge expansion, end-to-end verification.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-proactive-intelligence/37-CONTEXT.md
@.planning/phases/37-proactive-intelligence/37-RESEARCH.md
@.planning/phases/37-proactive-intelligence/37-01-SUMMARY.md
@.planning/phases/37-proactive-intelligence/37-02-SUMMARY.md
@.planning/phases/37-proactive-intelligence/37-03-SUMMARY.md

@packages/web/src/components/whats-new/insight-badges.tsx
@packages/web/src/components/departure-board/findings-panel.tsx
@packages/web/src/hooks/use-insights.ts
@packages/web/src/App.tsx

<interfaces>
<!-- From Plan 03 outputs -->

From packages/web/src/hooks/use-insights.ts:
```typescript
export interface Insight {
  id: string;
  type: "stale_capture" | "activity_gap" | "session_pattern" | "cross_project";
  title: string;
  body: string;
  metadata: string | null;
  projectSlug: string | null;
  createdAt: string;
}

export function useInsights(): {
  insights: Insight[];
  loading: boolean;
  dismissInsight: (id: string) => void;
  snoozeInsight: (id: string, hours?: number) => void;
}
```

From packages/web/src/components/whats-new/insight-badges.tsx:
```typescript
interface InsightBadgesProps {
  insights: Insight[];
  onDismiss: (id: string) => void;
  onSnooze: (id: string) => void;
}
```

From packages/web/src/components/departure-board/findings-panel.tsx (pattern to follow):
```typescript
// Inline expandable panel with border-l-2 accent, text-[11px], severity colors
export function FindingsPanel({ slug, expanded }: FindingsPanelProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insight detail cards and stale capture triage</name>
  <files>
    packages/web/src/components/insights/insight-card.tsx,
    packages/web/src/components/insights/insight-triage.tsx,
    packages/web/src/components/whats-new/insight-badges.tsx
  </files>
  <read_first>
    packages/web/src/components/whats-new/insight-badges.tsx,
    packages/web/src/components/departure-board/findings-panel.tsx,
    packages/web/src/components/whats-new/discovery-popover.tsx,
    packages/web/src/hooks/use-insights.ts,
    packages/web/src/lib/health-colors.ts,
    packages/web/src/lib/time.ts
  </read_first>
  <action>
    1. **Create `packages/web/src/components/insights/insight-card.tsx`**:
       - Props: `{ insight: Insight; onDismiss: (id: string) => void; onSnooze: (id: string) => void }`
       - Layout follows the FindingsPanel/DigestSection border-l-2 accent pattern
       - Color per type:
         - stale_capture: amber border-l, amber/10 bg
         - activity_gap: purple border-l, purple/10 bg
         - session_pattern: blue border-l, blue/10 bg
         - cross_project: green border-l, green/10 bg
       - Content: title (font-semibold text-[12px]), body (text-[11px] text-text-secondary)
       - If metadata is not null: parse JSON and render additional context
         - For cross_project: show shared terms as small pills
         - For activity_gap: show project slug badge
       - Action buttons row at bottom:
         - "Dismiss" button (X icon, text-[10px]) -- calls onDismiss(insight.id)
         - "Snooze 24h" button (clock icon, text-[10px]) -- calls onSnooze(insight.id)
         - For stale_capture type: additional "Triage" button that opens triage flow
       - Relative time: "3h ago" next to title (use formatRelativeTime from lib/time.ts)
       - Compact: entire card should be ~60-80px tall

    2. **Create `packages/web/src/components/insights/insight-triage.tsx`**:
       - Purpose: Quick triage actions for stale captures (per D-04)
       - This is a lightweight inline component, not a modal (per Claude's Discretion on triage UX)
       - Props: `{ onClose: () => void }`
       - Render a link/button to open the existing TriageView or navigate to captures view
       - Text: "Review {N} unassigned captures" with a right-arrow
       - The existing TriageView handles the actual triage actions
       - This just bridges from the insight card to the existing triage UI

    3. **Update `packages/web/src/components/whats-new/insight-badges.tsx`**:
       - Add expandable behavior: clicking a badge type toggles an expanded section below
       - State: `expandedType: InsightType | null`
       - When expanded, render InsightCard for each insight of that type
       - Use the same expand/collapse `transition-all duration-200 max-h-0 -> max-h-96` pattern from FindingsPanel
       - Import InsightCard from `../insights/insight-card.js`
       - Keep badges inline in the strip, expansion happens below the strip
  </action>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm typecheck && pnpm --filter @mission-control/web build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "InsightCard" packages/web/src/components/insights/insight-card.tsx returns 0
    - grep -q "onDismiss" packages/web/src/components/insights/insight-card.tsx returns 0
    - grep -q "onSnooze" packages/web/src/components/insights/insight-card.tsx returns 0
    - grep -q "InsightTriage" packages/web/src/components/insights/insight-triage.tsx returns 0
    - grep -q "expandedType" packages/web/src/components/whats-new/insight-badges.tsx returns 0
    - grep -q "InsightCard" packages/web/src/components/whats-new/insight-badges.tsx returns 0
  </acceptance_criteria>
  <done>
    Insight cards render with type-appropriate colors, dismiss/snooze actions, metadata display. Badges expand to show detail cards. Stale capture insights bridge to existing triage. Web build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete proactive intelligence flow</name>
  <files>none</files>
  <action>
    Run the full proactive intelligence system end-to-end:
    1. Start dev servers with `pnpm dev`
    2. Verify intelligence strip renders correctly at http://localhost:5173
    3. Run `pnpm test` to confirm all tests pass
    4. Run `pnpm typecheck` to confirm no type errors
    Present results to user for visual verification.
  </action>
  <what-built>
    Complete proactive intelligence system:
    1. Insights table with content-hash dedup (Plan 01)
    2. Four pattern detectors running on daemon schedule (Plan 02)
    3. Intelligence strip with morning digest view (Plan 03)
    4. Insight detail cards with dismiss/snooze (Plan 04 Task 1)
  </what-built>
  <how-to-verify>
    1. Start the dev servers: `pnpm dev` from project root
    2. Open http://localhost:5173 in browser
    3. Check the intelligence strip area (where "What's New" was):
       - If digest exists: should show digest content with "AI" badge and "Got it" button
       - Click "Got it" -- should fade to standard What's New content
       - If no digest: should show standard What's New content
    4. Check for insight badges:
       - If insights exist: colored pills (stale captures, activity gaps, session patterns, shared patterns)
       - Click a badge: should expand to show detail cards below
       - Click "Dismiss" on a card: should disappear immediately
       - Click "Snooze 24h": should disappear
    5. Verify the standalone DailyDigestPanel is gone from the layout (no duplicate digest display)
    6. Run full test suite: `pnpm test` -- all tests pass
    7. Verify `pnpm typecheck` passes
  </how-to-verify>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm test && pnpm typecheck</automated>
  </verify>
  <done>User confirms dashboard intelligence strip, insight badges, dismiss/snooze flow, and overall visual quality.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test` -- all tests pass (API + web)
- `pnpm typecheck` -- no type errors
- `pnpm --filter @mission-control/web build` -- build succeeds
- Intelligence strip renders digest in morning, fades to What's New
- Insight badges expand to show cards
- Dismiss/snooze actions work optimistically
- No duplicate digest display
</verification>

<success_criteria>
- Insight cards render with appropriate visual treatment per type
- Dismiss persists and insight doesn't return
- Snooze hides insight for specified duration
- Stale capture insights link to existing triage flow
- Activity patterns, session patterns, cross-project patterns all visible
- All proactive intelligence generated locally (LM Studio + rule-based, no external API)
- Dashboard visually verified by user
</success_criteria>

<output>
After completion, create `.planning/phases/37-proactive-intelligence/37-04-SUMMARY.md`
</output>
