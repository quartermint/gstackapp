---
phase: 37-proactive-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - packages/web/src/components/whats-new/whats-new-strip.tsx
  - packages/web/src/components/whats-new/digest-strip-view.tsx
  - packages/web/src/components/whats-new/insight-badges.tsx
  - packages/web/src/hooks/use-insights.ts
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - PROACT-01

must_haves:
  truths:
    - "Morning dashboard load shows AI-generated digest in the intelligence strip (same position as What's New)"
    - "After reading digest (clicking 'Got it' or similar), strip fades to regular What's New content"
    - "When no digest exists, strip shows standard What's New content (discoveries, stars, changed)"
    - "DailyDigestPanel standalone component removed from dashboard layout"
    - "Insight badges appear in the strip between digest and What's New content"
  artifacts:
    - path: "packages/web/src/components/whats-new/whats-new-strip.tsx"
      provides: "Evolved intelligence strip with digest + insights + whats-new"
      contains: "digest"
    - path: "packages/web/src/components/whats-new/digest-strip-view.tsx"
      provides: "Inline digest view for the intelligence strip"
      contains: "DigestStripView"
    - path: "packages/web/src/components/whats-new/insight-badges.tsx"
      provides: "Compact insight badge indicators"
      contains: "InsightBadges"
    - path: "packages/web/src/hooks/use-insights.ts"
      provides: "Hook for fetching and managing insights"
      exports: ["useInsights"]
    - path: "packages/web/src/App.tsx"
      provides: "Dashboard layout with intelligence strip replacing digest panel"
      contains: "useInsights"
  key_links:
    - from: "packages/web/src/hooks/use-insights.ts"
      to: "/api/intelligence/insights"
      via: "fetch call"
      pattern: "intelligence/insights"
    - from: "packages/web/src/components/whats-new/whats-new-strip.tsx"
      to: "packages/web/src/hooks/use-digest.ts"
      via: "digest prop passed from App.tsx"
      pattern: "digest"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/whats-new/whats-new-strip.tsx"
      via: "IntelligenceStrip render with digest + insights props"
      pattern: "WhatsNewStrip|digest|insights"
---

<objective>
Evolve the What's New strip into an intelligence strip that shows morning digest and insight badges.

Purpose: Per D-01, the What's New top strip becomes an "intelligence strip." Morning view shows AI-generated digest; after reading, fades to regular What's New content. Same real estate, smarter content. Per D-02, digest comes from the existing intelligence cache. Per D-03, content prioritized by actionability. The standalone DailyDigestPanel is absorbed into this strip, removing duplicate display.

Output: Evolved WhatsNewStrip component, DigestStripView sub-component, InsightBadges sub-component, useInsights hook, updated App.tsx layout.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-proactive-intelligence/37-CONTEXT.md
@.planning/phases/37-proactive-intelligence/37-RESEARCH.md
@.planning/phases/37-proactive-intelligence/37-01-SUMMARY.md

@packages/web/src/App.tsx
@packages/web/src/components/whats-new/whats-new-strip.tsx
@packages/web/src/components/digest/daily-digest.tsx
@packages/web/src/hooks/use-digest.ts

<interfaces>
<!-- Existing hooks and types the executor needs -->

From packages/web/src/hooks/use-digest.ts:
```typescript
export interface DailyDigest {
  summary: string;
  sections: DigestSection[];
  actionItems: string[];
  projectHighlights: ProjectHighlight[];
  generatedAt: string;
}
export function useDigest(): { digest: DailyDigest | null; loading: boolean; }
```

From packages/web/src/components/whats-new/whats-new-strip.tsx:
```typescript
interface WhatsNewStripProps {
  discoveries: DiscoveryItem[];
  stars: StarItem[];
  onPromote: (id: string) => void;
  onDismiss: (id: string) => void;
  onUpdateStarIntent: (githubId: number, intent: string) => void;
  changedCount?: number;
}
```

From Plan 01 API (packages/api/src/routes/intelligence.ts):
```
GET /intelligence/insights -> { insights: Insight[] }
POST /intelligence/insights/:id/dismiss -> { ok: true }
POST /intelligence/insights/:id/snooze -> { ok: true }
```

Insight shape from API:
```typescript
interface Insight {
  id: string;
  type: "stale_capture" | "activity_gap" | "session_pattern" | "cross_project";
  title: string;
  body: string;
  metadata: string | null;
  projectSlug: string | null;
  createdAt: string; // ISO
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useInsights hook and insight UI types</name>
  <files>
    packages/web/src/hooks/use-insights.ts
  </files>
  <read_first>
    packages/web/src/hooks/use-digest.ts,
    packages/web/src/hooks/use-discoveries.ts,
    packages/web/src/App.tsx
  </read_first>
  <action>
    Create `packages/web/src/hooks/use-insights.ts` following the exact `useDigest` hook pattern (fetchCounter convention, NOT TanStack Query):

    1. Define the `Insight` type matching API response:
       ```typescript
       export interface Insight {
         id: string;
         type: "stale_capture" | "activity_gap" | "session_pattern" | "cross_project";
         title: string;
         body: string;
         metadata: string | null;
         projectSlug: string | null;
         createdAt: string;
       }
       ```

    2. `useInsights()` hook:
       - State: `insights: Insight[]`, `loading: boolean`, `dismissedIds: Set<string>` (optimistic, follows seenSlugs pattern per D-08)
       - Fetch from `/api/intelligence/insights` on mount
       - Re-fetch every 5 minutes (same interval as useDigest)
       - SSE: listen for `intelligence:insight_created` and `intelligence:insight_dismissed` events to trigger refetch (use `useSSE` or manual EventSource per existing pattern)
       - `dismissInsight(id)`: add to dismissedIds optimistically, POST to `/api/intelligence/insights/${id}/dismiss`
       - `snoozeInsight(id, hours = 24)`: add to dismissedIds optimistically, POST to `/api/intelligence/insights/${id}/snooze` with `{ hours }`
       - `activeInsights`: filter insights to exclude dismissedIds
       - Return `{ insights: activeInsights, loading, dismissInsight, snoozeInsight }`

    3. Export types and hook.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useInsights" packages/web/src/hooks/use-insights.ts returns 0
    - grep -q "dismissInsight" packages/web/src/hooks/use-insights.ts returns 0
    - grep -q "snoozeInsight" packages/web/src/hooks/use-insights.ts returns 0
    - grep -q "intelligence/insights" packages/web/src/hooks/use-insights.ts returns 0
    - grep -q "dismissedIds" packages/web/src/hooks/use-insights.ts returns 0
  </acceptance_criteria>
  <done>
    useInsights hook fetches from API, supports optimistic dismiss/snooze, auto-refetches, types exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Intelligence strip evolution with digest view and insight badges</name>
  <files>
    packages/web/src/components/whats-new/whats-new-strip.tsx,
    packages/web/src/components/whats-new/digest-strip-view.tsx,
    packages/web/src/components/whats-new/insight-badges.tsx,
    packages/web/src/App.tsx
  </files>
  <read_first>
    packages/web/src/components/whats-new/whats-new-strip.tsx,
    packages/web/src/components/digest/daily-digest.tsx,
    packages/web/src/hooks/use-insights.ts,
    packages/web/src/hooks/use-digest.ts,
    packages/web/src/App.tsx
  </read_first>
  <action>
    1. **Create `packages/web/src/components/whats-new/digest-strip-view.tsx`** (per D-01):
       A compact inline digest view for the intelligence strip (NOT the full DailyDigestPanel card). This is a condensed newspaper-headline view:
       - Props: `{ digest: DailyDigest; onRead: () => void }`
       - Render: the digest summary as a single prominent line, sections as compact pills/badges
       - "Got it" button to dismiss (calls onRead)
       - Use the same `text-[11px]` sizing as WhatsNewStrip badges
       - AI badge: small "AI" pill in terracotta (same as DailyDigestPanel header)
       - Compact action items list (first 3 only, collapsible)
       - Keep it inline-ish -- this replaces the strip, not a card below it
       - Use `transition-all duration-300` for smooth fade when dismissed

    2. **Create `packages/web/src/components/whats-new/insight-badges.tsx`**:
       - Props: `{ insights: Insight[]; onDismiss: (id: string) => void; onSnooze: (id: string) => void }`
       - Render insight count badges by type, similar to existing discovery/star badges:
         - stale_capture: amber color, "[N] stale captures" pill
         - activity_gap: purple/indigo, "[N] activity gaps" pill
         - session_pattern: blue, "session pattern" pill
         - cross_project: green, "shared pattern" pill
       - Each badge is a button that could expand to show details (or a popover like discovery-popover)
       - For now: simple badge with tooltip showing the insight title on hover
       - Each badge has a small X or dismiss action per D-08
       - Use same badge styling as existing WhatsNewStrip badges (`px-2 py-0.5 rounded-full text-[10px] font-semibold`)

    3. **Evolve `packages/web/src/components/whats-new/whats-new-strip.tsx`** (per D-01):
       - Extend props to accept: `digest: DailyDigest | null`, `digestLoading: boolean`, `insights: Insight[]`, `onDigestRead: () => void`, `onInsightDismiss: (id: string) => void`, `onInsightSnooze: (id: string) => void`
       - State: `digestRead: boolean` (local state, starts false)
       - Logic per D-01:
         ```
         if (digest && !digestRead) → render DigestStripView
         else → render standard What's New content (existing behavior)
         ```
       - Between digest/standard content and discovery badges, show InsightBadges if insights exist
       - The label changes: when showing digest, label is "Intelligence" instead of "What's New"
       - Empty state: strip visible if digest OR insights OR discoveries OR stars OR changedCount > 0 (expand existing condition)
       - Preserve all existing discovery popover and star popover functionality exactly

    4. **Update `packages/web/src/App.tsx`**:
       - Import `useInsights` from hooks
       - Call `useInsights()` to get `{ insights, loading: insightsLoading, dismissInsight, snoozeInsight }`
       - Add `digestRead` state: `const [digestRead, setDigestRead] = useState(false)`
       - Pass new props to WhatsNewStrip: `digest={digest}`, `digestLoading={digestLoading}`, `insights={insights}`, `onDigestRead={() => setDigestRead(true)}`, `onInsightDismiss={dismissInsight}`, `onInsightSnooze={snoozeInsight}`
       - **Remove the standalone DailyDigestPanel section** (lines ~315-320 that render `<DailyDigestPanel digest={digest} loading={digestLoading} />`). The intelligence strip now absorbs this per Research anti-pattern note.
       - Keep the `useDigest()` call in App.tsx (it feeds the strip now)
       - Do NOT remove the `DailyDigestPanel` component file itself (dead code removal is a separate concern) but remove its import from App.tsx if no longer used
  </action>
  <verify>
    <automated>cd /Users/ryanstern/mission-control && pnpm typecheck && pnpm --filter @mission-control/web build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "digest" packages/web/src/components/whats-new/whats-new-strip.tsx returns 0
    - grep -q "DigestStripView" packages/web/src/components/whats-new/digest-strip-view.tsx returns 0
    - grep -q "InsightBadges" packages/web/src/components/whats-new/insight-badges.tsx returns 0
    - grep -q "useInsights" packages/web/src/App.tsx returns 0
    - grep -q "DailyDigestPanel" packages/web/src/App.tsx returns 1 (should NOT be found -- removed)
    - grep -q "Intelligence" packages/web/src/components/whats-new/whats-new-strip.tsx returns 0
  </acceptance_criteria>
  <done>
    Intelligence strip shows digest on morning load, fades to What's New after reading. Insight badges visible. DailyDigestPanel removed from dashboard layout. Web build succeeds.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm --filter @mission-control/web build` succeeds
- WhatsNewStrip accepts digest and insight props
- DailyDigestPanel no longer rendered in App.tsx
- useInsights hook connects to API endpoints
</verification>

<success_criteria>
- Morning dashboard shows digest in intelligence strip (same position as What's New)
- After clicking "Got it", strip shows standard What's New content
- Insight badges appear as compact pills in the strip
- DailyDigestPanel standalone rendering removed (single source of digest display per D-01)
- All types compile, web build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/37-proactive-intelligence/37-03-SUMMARY.md`
</output>
