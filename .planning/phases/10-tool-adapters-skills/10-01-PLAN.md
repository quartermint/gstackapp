---
phase: 10-tool-adapters-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/harness/src/adapters/types.ts
  - packages/harness/src/adapters/claude-code.ts
  - packages/harness/src/adapters/opencode.ts
  - packages/harness/src/adapters/codex.ts
  - packages/harness/src/adapters/index.ts
  - packages/harness/src/skills/manifest.ts
  - packages/harness/src/skills/registry.ts
  - packages/harness/src/skills/index.ts
  - packages/harness/src/__tests__/adapters.test.ts
  - packages/harness/src/__tests__/manifest.test.ts
  - packages/harness/src/__tests__/registry.test.ts
  - packages/harness/src/__tests__/fixtures/valid.skill.json
  - packages/harness/src/__tests__/fixtures/invalid.skill.json
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - ADPT-03
  - ADPT-04

must_haves:
  truths:
    - "Tool adapter maps canonical tool names (Read, Write, Edit, Bash, Grep, Glob) to harness-specific equivalents for all 3 adapters"
    - "SkillManifest Zod schema validates valid .skill.json and rejects invalid ones"
    - "Skill registry discovers and loads manifests from local directories"
    - "Skill registry loads manifests from remote HTTPS URLs"
  artifacts:
    - path: "packages/harness/src/adapters/types.ts"
      provides: "ToolAdapter interface"
      exports: ["ToolAdapter"]
    - path: "packages/harness/src/adapters/index.ts"
      provides: "Barrel export + getAdapter factory"
      exports: ["getAdapter", "claudeCodeAdapter", "openCodeAdapter", "codexAdapter"]
    - path: "packages/harness/src/skills/manifest.ts"
      provides: "SkillManifest Zod schema"
      exports: ["SkillManifestSchema", "SkillManifest"]
    - path: "packages/harness/src/skills/registry.ts"
      provides: "SkillRegistry class with local + remote loading"
      exports: ["SkillRegistry"]
  key_links:
    - from: "packages/harness/src/adapters/claude-code.ts"
      to: "packages/harness/src/adapters/types.ts"
      via: "implements ToolAdapter"
      pattern: "ToolAdapter"
    - from: "packages/harness/src/skills/registry.ts"
      to: "packages/harness/src/skills/manifest.ts"
      via: "validates manifests with SkillManifestSchema.parse()"
      pattern: "SkillManifestSchema\\.parse"
---

<objective>
Create the tool adapter interface with 3 built-in adapters, the SkillManifest Zod schema, and the skill registry with local directory + remote URL loading.

Purpose: Establishes the portable skill contract layer -- adapters normalize tools across harnesses, manifests define skills, registry discovers them. This is the foundation the skill runner (Plan 02) executes against.

Output: Adapter types + 3 implementations, SkillManifest schema, SkillRegistry class, full test coverage for all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tool-adapters-skills/10-CONTEXT.md
@.planning/phases/10-tool-adapters-skills/10-RESEARCH.md

@packages/harness/src/types.ts
@packages/harness/src/index.ts
@packages/harness/package.json

<interfaces>
<!-- Key types the executor needs from the existing harness package -->

From packages/harness/src/types.ts:
```typescript
export interface ToolDefinition {
  name: string
  description: string
  inputSchema: Record<string, unknown>  // JSON Schema object
}

export interface LLMProvider {
  readonly name: string
  createCompletion(params: CompletionParams): Promise<CompletionResult>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolAdapter interface + 3 adapters + SkillManifest schema</name>
  <files>
    packages/harness/src/adapters/types.ts
    packages/harness/src/adapters/claude-code.ts
    packages/harness/src/adapters/opencode.ts
    packages/harness/src/adapters/codex.ts
    packages/harness/src/adapters/index.ts
    packages/harness/src/skills/manifest.ts
    packages/harness/src/__tests__/adapters.test.ts
    packages/harness/src/__tests__/manifest.test.ts
  </files>
  <read_first>
    packages/harness/src/types.ts
    packages/harness/src/index.ts
    .planning/phases/10-tool-adapters-skills/10-RESEARCH.md
  </read_first>
  <behavior>
    - claude-code adapter: mapToolName('Read') returns 'Read' (identity), mapToolSchema returns unchanged ToolDefinition, mapToolResult returns unchanged string
    - opencode adapter: mapToolName('Read') returns 'read', mapToolName('Write') returns 'write', mapToolName('Edit') returns 'edit', mapToolName('Bash') returns 'bash', mapToolName('Grep') returns 'grep', mapToolName('Glob') returns 'glob'
    - opencode adapter: mapToolName('UnknownTool') throws Error with message containing "unknown canonical tool"
    - codex adapter: mapToolName('Read') returns 'shell', mapToolName('Bash') returns 'shell', mapToolName('Grep') returns 'shell', mapToolName('Glob') returns 'shell', mapToolName('Write') returns 'apply_patch', mapToolName('Edit') returns 'apply_patch'
    - codex adapter: mapToolSchema for 'Read' transforms inputSchema to shell format with cmd array pattern
    - getAdapter('claude-code') returns claude-code adapter, getAdapter('opencode') returns opencode adapter, getAdapter('codex') returns codex adapter, getAdapter('unknown') throws
    - SkillManifestSchema.parse(validManifest) succeeds with all required fields (id, name, version, tools, prompt, outputSchema)
    - SkillManifestSchema.parse(validManifest) succeeds with optional fields (minimumModel, capabilities, description, author, license)
    - SkillManifestSchema.parse({}) throws ZodError (missing required fields)
    - SkillManifestSchema.parse({...validManifest, version: 'bad'}) throws ZodError (invalid semver)
    - SkillManifestSchema.parse({...validManifest, tools: []}) throws ZodError (min 1 tool)
    - SkillManifestSchema.parse({...validManifest, tools: ['InvalidTool']}) throws ZodError (not a canonical tool)
  </behavior>
  <action>
    Create the ToolAdapter interface per D-03 in packages/harness/src/adapters/types.ts. Import ToolDefinition from '../types'.

    The ToolAdapter interface has 4 members:
    - readonly name: string
    - mapToolName(canonical: string): string
    - mapToolSchema(canonical: ToolDefinition): ToolDefinition
    - mapToolResult(toolName: string, result: string): string

    Create 3 adapter implementations per D-01, D-02, D-04:

    claude-code.ts: Identity adapter -- all methods return inputs unchanged. Name is 'claude-code'.

    opencode.ts: Static NAME_MAP lookup (Read->read, Write->write, Edit->edit, Bash->bash, Grep->grep, Glob->glob). mapToolName uses lookup, throws on unknown. mapToolSchema clones definition with mapped name. mapToolResult returns unchanged. Name is 'opencode'.

    codex.ts: Maps Read/Bash/Grep/Glob to 'shell', Write/Edit to 'apply_patch'. mapToolSchema transforms inputSchema: for Read, wraps in shell format {"cmd": ["cat", filePath]}. For Bash, wraps in shell format {"cmd": [...]}. For Write/Edit, transforms to apply_patch format. mapToolResult returns unchanged. Name is 'codex'.

    index.ts: Barrel export. Export all 3 adapter constants + getAdapter(name: string): ToolAdapter factory that looks up by name string, throws on unknown.

    Create SkillManifest Zod schema per D-05, D-06, D-07 in packages/harness/src/skills/manifest.ts:
    - CanonicalToolSchema = z.enum(['Read', 'Write', 'Edit', 'Bash', 'Grep', 'Glob'])
    - ModelTierSchema = z.enum(['opus', 'sonnet', 'haiku'])
    - CapabilitySchema = z.enum(['tool_use', 'vision', 'long_context'])
    - SkillManifestSchema = z.object with required: id (string.min(1)), name (string.min(1)), version (string.regex semver), tools (array(CanonicalToolSchema).min(1)), prompt (string.min(1)), outputSchema (z.record(z.unknown()))
    - Optional: minimumModel, capabilities, description, author, license
    - Export SkillManifest type via z.infer

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp/packages/harness && npx vitest run src/__tests__/adapters.test.ts src/__tests__/manifest.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export interface ToolAdapter" packages/harness/src/adapters/types.ts
    - grep -q "mapToolName" packages/harness/src/adapters/types.ts
    - grep -q "mapToolSchema" packages/harness/src/adapters/types.ts
    - grep -q "mapToolResult" packages/harness/src/adapters/types.ts
    - grep -q "name: 'claude-code'" packages/harness/src/adapters/claude-code.ts
    - grep -q "name: 'opencode'" packages/harness/src/adapters/opencode.ts
    - grep -q "name: 'codex'" packages/harness/src/adapters/codex.ts
    - grep -q "export function getAdapter" packages/harness/src/adapters/index.ts
    - grep -q "SkillManifestSchema" packages/harness/src/skills/manifest.ts
    - grep -q "CanonicalToolSchema" packages/harness/src/skills/manifest.ts
    - grep -q "export type SkillManifest" packages/harness/src/skills/manifest.ts
  </acceptance_criteria>
  <done>
    All 3 adapters implement ToolAdapter with correct name mappings. getAdapter factory resolves by name. SkillManifest Zod schema validates required/optional fields, rejects invalid inputs. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SkillRegistry with local directory + remote URL loading</name>
  <files>
    packages/harness/src/skills/registry.ts
    packages/harness/src/skills/index.ts
    packages/harness/src/__tests__/registry.test.ts
    packages/harness/src/__tests__/fixtures/valid.skill.json
    packages/harness/src/__tests__/fixtures/invalid.skill.json
  </files>
  <read_first>
    packages/harness/src/skills/manifest.ts
    packages/harness/src/adapters/types.ts
    .planning/phases/10-tool-adapters-skills/10-CONTEXT.md
  </read_first>
  <behavior>
    - loadFromDirectory with valid .skill.json files populates registry map, registry.list() returns loaded manifests
    - loadFromDirectory with nonexistent directory returns silently (no error)
    - loadFromDirectory skips invalid .skill.json files with console.warn, loads valid ones
    - loadFromDirectory ignores non-.skill.json files
    - loadFromUrl fetches HTTPS URL, parses JSON, validates with SkillManifestSchema, adds to registry
    - loadFromUrl throws on non-HTTPS URLs
    - loadFromUrl throws on HTTP error responses
    - registry.get(id) returns manifest by id, returns undefined for unknown id
    - registry.list() returns all loaded manifests
  </behavior>
  <action>
    Create test fixtures first:

    packages/harness/src/__tests__/fixtures/valid.skill.json:
    ```json
    {
      "id": "test-skill",
      "name": "Test Skill",
      "version": "1.0.0",
      "tools": ["Read", "Bash"],
      "prompt": "You are a test skill.",
      "outputSchema": { "type": "object", "properties": { "result": { "type": "string" } } }
    }
    ```

    packages/harness/src/__tests__/fixtures/invalid.skill.json:
    ```json
    { "id": "", "name": "" }
    ```

    Create SkillRegistry class per D-08, D-09, D-10 in packages/harness/src/skills/registry.ts:
    - Private field: skills = new Map<string, SkillManifest>()
    - loadFromDirectory(dir?: string): void -- defaults to join(homedir(), '.gstackapp', 'skills'). Uses readdirSync with {recursive: true}. Filters *.skill.json. Reads each, JSON.parse, SkillManifestSchema.parse, adds to map. Catches errors per-file (console.warn, continue). Returns silently if directory doesn't exist.
    - async loadFromUrl(url: string): Promise<void> -- validates url starts with 'https://'. fetch, response.json, SkillManifestSchema.parse, adds to map.
    - get(id: string): SkillManifest | undefined
    - list(): SkillManifest[]

    Create packages/harness/src/skills/index.ts barrel export:
    - Export { SkillManifestSchema, type SkillManifest } from './manifest'
    - Export { SkillRegistry } from './registry'

    Write tests FIRST using temp directories (node:os tmpdir + node:fs mkdtempSync) for local discovery tests. Mock fetch for remote URL tests using vi.stubGlobal.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp/packages/harness && npx vitest run src/__tests__/registry.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class SkillRegistry" packages/harness/src/skills/registry.ts
    - grep -q "loadFromDirectory" packages/harness/src/skills/registry.ts
    - grep -q "loadFromUrl" packages/harness/src/skills/registry.ts
    - grep -q "https://" packages/harness/src/skills/registry.ts
    - grep -q "SkillManifestSchema" packages/harness/src/skills/registry.ts
    - grep -q "SkillRegistry" packages/harness/src/skills/index.ts
    - grep -q "SkillManifestSchema" packages/harness/src/skills/index.ts
    - test -f packages/harness/src/__tests__/fixtures/valid.skill.json
    - test -f packages/harness/src/__tests__/fixtures/invalid.skill.json
  </acceptance_criteria>
  <done>
    SkillRegistry discovers .skill.json from directories, loads from HTTPS URLs, validates all manifests via Zod, and handles errors gracefully. All registry tests pass.
  </done>
</task>

</tasks>

<verification>
cd /Users/ryanstern/gstackapp/packages/harness && npx vitest run src/__tests__/adapters.test.ts src/__tests__/manifest.test.ts src/__tests__/registry.test.ts --reporter=verbose
</verification>

<success_criteria>
- All 3 tool adapters map canonical tool names correctly (ADPT-01)
- SkillManifest Zod schema validates/rejects .skill.json files (ADPT-02)
- Registry loads from local directories (ADPT-03)
- Registry loads from remote HTTPS URLs (ADPT-04)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-tool-adapters-skills/10-01-SUMMARY.md`
</output>
