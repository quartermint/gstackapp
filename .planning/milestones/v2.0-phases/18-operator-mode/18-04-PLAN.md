---
phase: 18-operator-mode
plan: 04
type: execute
wave: 3
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - packages/api/src/__tests__/operator-audit.test.ts
  - packages/api/src/__tests__/operator-integration.test.ts
autonomous: false
requirements: [OP-07]
must_haves:
  truths:
    - "Every operator action (submit, clarify, approve, gate response, escalate) creates an audit trail entry"
    - "Audit trail entries have correct action types and timestamps"
    - "Full flow from intake to completion produces audit entries for each step"
    - "All API tests pass as a suite"
  artifacts:
    - path: "packages/api/src/__tests__/operator-audit.test.ts"
      provides: "Tests verifying audit trail entries for all action types"
    - path: "packages/api/src/__tests__/operator-integration.test.ts"
      provides: "Integration tests covering the full clarification-to-completion flow"
  key_links:
    - from: "packages/api/src/routes/operator.ts"
      to: "packages/api/src/db/schema.ts (auditTrail)"
      via: "db.insert(auditTrail) calls in every route handler"
      pattern: "db\\.insert\\(auditTrail\\)"
---

<objective>
Verify the full operator mode integration — audit trail completeness, end-to-end flow tests, and human visual verification.

Purpose: Ensure all Phase 18 components work together correctly. The audit trail (OP-07, D-07) gets dedicated testing, and the full flow is verified end-to-end. Human checkpoint confirms the visual experience matches the design contract.

Output: Audit trail tests, integration tests, and human verification of the complete operator flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-operator-mode/18-CONTEXT.md
@.planning/phases/18-operator-mode/18-01-SUMMARY.md
@.planning/phases/18-operator-mode/18-02-SUMMARY.md
@.planning/phases/18-operator-mode/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit trail tests + integration tests</name>
  <files>packages/api/src/__tests__/operator-audit.test.ts, packages/api/src/__tests__/operator-integration.test.ts</files>
  <read_first>
    - packages/api/src/routes/operator.ts
    - packages/api/src/db/schema.ts
    - packages/api/src/pipeline/state-machine.ts
    - packages/api/src/__tests__/operator-request.test.ts (existing test patterns)
  </read_first>
  <behavior>
    - POST /request creates audit entry with action='request_submitted'
    - POST /request creates audit entry with action='clarification_question' (after question generated)
    - POST /:requestId/clarify-answer creates audit entry with action='clarification_answer'
    - POST /:requestId/approve-brief creates audit entries for 'brief_approved' and 'pipeline_spawned'
    - POST /:requestId/reject-brief creates audit entry with action='brief_rejected'
    - POST /:requestId/escalate creates audit entry with action='escalated_to_ryan'
    - POST /:requestId/gate-response creates audit entry with action='gate_response'
    - Full flow: submit -> clarify x2 -> approve -> complete produces 7+ audit entries in correct chronological order
    - Each audit entry has a non-null createdAt timestamp
    - Each audit entry has the correct requestId
    - Each audit entry has the correct userId
  </behavior>
  <action>
Create `packages/api/src/__tests__/operator-audit.test.ts`:

Test each API route to verify it creates the correct audit trail entry. Mock the Claude API (clarifier, brief-generator) and pipeline spawner. After each route call, query the auditTrail table and assert:
- `action` field matches expected value
- `requestId` matches the request
- `userId` matches the authenticated user
- `detail` JSON contains relevant context
- `createdAt` is a valid timestamp

Audit action types to test:
- `request_submitted` — from POST /request
- `clarification_question` — from POST /request (initial question)
- `clarification_answer` — from POST /:id/clarify-answer
- `brief_generated` — when brief is created
- `brief_approved` — from POST /:id/approve-brief
- `brief_rejected` — from POST /:id/reject-brief
- `pipeline_spawned` — from POST /:id/approve-brief after approval
- `escalated_to_ryan` — from POST /:id/escalate
- `gate_response` — from POST /:id/gate-response

Create `packages/api/src/__tests__/operator-integration.test.ts`:

End-to-end flow test (with mocked Claude API and spawner):
1. POST /request with valid intake data
2. Verify status is 'clarifying' and first question is generated
3. POST /:id/clarify-answer with answer
4. Verify next question generated (or brief generated if isComplete)
5. Mock the clarifier to return isComplete after 2 questions
6. Verify brief is generated and status transitions to 'briefing'
7. POST /:id/approve-brief
8. Verify pipeline spawned and status is 'running'
9. Verify all audit trail entries exist in correct order

Test error paths:
- Submit answer when status is not 'clarifying' -> 400 error
- Approve brief when status is not 'briefing' -> 400 error
- Escalate from 'complete' status -> 400 error (invalid transition)
- Cross-user access attempt -> 403 error

Run the full API test suite to verify nothing is broken:
```bash
cd packages/api && npx vitest run --reporter=dot
```
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp/packages/api && npx vitest run src/__tests__/operator-audit.test.ts src/__tests__/operator-integration.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - packages/api/src/__tests__/operator-audit.test.ts contains at least 8 test cases (one per audit action type)
    - packages/api/src/__tests__/operator-integration.test.ts contains `POST /request`
    - packages/api/src/__tests__/operator-integration.test.ts contains `clarify-answer`
    - packages/api/src/__tests__/operator-integration.test.ts contains `approve-brief`
    - packages/api/src/__tests__/operator-integration.test.ts contains `escalate`
    - Both test files exit 0
    - Full API test suite passes: `cd packages/api && npx vitest run --reporter=dot` exits 0
  </acceptance_criteria>
  <done>Audit trail produces entries for all 9+ action types, integration tests verify full clarify-approve-complete flow and error paths, full test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete operator flow</name>
  <files>none</files>
  <action>
Human verifies the complete operator mode visual flow in the browser. All automated work is complete — this checkpoint confirms the UX matches the design contract from 18-UI-SPEC.md and 18-CONTEXT.md decisions D-01 through D-07.
  </action>
  <what-built>
Complete operator mode: chat-style clarification flow, execution brief with approval, horizontal progress bar, error cards (timeout, verification failure, ambiguous scope, provider exhaustion), verification report with expandable details, audit trail, and decision gates — all within a single-page chat thread.
  </what-built>
  <how-to-verify>
1. Start the dev servers: `cd /Users/ryanstern/gstackapp && npm run dev` (or start API + web separately)
2. Navigate to the app in browser. Log in as an operator user.
3. **Intake flow (OP-01):** Fill out "What do you need?" and "What does good look like?" fields. Click "Submit Request."
4. **Clarification (OP-02, D-01):** Verify questions appear as assistant-style chat bubbles below the intake form. Answer at least one question. Verify the answer appears as a user-style message and a new question appears.
5. **Execution brief (OP-03, D-02):** After clarification completes, verify the execution brief card appears with scope, assumptions, and acceptance criteria sections. Verify "Approve & Start" and "Request Changes" buttons are visible.
6. **Request Changes:** Click "Request Changes" — verify it goes back to clarification with a new question.
7. **Approve & Start:** Click "Approve & Start" — verify the horizontal progress bar appears below (D-03).
8. **Progress (OP-04, D-03):** Verify 5 steps (Thinking, Planning, Building, Checking, Done) shown horizontally. Active step should pulse/glow. Elapsed time should tick. Status message should update.
9. **Decision gates (OP-06, D-06):** If a gate appears, verify Approve / Request Changes / Ask Ryan buttons. Click one and verify pipeline continues.
10. **Completion (OP-05, D-05):** On completion, verify the verification report card appears with pass/fail badge and expandable sections.
11. **Audit trail (OP-07, D-07):** Click "View Activity Log" — verify timestamped entries for each step appear.
12. **Request history:** Scroll down — verify the completed request appears in the history list with correct status badge.
13. **Error states:** These are harder to trigger organically. Verify the ErrorCard component renders correctly by checking the component exists and builds.

Note: Steps 8-10 require a real Claude Code binary on the system PATH to produce pipeline output. If not available, verify up through step 7 and confirm the UI components render correctly.
  </how-to-verify>
  <verify>Human confirms visual flow matches design contract</verify>
  <done>Human approves the complete operator flow visual experience</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test isolation | Tests use mocked DB and Claude API — no real API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Information Disclosure | audit trail API | accept | Already scoped to request owner via session isolation (tested in integration tests) |
</threat_model>

<verification>
- Full API test suite: `cd packages/api && npx vitest run --reporter=dot`
- Web build: `cd packages/web && npx vite build`
- Human verification of visual flow
</verification>

<success_criteria>
- All audit trail action types produce correct entries (OP-07)
- Integration tests verify full clarify-approve-complete flow
- Full API test suite passes
- Human confirms visual flow matches design contract
</success_criteria>

<output>
After completion, create `.planning/phases/18-operator-mode/18-04-SUMMARY.md`
</output>
