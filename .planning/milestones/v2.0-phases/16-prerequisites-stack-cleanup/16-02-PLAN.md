---
phase: 16-prerequisites-stack-cleanup
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
autonomous: false
requirements: [PRE-02]
must_haves:
  truths:
    - "All 6 UAT items have been exercised in a browser"
    - "UAT results are documented with pass/fail status"
    - "Any blocking issues discovered during UAT are fixed"
  artifacts:
    - path: ".planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md"
      provides: "UAT results with pass/fail for all 6 items"
      contains: "passed"
  key_links:
    - from: "packages/api/src/routes/ideation.ts"
      to: "packages/web/src/components/ideation/"
      via: "SSE stream to React components"
      pattern: "EventSource"
---

<objective>
Execute the 6 human UAT items from Phase 15 and document results.

Purpose: PRE-02 requires all 6 UAT items to pass before v2.0 work begins. The SSE bug fix from Plan 01 unblocks UAT items 3 and 4 (autonomous execution and decision gates).

Output: Completed UAT report with pass/fail for all 6 items, plus fixes for any issues found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-prerequisites-stack-cleanup/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev servers and recover UAT checklist</name>
  <files>
    .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
  </files>
  <read_first>
    packages/api/package.json
    packages/web/package.json
  </read_first>
  <action>
  1. Recover the UAT file from git history and place it in the phase directory:
     ```bash
     git show 0c74f76:.planning/phases/15-ideation-funnel-autonomous-gsd/15-HUMAN-UAT.md > .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
     ```

  2. Start both dev servers (api + web) in background:
     ```bash
     npm run dev --workspace=@gstackapp/api &
     npm run dev --workspace=@gstackapp/web &
     ```

  3. Verify servers are healthy:
     - API: `curl http://localhost:3000/api/health` returns 200
     - Web: `curl http://localhost:5173` returns HTML

  4. Update the UAT file header to reflect Phase 16 context and reset statuses to `[testing]`.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && test -f .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md && echo "UAT file exists"</automated>
  </verify>
  <acceptance_criteria>
  - `.planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md` exists
  - Dev servers respond on their expected ports
  </acceptance_criteria>
  <done>UAT checklist recovered, dev servers running, ready for browser testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute all 6 UAT items in browser</name>
  <files>
    .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
  </read_first>
  <action>
  CHECKPOINT: Human browser testing required. Present the verification steps below to the user and wait for their results.
  </action>
  <what-built>
  Plan 01 fixed the autonomous SSE named-event bug. All existing tests pass. The ideation pipeline and autonomous execution should now stream events correctly to the frontend.
  </what-built>
  <how-to-verify>
  Open http://localhost:5173 in your browser and test each item:

  1. **Ideation pipeline visual flow** -- Submit an idea, verify the 4-node horizontal pipeline renders with pending/running/complete animation states
  2. **Pipeline completion to scaffold modal** -- After pipeline completes, verify "Launch Execution" CTA appears and scaffold modal pre-populates with ideation context
  3. **Autonomous execution visualization** -- Launch autonomous execution, verify real-time phase/commit streaming updates UI via SSE (this was broken before Plan 01 fix)
  4. **Decision gate interaction** -- During autonomous execution, verify blocking gate card renders with options, respond to it, verify it unblocks execution
  5. **Multi-tab session management** -- Open multiple tabs, verify tab strip shows active sessions with status dots, verify 10-tab cap
  6. **Repo scaffold form validation** -- Test real-time name/stack validation in scaffold form, verify filesystem write creates project directory

  For each item, report: PASS, FAIL (with description), or SKIP (with reason).
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user input"</automated>
  </verify>
  <done>User has reported results for all 6 UAT items.</done>
  <resume-signal>Report results for all 6 items. Type "all pass" if clean, or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Document UAT results and fix any issues</name>
  <files>
    .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
  </read_first>
  <action>
  Based on the human verification results from Task 2:

  1. Update `16-HUMAN-UAT.md` with pass/fail results for each of the 6 items
  2. If any items failed:
     - Diagnose the root cause
     - Implement the fix
     - Re-run relevant tests to confirm
     - Update the UAT item to "passed (after fix)"
  3. Update the summary section with final counts (passed/failed/skipped)
  4. Stop dev servers if still running
  </action>
  <verify>
    <automated>cd /Users/ryanstern/gstackapp && grep -c "passed\|PASS" .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
  - All 6 UAT items have a result recorded (no `[pending]` remaining)
  - UAT summary shows total and passed counts
  - Any fixes have passing tests
  </acceptance_criteria>
  <done>All 6 UAT items tested, results documented, any failures fixed and retested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. UAT exercises existing functionality.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | No new threats — manual testing of existing features |
</threat_model>

<verification>
1. UAT file has results for all 6 items
2. No items remain in `[pending]` status
3. Any fixes discovered during UAT have passing tests
</verification>

<success_criteria>
- All 6 human UAT items exercised in browser
- Results documented in 16-HUMAN-UAT.md
- Any blocking failures fixed and retested
- PRE-02 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-prerequisites-stack-cleanup/16-02-SUMMARY.md`
</output>
