---
phase: 16-prerequisites-stack-cleanup
plan: 04
type: execute
wave: 1
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
autonomous: true
gap_closure: true
requirements: [PRE-02]

must_haves:
  truths:
    - "All 6 UAT items in 16-HUMAN-UAT.md have a concrete pass/fail result (no deferred)"
    - "16-HUMAN-UAT.md summary shows passed count reflecting actual browser test outcomes"
    - "PRE-02 is satisfiable — either all 6 pass, or failures are diagnosed and fixed then re-tested"
  artifacts:
    - path: ".planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md"
      provides: "Completed UAT results with pass/fail for all 6 items"
      contains: "passed:"
  key_links:
    - from: "packages/api/src/index.ts"
      to: "http://localhost:3000"
      via: "Hono dev server"
      pattern: "localhost:3000"
    - from: "packages/web/src"
      to: "http://localhost:5173"
      via: "Vite dev server"
      pattern: "localhost:5173"
---

<objective>
Close PRE-02 gap: Exercise all 6 human UAT items using headless browser automation, record results, and fix any failures found.

Purpose: Phase 16 verification found that all 6 UAT items were auto-approved without browser testing. PRE-02 cannot be satisfied until each item is exercised in a real browser and results documented.

Output: Updated 16-HUMAN-UAT.md with pass/fail results for all 6 items. Any bug fixes committed if failures are found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md
@.planning/phases/16-prerequisites-stack-cleanup/16-01-SUMMARY.md
@.planning/phases/16-prerequisites-stack-cleanup/16-02-SUMMARY.md
@.planning/phases/16-prerequisites-stack-cleanup/16-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev servers and exercise all 6 UAT items via headless browser</name>
  <files>.planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md (current UAT state, all 6 items and expectations)
    - .planning/phases/16-prerequisites-stack-cleanup/16-VERIFICATION.md (gap details and test instructions)
    - packages/api/package.json (dev script)
    - packages/web/package.json (dev script)
  </read_first>
  <action>
**Step 1: Start dev servers**

Start both dev servers as background processes:

```bash
# Start API server (port 3000)
cd /Users/ryanstern/gstackapp && npm run dev --workspace=@gstackapp/api &

# Start Web server (port 5173)
cd /Users/ryanstern/gstackapp && npm run dev --workspace=@gstackapp/web &
```

Wait for both servers to be ready (API on http://localhost:3000, Web on http://localhost:5173). Verify with curl that both respond before proceeding.

**Step 2: Exercise each UAT item using the browse/Playwright headless browser tool**

For each of the 6 UAT items below, navigate to http://localhost:5173, interact with the UI, take screenshots, and record the result as PASS or FAIL with details.

**UAT Item 1: Ideation pipeline visual flow**
- Navigate to http://localhost:5173
- Find and click the ideation/ideas entry point in the UI
- Submit an idea (any test content like "Build a CLI tool for project scaffolding")
- VERIFY: A 4-node horizontal pipeline renders showing stages (e.g., pending/running/complete states)
- Take a screenshot as evidence
- Record: PASS if pipeline visualization appears with stage indicators, FAIL with description if not

**UAT Item 2: Pipeline completion to scaffold modal**
- After a pipeline run completes (or navigate to a completed pipeline)
- VERIFY: A "Launch Execution" CTA button appears
- Click it and verify the scaffold modal opens with ideation context pre-populated
- Record: PASS if CTA appears and modal pre-populates, FAIL with description if not

**UAT Item 3: Autonomous execution visualization**
- Launch autonomous execution from the scaffold modal or equivalent entry point
- VERIFY: Real-time phase/commit streaming updates appear in the UI via SSE
- This tests the Plan 16-01 SSE fix (unnamed events instead of named events)
- Record: PASS if streaming updates render in real-time, FAIL with description if not

**UAT Item 4: Decision gate interaction**
- During autonomous execution, watch for a blocking decision gate
- VERIFY: A gate card renders with response options (approve/reject/etc.)
- If a gate appears, respond to it and verify execution resumes
- If no gate triggers during the test run, note this and check if gate UI component exists and renders
- Record: PASS if gate card renders correctly, FAIL or SKIP with explanation

**UAT Item 5: Multi-tab session management**
- Open the dashboard, look for the tab strip showing active sessions
- VERIFY: Tab strip shows sessions with status dots
- If possible, create multiple sessions and verify they appear as separate tabs
- Check if 10-tab cap is enforced (may need to inspect code if cannot create 10 sessions)
- Record: PASS if tab management works, FAIL with description if not

**UAT Item 6: Repo scaffold form validation**
- Open the repo scaffold form
- Test entering invalid names (e.g., spaces, special characters) and verify real-time validation feedback
- Test valid name/stack combinations
- VERIFY: Real-time validation messages appear, form prevents invalid submissions
- Note: Do NOT actually submit a valid form that would create a filesystem directory unless in a safe test state
- Record: PASS if validation works as expected, FAIL with description if not

**Step 3: Update 16-HUMAN-UAT.md with results**

For each UAT item, replace `[deferred - autonomous mode]` with the actual result:
- `[passed]` if the item works as expected
- `[passed - with notes: ...]` if it works but with minor observations
- `[failed: description of what went wrong]` if it does not work

Update the summary counts at the bottom of the file (passed, failed, etc.).

Update the frontmatter status from `deferred` to `completed`.

**Step 4: Stop dev servers**

Kill the background dev server processes when testing is complete.
  </action>
  <verify>
    <automated>grep -c "deferred" /Users/ryanstern/gstackapp/.planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 UAT items have a concrete result (no "deferred" entries remain)
    - 16-HUMAN-UAT.md summary counts are updated to reflect actual results
    - Frontmatter status changed from "deferred" to "completed"
  </acceptance_criteria>
  <done>All 6 UAT items exercised in browser, results recorded in 16-HUMAN-UAT.md, no deferred items remain</done>
</task>

<task type="auto">
  <name>Task 2: Fix any UAT failures and re-test</name>
  <files>.planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md (results from Task 1)
  </read_first>
  <action>
**If all 6 UAT items passed in Task 1:** Skip this task. Update 16-HUMAN-UAT.md summary to confirm all passed. No code changes needed.

**If any UAT items failed in Task 1:**

1. For each failed item, diagnose the root cause:
   - Read the relevant source files (check packages/web/src/components/ for UI issues, packages/api/src/routes/ for API issues)
   - Identify the specific bug or missing functionality
   
2. Fix the issue in the source code:
   - Make minimal, targeted fixes
   - Run `npx vitest run` in the relevant package to ensure existing tests still pass

3. Re-test the failed items:
   - Restart dev servers if needed
   - Use headless browser to re-exercise only the previously-failed items
   - Update 16-HUMAN-UAT.md results from `[failed: ...]` to `[passed]` or `[passed - with notes: ...]`

4. Update summary counts to reflect final state.

**If a failure cannot be fixed** (e.g., depends on external service, requires architecture change):
- Document the failure clearly with root cause
- Mark the item as `[blocked: reason]` in the UAT file
- Note whether this blocks PRE-02 satisfaction or is acceptable

**Final state of 16-HUMAN-UAT.md must have:**
- status: completed (frontmatter)
- All 6 items with non-deferred results
- Accurate summary counts
  </action>
  <verify>
    <automated>grep -E "^passed:" /Users/ryanstern/gstackapp/.planning/phases/16-prerequisites-stack-cleanup/16-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Any failures from Task 1 are either fixed and re-verified, or documented as blocked with root cause
    - 16-HUMAN-UAT.md has final pass/fail counts
    - PRE-02 status is clear: either satisfied (6/6 pass) or documented blockers
  </acceptance_criteria>
  <done>All fixable UAT failures resolved, 16-HUMAN-UAT.md reflects final state, PRE-02 requirement status is deterministic</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost dev servers | Local-only servers, no external exposure during testing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | T (Tampering) | UAT results file | accept | Local file, single user, results verified by executor screenshots |
| T-16-02 | D (DoS) | Dev server processes | accept | Background processes killed after testing; local only |
</threat_model>

<verification>
1. `grep -c "deferred" 16-HUMAN-UAT.md` returns 0 (no deferred items)
2. 16-HUMAN-UAT.md frontmatter shows `status: completed`
3. Summary counts in 16-HUMAN-UAT.md are consistent with individual item results
4. If all 6 passed: PRE-02 is satisfied
</verification>

<success_criteria>
- All 6 UAT items exercised via headless browser with evidence (screenshots or detailed observations)
- 16-HUMAN-UAT.md updated with concrete results replacing all "[deferred - autonomous mode]" entries
- PRE-02 requirement either satisfied (all pass) or has documented blockers with root causes
</success_criteria>

<output>
After completion, create `.planning/phases/16-prerequisites-stack-cleanup/16-04-SUMMARY.md`
</output>
