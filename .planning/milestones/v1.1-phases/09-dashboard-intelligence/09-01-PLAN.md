---
phase: 09-dashboard-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/health-colors.ts
  - packages/web/src/lib/action-hints.ts
  - packages/web/src/hooks/use-risks.ts
  - packages/web/src/components/risk-feed/risk-feed.tsx
  - packages/web/src/components/risk-feed/risk-card.tsx
  - packages/web/src/__tests__/components/risk-feed.test.tsx
  - packages/web/src/__tests__/lib/health-colors.test.ts
autonomous: true
requirements: [RISK-01, RISK-02, RISK-03]

must_haves:
  truths:
    - "Risk feed renders above departure board with critical findings first, then warning"
    - "Each risk card shows severity icon, project name, problem description, duration since detection, and a copy-command action hint"
    - "Clicking an action hint copies the git command to clipboard with brief visual feedback"
    - "Risk cards are non-dismissable -- no dismiss button exists"
    - "When no risks exist, a subtle green bar shows 'All projects healthy'"
    - "Findings detected in current scan cycle show a 'new' badge"
  artifacts:
    - path: "packages/web/src/lib/health-colors.ts"
      provides: "Severity-to-warm-palette color mapping shared across risk feed, health dots, findings panel"
      exports: ["SEVERITY_COLORS", "severityIcon"]
    - path: "packages/web/src/lib/action-hints.ts"
      provides: "Check type to git command mapping with metadata-aware defaults"
      exports: ["getActionCommand"]
    - path: "packages/web/src/hooks/use-risks.ts"
      provides: "Hook fetching /api/risks with fetchCounter SSE refetch pattern"
      exports: ["useRisks"]
    - path: "packages/web/src/components/risk-feed/risk-feed.tsx"
      provides: "Container rendering severity-grouped risk cards or clean bar"
      exports: ["RiskFeed"]
    - path: "packages/web/src/components/risk-feed/risk-card.tsx"
      provides: "Single-line finding card with copy-command action"
      exports: ["RiskCard"]
  key_links:
    - from: "packages/web/src/components/risk-feed/risk-feed.tsx"
      to: "/api/risks"
      via: "useRisks hook with fetchCounter"
      pattern: "client\\.api\\.risks\\.\\$get"
    - from: "packages/web/src/components/risk-feed/risk-card.tsx"
      to: "packages/web/src/lib/action-hints.ts"
      via: "getActionCommand(checkType, metadata)"
      pattern: "getActionCommand"
    - from: "packages/web/src/components/risk-feed/risk-card.tsx"
      to: "navigator.clipboard"
      via: "clipboard.writeText on action hint click"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
Build the risk feed component with shared health utilities, displaying severity-grouped findings above the departure board with copy-command action hints.

Purpose: Surfaces critical git health issues immediately on dashboard load -- the core "you're smarter in 3 seconds" value for risk awareness.
Output: RiskFeed component, useRisks hook, shared health-colors and action-hints utilities, component tests.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-intelligence/09-CONTEXT.md
@.planning/phases/09-dashboard-intelligence/09-RESEARCH.md
@.planning/phases/08-health-api-events/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/shared/src/schemas/health.ts:
```typescript
export const risksResponseSchema = z.object({
  critical: z.array(healthFindingResponseSchema),
  warning: z.array(healthFindingResponseSchema),
  riskCount: z.number(),
  summary: z.string(),
});

export const healthFindingResponseSchema = healthFindingSchema.extend({
  isNew: z.boolean(),
});
// healthFindingSchema fields: id, projectSlug, checkType, severity, detail, metadata, detectedAt, resolvedAt
```

From packages/web/src/api/client.ts:
```typescript
import { hc } from "hono/client";
import type { AppType } from "@mission-control/api";
type Client = ReturnType<typeof hc<AppType>>;
export const client: Client = hc<AppType>("");
```

From packages/web/src/hooks/use-heatmap.ts (fetchCounter pattern to follow):
```typescript
const [fetchCounter, setFetchCounter] = useState(0);
useEffect(() => { /* fetch logic */ }, [fetchCounter]);
const refetch = useCallback(() => setFetchCounter(c => c + 1), []);
```

From packages/web/src/lib/time.ts:
```typescript
export function formatRelativeTime(isoDate: string | null | undefined): string;
// "2 hours ago", "yesterday", "3 days ago"
```

From packages/web/src/app.css @theme tokens:
```css
--color-sage: #6b8f71;       /* healthy */
--color-gold-status: #c49b2a; /* warning */
--color-rust: #b7410e;        /* critical */
```

From packages/web/src/App.tsx layout (current order):
```
Capture → SprintHeatmap → HeroCard → DepartureBoard → LooseThoughts
// Target: Capture → RiskFeed → SprintTimeline → HeroCard → DepartureBoard → LooseThoughts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared health utilities and risk data hook</name>
  <files>
    packages/web/src/lib/health-colors.ts,
    packages/web/src/lib/action-hints.ts,
    packages/web/src/hooks/use-risks.ts,
    packages/web/src/__tests__/lib/health-colors.test.ts
  </files>
  <behavior>
    - SEVERITY_COLORS maps "critical" to rust, "warning" to gold-status, "healthy" to sage Tailwind classes (text, bg, border, dot, icon variants)
    - severityIcon returns appropriate SVG icon string/element for each severity level
    - getActionCommand("unpushed_commits", { branch: "develop" }) returns "git push origin develop"
    - getActionCommand("unpushed_commits", null) returns "git push origin main" (default branch)
    - getActionCommand("no_remote", null) returns 'git remote add origin <url>'
    - getActionCommand("broken_tracking", { branch: "feat" }) returns "git branch -u origin/feat"
    - getActionCommand("unpulled_commits", null) returns "git pull"
    - getActionCommand("dirty_working_tree", null) returns "git stash"
    - getActionCommand("diverged_copies", null) returns "git pull --rebase"
    - useRisks() returns { data, loading, refetch } following the fetchCounter pattern from use-heatmap.ts
  </behavior>
  <action>
    Create `packages/web/src/lib/health-colors.ts`:
    - Export `SEVERITY_COLORS` const object keyed by "critical" | "warning" | "healthy", each with text, bg, border, dot, icon Tailwind class strings. Use the warm palette: rust for critical, gold-status for warning, sage for healthy. Background classes use opacity: `bg-rust/10 dark:bg-rust/15`.
    - Export `severityIcon(severity)` function returning an inline SVG React element: exclamation triangle for critical (filled), exclamation circle for warning, check circle for healthy. Size: w-3.5 h-3.5.

    Create `packages/web/src/lib/action-hints.ts`:
    - Export `getActionCommand(checkType: string, metadata: Record<string, unknown> | null): string` that maps check types to git commands. Branch defaults to `metadata?.branch ?? "main"`. For `remote_branch_gone`, use `git checkout -b {branch} && git push -u origin {branch}`.

    Create `packages/web/src/hooks/use-risks.ts`:
    - Follow the exact fetchCounter pattern from `use-heatmap.ts`. Fetch `client.api.risks.$get()`. Return `{ data: RisksResponse | null, loading: boolean, refetch: () => void }`.
    - Define local `RisksResponse` interface matching the Zod schema shape (critical, warning arrays of findings, riskCount, summary). Cast API response `as unknown as RisksResponse` per existing project pattern.

    Create tests in `packages/web/src/__tests__/lib/health-colors.test.ts`:
    - Test SEVERITY_COLORS has entries for critical, warning, healthy with all class variants.
    - Test getActionCommand for each check type with and without metadata.
  </action>
  <verify>
    <automated>pnpm --filter @mission-control/web test -- --run src/__tests__/lib/health-colors.test.ts</automated>
  </verify>
  <done>SEVERITY_COLORS exports warm palette classes for 3 severity levels. getActionCommand maps all 7 check types to git commands with metadata-aware branch substitution. useRisks hook fetches /api/risks with refetch capability. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Risk feed components with tests</name>
  <files>
    packages/web/src/components/risk-feed/risk-feed.tsx,
    packages/web/src/components/risk-feed/risk-card.tsx,
    packages/web/src/__tests__/components/risk-feed.test.tsx
  </files>
  <behavior>
    - RiskFeed renders critical findings first, then warning findings
    - When riskCount is 0, renders a green bar with "All projects healthy" text
    - RiskCard shows severity icon (from health-colors), project name, problem description (detail), duration (from detectedAt via formatRelativeTime), and action hint button
    - No dismiss button renders on any card (RISK-03)
    - Findings with isNew=true show a "new" badge
    - Clicking the action hint button copies the command to clipboard and shows brief "Copied!" feedback
  </behavior>
  <action>
    Create `packages/web/src/components/risk-feed/risk-card.tsx`:
    - Single-line card layout: `[severity-icon] [project-name] [detail] [duration] [action-hint-button]`. All on one line using flex.
    - Project name styled as font-medium text-sm. Detail text styled as text-xs text-text-secondary.
    - Duration computed from `detectedAt` using `formatRelativeTime()` from `lib/time.ts`.
    - Action hint button: monospace text showing the git command (from `getActionCommand`). On click: `navigator.clipboard.writeText(command)`, show "Copied!" text for 1.5s via `useState<boolean>`.
    - If `isNew` is true, render a small "new" badge (text-[9px] uppercase bg-terracotta/15 text-terracotta rounded px-1) after the severity icon.
    - Background uses SEVERITY_COLORS[severity].bg. Left border uses SEVERITY_COLORS[severity].border (border-l-2).
    - No dismiss button or close icon anywhere.

    Create `packages/web/src/components/risk-feed/risk-feed.tsx`:
    - Props: `{ data: RisksResponse | null; loading: boolean }`.
    - When loading: return null (risk feed appears only when data is ready -- no skeleton for a status bar).
    - When `data` is null or `riskCount === 0`: render clean bar -- a rounded-xl div with sage/10 background, sage text, check icon, and "All projects healthy" message.
    - When findings exist: render section-divider header "Risk Feed", then critical cards first, then warning cards. Each card wrapped in a subtle container with rounded-lg, padding, and the severity background.
    - No wrapping animation -- simple static render. Cards appear/disappear with data changes (per discretion: simple fade transition via CSS).

    Create tests in `packages/web/src/__tests__/components/risk-feed.test.tsx`:
    - Test: renders "All projects healthy" when riskCount is 0
    - Test: renders critical findings before warning findings
    - Test: each card shows project name, severity icon, detail text, duration
    - Test: "new" badge appears for findings with isNew=true
    - Test: no dismiss button exists (query for "dismiss"/"close" button, assert not found)
    - Test: action hint button renders with correct git command text
    - Mock navigator.clipboard.writeText for copy test
  </action>
  <verify>
    <automated>pnpm --filter @mission-control/web test -- --run src/__tests__/components/risk-feed.test.tsx</automated>
  </verify>
  <done>RiskFeed renders severity-grouped cards (critical first) with project name, detail, duration, action hint. Clean bar when healthy. "new" badge for current-cycle findings. No dismiss buttons. Copy-to-clipboard works. All tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @mission-control/web test` -- all component and utility tests pass
- `pnpm typecheck` -- no type errors
- Risk feed imports resolve and components render without runtime errors
</verification>

<success_criteria>
- RISK-01: Risk feed renders with severity-grouped cards (critical first)
- RISK-02: Each card shows severity icon, project name, problem description, duration, action hint
- RISK-03: Cards have no dismiss mechanism -- only disappear when API data changes
- Shared health-colors and action-hints utilities are available for Plan 03 (health dots)
- useRisks hook ready for App.tsx wiring in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-intelligence/09-01-SUMMARY.md`
</output>
