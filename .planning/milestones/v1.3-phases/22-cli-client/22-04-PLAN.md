# Plan 22-04: Tests & Integration Verification

```yaml
wave: 3
depends_on: [22-01, 22-02, 22-03]
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06, CLI-07, CLI-08, CLI-09]
files_modified:
  - packages/cli/src/__tests__/config.test.ts
  - packages/cli/src/__tests__/queue.test.ts
  - packages/cli/src/__tests__/api-client.test.ts
  - packages/cli/src/__tests__/project-detect.test.ts
  - packages/cli/src/__tests__/output.test.ts
  - packages/cli/src/__tests__/commands/capture.test.ts
  - packages/cli/vitest.config.ts
autonomous: true
```

## Objective

Add comprehensive unit tests for all CLI modules, verify the build produces a working binary, and validate `pnpm install` / `pnpm build` / `pnpm test` all pass for the new package.

## Tasks

### Task 1: Create vitest config

<read_first>
- packages/mcp/vitest.config.ts (if exists — reference for test config pattern)
- packages/api/vitest.config.ts (reference for vitest config in this monorepo)
</read_first>

<action>
Create `packages/cli/vitest.config.ts`:

```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    globals: true,
    environment: "node",
  },
});
```
</action>

<acceptance_criteria>
- `packages/cli/vitest.config.ts` exists
- Contains `environment: "node"`
</acceptance_criteria>

### Task 2: Create config tests

<read_first>
- packages/cli/src/config.ts (loadConfig, saveConfig, getApiUrl, getConfigDir, getConfigPath, ensureConfigDir)
</read_first>

<action>
Create `packages/cli/src/__tests__/config.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { existsSync, mkdirSync, readFileSync, writeFileSync } from "node:fs";
import { join } from "node:path";
import { tmpdir } from "node:os";
import { rmSync } from "node:fs";

// We test config by overriding the home directory
// Since config.ts uses homedir(), we mock os.homedir
vi.mock("node:os", async () => {
  const actual = await vi.importActual("node:os");
  return {
    ...actual,
    homedir: vi.fn(),
  };
});

import { homedir } from "node:os";
const mockHomedir = vi.mocked(homedir);

describe("config", () => {
  let tempDir: string;

  beforeEach(() => {
    tempDir = join(tmpdir(), `mc-test-${Date.now()}`);
    mkdirSync(tempDir, { recursive: true });
    mockHomedir.mockReturnValue(tempDir);
    // Re-import to pick up mocked homedir
    vi.resetModules();
  });

  afterEach(() => {
    rmSync(tempDir, { recursive: true, force: true });
  });

  it("loadConfig returns null when no config file exists", async () => {
    const { loadConfig } = await import("../config.js");
    expect(loadConfig()).toBeNull();
  });

  it("saveConfig creates ~/.mc directory and writes config", async () => {
    const { saveConfig, loadConfig } = await import("../config.js");
    saveConfig({ apiUrl: "http://localhost:3000" });
    const config = loadConfig();
    expect(config).toEqual({ apiUrl: "http://localhost:3000" });
  });

  it("getApiUrl returns default when no config", async () => {
    const { getApiUrl } = await import("../config.js");
    expect(getApiUrl()).toBe("http://100.123.8.125:3000");
  });

  it("getApiUrl returns configured URL when config exists", async () => {
    const { saveConfig, getApiUrl } = await import("../config.js");
    saveConfig({ apiUrl: "http://custom:4000" });
    const { getApiUrl: getApiUrl2 } = await import("../config.js");
    // Need fresh import due to module-level constants
    // Direct file check instead:
    const configPath = join(tempDir, ".mc", "config.json");
    expect(existsSync(configPath)).toBe(true);
    const content = JSON.parse(readFileSync(configPath, "utf-8"));
    expect(content.apiUrl).toBe("http://custom:4000");
  });

  it("loadConfig returns null for corrupt JSON", async () => {
    const mcDir = join(tempDir, ".mc");
    mkdirSync(mcDir, { recursive: true });
    writeFileSync(join(mcDir, "config.json"), "not json", "utf-8");
    const { loadConfig } = await import("../config.js");
    expect(loadConfig()).toBeNull();
  });
});
```
</action>

<acceptance_criteria>
- `packages/cli/src/__tests__/config.test.ts` exists
- Tests: null config when no file, save+load roundtrip, default API URL, corrupt JSON handling
- Uses temp directory (not actual ~/.mc)
- At least 4 test cases
</acceptance_criteria>

### Task 3: Create queue tests

<read_first>
- packages/cli/src/queue.ts (enqueue, readQueue, clearQueue, queueCount)
</read_first>

<action>
Create `packages/cli/src/__tests__/queue.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { mkdirSync, rmSync, existsSync, readFileSync } from "node:fs";
import { join } from "node:path";
import { tmpdir } from "node:os";

vi.mock("node:os", async () => {
  const actual = await vi.importActual("node:os");
  return {
    ...actual,
    homedir: vi.fn(),
  };
});

import { homedir } from "node:os";
const mockHomedir = vi.mocked(homedir);

describe("queue", () => {
  let tempDir: string;

  beforeEach(() => {
    tempDir = join(tmpdir(), `mc-queue-test-${Date.now()}`);
    mkdirSync(tempDir, { recursive: true });
    mockHomedir.mockReturnValue(tempDir);
    vi.resetModules();
  });

  afterEach(() => {
    rmSync(tempDir, { recursive: true, force: true });
  });

  it("readQueue returns empty array when no queue file", async () => {
    const { readQueue } = await import("../queue.js");
    expect(readQueue()).toEqual([]);
  });

  it("enqueue creates queue.jsonl with entry", async () => {
    const { enqueue, readQueue } = await import("../queue.js");
    enqueue({ rawContent: "test thought", queuedAt: "2026-01-01T00:00:00Z" });
    const items = readQueue();
    expect(items).toHaveLength(1);
    expect(items[0].rawContent).toBe("test thought");
    expect(items[0].queuedAt).toBe("2026-01-01T00:00:00Z");
  });

  it("enqueue appends multiple entries", async () => {
    const { enqueue, readQueue } = await import("../queue.js");
    enqueue({ rawContent: "first", queuedAt: "2026-01-01T00:00:00Z" });
    enqueue({ rawContent: "second", queuedAt: "2026-01-01T00:01:00Z" });
    const items = readQueue();
    expect(items).toHaveLength(2);
    expect(items[0].rawContent).toBe("first");
    expect(items[1].rawContent).toBe("second");
  });

  it("clearQueue removes the file", async () => {
    const { enqueue, clearQueue } = await import("../queue.js");
    enqueue({ rawContent: "test", queuedAt: "2026-01-01T00:00:00Z" });
    clearQueue();
    const queuePath = join(tempDir, ".mc", "queue.jsonl");
    expect(existsSync(queuePath)).toBe(false);
  });

  it("queueCount returns 0 when no file", async () => {
    const { queueCount } = await import("../queue.js");
    expect(queueCount()).toBe(0);
  });

  it("queueCount returns correct count", async () => {
    const { enqueue, queueCount } = await import("../queue.js");
    enqueue({ rawContent: "one", queuedAt: "2026-01-01T00:00:00Z" });
    enqueue({ rawContent: "two", queuedAt: "2026-01-01T00:01:00Z" });
    expect(queueCount()).toBe(2);
  });

  it("enqueue preserves optional projectId", async () => {
    const { enqueue, readQueue } = await import("../queue.js");
    enqueue({ rawContent: "thought", projectId: "mission-control", queuedAt: "2026-01-01T00:00:00Z" });
    const items = readQueue();
    expect(items[0].projectId).toBe("mission-control");
  });
});
```
</action>

<acceptance_criteria>
- `packages/cli/src/__tests__/queue.test.ts` exists
- Tests: empty queue, single enqueue, multiple enqueue, clearQueue, queueCount, projectId preservation
- Uses temp directory
- At least 6 test cases
</acceptance_criteria>

### Task 4: Create API client tests

<read_first>
- packages/cli/src/api-client.ts (createCapture, listProjects, listSessions, checkHealth, McApiError, McApiUnreachable)
</read_first>

<action>
Create `packages/cli/src/__tests__/api-client.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";

// Mock config to return a test URL
vi.mock("../config.js", () => ({
  getApiUrl: () => "http://localhost:9999",
}));

// Mock global fetch
const mockFetch = vi.fn();
vi.stubGlobal("fetch", mockFetch);

import {
  createCapture,
  listProjects,
  listSessions,
  checkHealth,
  McApiError,
  McApiUnreachable,
} from "../api-client.js";

describe("api-client", () => {
  beforeEach(() => {
    mockFetch.mockReset();
  });

  describe("createCapture", () => {
    it("sends POST to /api/captures with content", async () => {
      mockFetch.mockResolvedValue({
        ok: true,
        json: async () => ({ capture: { id: "abc", rawContent: "test", projectId: null } }),
      });

      const result = await createCapture({ rawContent: "test thought" });
      expect(mockFetch).toHaveBeenCalledWith(
        "http://localhost:9999/api/captures",
        expect.objectContaining({
          method: "POST",
          body: JSON.stringify({ rawContent: "test thought" }),
        })
      );
      expect(result.capture.id).toBe("abc");
    });

    it("throws McApiError on 400", async () => {
      mockFetch.mockResolvedValue({
        ok: false,
        status: 400,
        json: async () => ({ error: { code: "VALIDATION", message: "Bad input" } }),
      });

      await expect(createCapture({ rawContent: "" })).rejects.toThrow(McApiError);
    });
  });

  describe("listProjects", () => {
    it("fetches GET /api/projects", async () => {
      mockFetch.mockResolvedValue({
        ok: true,
        json: async () => ({ projects: [{ slug: "mc", name: "Mission Control" }] }),
      });

      const result = await listProjects();
      expect(mockFetch).toHaveBeenCalledWith(
        "http://localhost:9999/api/projects",
        expect.objectContaining({ method: undefined })
      );
      expect(result.projects).toHaveLength(1);
    });
  });

  describe("listSessions", () => {
    it("fetches GET /api/sessions with status filter", async () => {
      mockFetch.mockResolvedValue({
        ok: true,
        json: async () => ({ sessions: [], total: 0 }),
      });

      await listSessions("active");
      expect(mockFetch).toHaveBeenCalledWith(
        "http://localhost:9999/api/sessions?status=active",
        expect.anything()
      );
    });
  });

  describe("checkHealth", () => {
    it("returns true when API is reachable", async () => {
      mockFetch.mockResolvedValue({
        ok: true,
        json: async () => ({ status: "ok" }),
      });

      expect(await checkHealth()).toBe(true);
    });

    it("returns false when API is unreachable", async () => {
      mockFetch.mockRejectedValue(new Error("ECONNREFUSED"));
      expect(await checkHealth()).toBe(false);
    });
  });

  describe("McApiUnreachable", () => {
    it("is thrown on network errors", async () => {
      mockFetch.mockRejectedValue(new TypeError("fetch failed"));
      await expect(listProjects()).rejects.toThrow(McApiUnreachable);
    });
  });
});
```
</action>

<acceptance_criteria>
- `packages/cli/src/__tests__/api-client.test.ts` exists
- Mocks global `fetch` (not real network calls)
- Tests: createCapture POST, listProjects GET, listSessions with filter, checkHealth true/false, McApiUnreachable on network error
- At least 6 test cases
</acceptance_criteria>

### Task 5: Create output tests

<read_first>
- packages/cli/src/output.ts (colors, success, warn, error, table, relativeTime)
</read_first>

<action>
Create `packages/cli/src/__tests__/output.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";

describe("output", () => {
  describe("relativeTime", () => {
    it("returns 'never' for null", async () => {
      const { relativeTime } = await import("../output.js");
      expect(relativeTime(null)).toBe("never");
    });

    it("returns relative time for recent date", async () => {
      const { relativeTime } = await import("../output.js");
      const fiveMinAgo = new Date(Date.now() - 5 * 60 * 1000).toISOString();
      expect(relativeTime(fiveMinAgo)).toBe("5m ago");
    });

    it("returns hours for dates within a day", async () => {
      const { relativeTime } = await import("../output.js");
      const threeHoursAgo = new Date(Date.now() - 3 * 60 * 60 * 1000).toISOString();
      expect(relativeTime(threeHoursAgo)).toBe("3h ago");
    });

    it("returns days for dates within a month", async () => {
      const { relativeTime } = await import("../output.js");
      const fiveDaysAgo = new Date(Date.now() - 5 * 24 * 60 * 60 * 1000).toISOString();
      expect(relativeTime(fiveDaysAgo)).toBe("5d ago");
    });
  });

  describe("colors with NO_COLOR", () => {
    it("strips color when NO_COLOR is set", async () => {
      process.env["NO_COLOR"] = "1";
      vi.resetModules();
      const { colors } = await import("../output.js");
      expect(colors.green("test")).toBe("test");
      expect(colors.red("test")).toBe("test");
      delete process.env["NO_COLOR"];
    });
  });
});
```
</action>

<acceptance_criteria>
- `packages/cli/src/__tests__/output.test.ts` exists
- Tests relativeTime with null, minutes, hours, days
- Tests NO_COLOR env var strips ANSI codes
- At least 5 test cases
</acceptance_criteria>

### Task 6: Create project-detect tests

<read_first>
- packages/cli/src/project-detect.ts (detectProjectFromCwd)
- packages/cli/src/api-client.ts (listProjects response shape, McApiUnreachable)
</read_first>

<action>
Create `packages/cli/src/__tests__/project-detect.test.ts`:

```typescript
import { describe, it, expect, vi } from "vitest";

vi.mock("../api-client.js", () => ({
  listProjects: vi.fn(),
  McApiUnreachable: class extends Error {
    constructor() {
      super("unreachable");
      this.name = "McApiUnreachable";
    }
  },
}));

import { listProjects, McApiUnreachable } from "../api-client.js";
import { detectProjectFromCwd } from "../project-detect.js";

const mockListProjects = vi.mocked(listProjects);

describe("detectProjectFromCwd", () => {
  it("returns slug when cwd matches project path", async () => {
    mockListProjects.mockResolvedValue({
      projects: [
        { slug: "mission-control", name: "Mission Control", path: "/Users/ryan/mission-control", host: "local", lastCommitTime: null, lastCommitDate: null, lastCommitMessage: null, healthScore: null, riskLevel: "healthy" },
      ],
    });

    const slug = await detectProjectFromCwd("/Users/ryan/mission-control");
    expect(slug).toBe("mission-control");
  });

  it("returns slug when cwd is subdirectory of project", async () => {
    mockListProjects.mockResolvedValue({
      projects: [
        { slug: "mission-control", name: "Mission Control", path: "/Users/ryan/mission-control", host: "local", lastCommitTime: null, lastCommitDate: null, lastCommitMessage: null, healthScore: null, riskLevel: "healthy" },
      ],
    });

    const slug = await detectProjectFromCwd("/Users/ryan/mission-control/packages/cli");
    expect(slug).toBe("mission-control");
  });

  it("returns longest match when multiple projects match", async () => {
    mockListProjects.mockResolvedValue({
      projects: [
        { slug: "parent", name: "Parent", path: "/Users/ryan", host: "local", lastCommitTime: null, lastCommitDate: null, lastCommitMessage: null, healthScore: null, riskLevel: "healthy" },
        { slug: "child", name: "Child", path: "/Users/ryan/child-project", host: "local", lastCommitTime: null, lastCommitDate: null, lastCommitMessage: null, healthScore: null, riskLevel: "healthy" },
      ],
    });

    const slug = await detectProjectFromCwd("/Users/ryan/child-project/src");
    expect(slug).toBe("child");
  });

  it("returns null when no project matches", async () => {
    mockListProjects.mockResolvedValue({
      projects: [
        { slug: "other", name: "Other", path: "/somewhere/else", host: "local", lastCommitTime: null, lastCommitDate: null, lastCommitMessage: null, healthScore: null, riskLevel: "healthy" },
      ],
    });

    const slug = await detectProjectFromCwd("/Users/ryan/untracked");
    expect(slug).toBeNull();
  });

  it("returns null when API is unreachable", async () => {
    mockListProjects.mockRejectedValue(new McApiUnreachable());
    const slug = await detectProjectFromCwd("/Users/ryan/mission-control");
    expect(slug).toBeNull();
  });
});
```
</action>

<acceptance_criteria>
- `packages/cli/src/__tests__/project-detect.test.ts` exists
- Tests: exact path match, subdirectory match, longest prefix match, no match, API unreachable
- Mocks `listProjects` (no real API calls)
- At least 5 test cases
</acceptance_criteria>

### Task 7: Build verification and pnpm install

<read_first>
- packages/cli/package.json (scripts, dependencies)
- packages/cli/tsup.config.ts (build config)
</read_first>

<action>
Run the following verification sequence:

1. `cd /Users/ryanstern/mission-control && pnpm install` — Install cli package dependencies
2. `pnpm --filter @mission-control/cli typecheck` — Verify TypeScript compilation
3. `pnpm --filter @mission-control/cli build` — Verify tsup produces dist/index.js
4. Verify built binary: `test -f packages/cli/dist/index.js && head -1 packages/cli/dist/index.js` — Should show `#!/usr/bin/env node`
5. `pnpm --filter @mission-control/cli test` — Run all unit tests
6. `pnpm typecheck` — Verify no type regressions across entire monorepo
7. `pnpm test` — Verify no test regressions across entire monorepo

Fix any issues found during verification. Common issues to watch for:
- Missing `@mission-control/shared` workspace dependency (add `"@mission-control/shared": "workspace:*"` to package.json if shared types are imported)
- tsup may need `external` adjustments if node builtins need excluding
- Commander.js types may need `@types/commander` (usually not — Commander ships types)
</action>

<acceptance_criteria>
- `pnpm install` completes without errors
- `pnpm --filter @mission-control/cli typecheck` passes
- `pnpm --filter @mission-control/cli build` produces `dist/index.js`
- `head -1 packages/cli/dist/index.js` shows `#!/usr/bin/env node`
- `pnpm --filter @mission-control/cli test` passes all tests
- `pnpm typecheck` passes (no monorepo-wide regressions)
- `pnpm test` passes (no monorepo-wide regressions)
</acceptance_criteria>

## Verification

```bash
# Tests pass
pnpm --filter @mission-control/cli test 2>&1 | tail -5

# Build produces shebang binary
head -1 packages/cli/dist/index.js | grep -q '#!/usr/bin/env node' && echo "PASS: shebang"

# All test files exist
ls packages/cli/src/__tests__/*.test.ts | wc -l | grep -q '[5-9]' && echo "PASS: 5+ test files"

# No monorepo regressions
pnpm typecheck 2>&1 | tail -3
pnpm test 2>&1 | tail -5
```

## must_haves

- [ ] Unit tests cover config, queue, api-client, project-detect, output modules
- [ ] All tests pass (`pnpm --filter @mission-control/cli test`)
- [ ] Build produces `dist/index.js` with shebang
- [ ] TypeScript strict mode passes
- [ ] No regressions in other packages
